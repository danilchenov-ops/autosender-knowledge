# Интеграции и доступы

Токенов и паролей здесь нет — только где что лежит.

## Яндекс Директ

- Рекламный аккаунт: **synergosmoto**, боевой, общий счёт
- Доступ у Claude: MCP `yandex_direct` (`direct_campaigns`, `direct_report`, `direct_api`)
- Запись включается на маке Тимофея:
  `sed -i '' 's/^YANDEX_DIRECT_ALLOW_WRITE=no/YANDEX_DIRECT_ALLOW_WRITE=yes/' ~/.config/yandex-mcp/.env`
  Сервер читает `.env` на лету, перезапуск не нужен.
- Расход в Директе **с НДС 22 %**, в Метрике — **без НДС**. Всегда указывать базис.

## Яндекс Метрика

- Счётчик **56331115** «Autosender.ru», владелец `synergosmoto`, TZ Asia/Vladivostok
- Доступ у Claude: MCP `yandex_metrika`
- ~105 городских зеркал вида `<город>.autosender.ru`.
  **`mes.autosender.ru` в списке зеркал НЕ значится** — на сбор данных не влияет.

### Ключевые цели

| Цель | Id | Что |
|---|---|---|
| `sub` | 598775226 | подписка (MAX или Telegram), прокладка |
| `sub_max` / `sub_tg` | см. `metrika_goals` | по каналам |
| Подобрать автомобиль+ | 63078325 | заявка |
| CRM: Заказ создан+ | 255698793 | |
| CRM: Заказ оплачен+ | 255698794 | ценность в Метрике 40 000 ₽, фактическая — 100 000 ₽ |

### Сегменты для ретаргетинга

| Сегмент | Id | Размер, 90 дн. |
|---|---|---|
| Калькулятор или избранное без заказа | 1008072855 | 6 621 чел. (не-десктоп 4 529) |
| Вовлечённые без заказа | 1008072850 | 135 099 чел. (не-десктоп 89 625) |

## Битрикс24

- Вебхук в `/opt/ropbot/.env`, переменная `B24_WEBHOOK`
- Используется коллектором Виртуального РОПа для истории звонков

## Telegram

- Канал подписки, пригласительная ссылка прокладки `https://t.me/+WZfPxLy5sD1lZmMy`
- Базовая точка: **6 762 подписчика на 18.08.2026** (по старой ссылке)
- Замер **6 795 подписчиков на 31.08.2026** (со слов Тимофея). Прирост за 14 дней
  работы подписных кампаний — **+33**. Метрика за тот же период насчитала
  54 достижения цели `sub_tg` (47 с рекламы). Сходимость 61–70 %: цель считает
  реальные подписки, разница — отписки и погрешность. **Это единственный канал,
  по которому подписные кампании можно сверить с фактом.**
- Бот Виртуального РОПа: контейнер `ropbot-tgbot-1`

## MAX

- Канал `https://max.ru/channel_autosender`
- Аналитики по ссылкам нет. Мерить только целью `sub_max`
- 18.08.2026 шёл посев — прирост подписчиков за эту дату не показателен
- **Посевы в группах идут постоянно (Тимофей, 31.08.2026), поэтому прирост канала
  MAX сверить с рекламой нельзя в принципе.** Цель `sub_max` (287 достижений
  за 18–31.08) остаётся единственным измерением, проверить его нечем.

### Замеры подписчиков MAX

| Дата (MSK) | Подписчиков | Прирост | Источник | Примечание |
|---|---:|---:|---|---|
| 31.08.2026 ~08:36 | **2 113** | — | со слов Тимофея | первая зафиксированная точка |
| 01.09.2026 ~08:49 | **2 170** | **+57** | со слов Тимофея | за сутки; Метрика ≈ 28 `sub_max` |
| 07.09.2026 | **2 208** | **+38** | со слов Тимофея | за 6 дней; Метрика 177 `sub_max` — **сходимость 21 %** |

### Точный учёт подписок Telegram (с 07.09.2026)

Бот `@ironbossAS_bot` — администратор канала `-1001774094574`. Каждое вступление
и выход пишется в `tg_channel_members` (БД `rop`), сводка — `v_tg_subs_daily`.
Пригласительные ссылки с метками — `tg_invite_links`. **Цель Метрики `sub_tg`
больше не источник правды — это клики.** Считать подписчиков только по базе.

```sql
SELECT day_msk, invite_name, joins, leaves, net FROM v_tg_subs_daily
WHERE day_msk >= current_date - 7 ORDER BY 1,2;
```

### Замеры подписчиков Telegram

| Дата (MSK) | Подписчиков | Прирост | Метрика `sub_tg` | Сходимость |
|---|---:|---:|---:|---:|
| 18.08.2026 | 6 762 | — | — | — |
| 31.08.2026 | 6 795 | +33 | 47 | 70 % |
| 07.09.2026 | 6 808 | **+13** | 32 | **41 %** |

**Сходимость падает.** 18–31.08 Telegram сходился на 70 %, 31.08–07.09 — уже 41 %,
MAX — 21 %. Причём у MAX посевы работают в плюс к приросту, значит рекламных
подписчиков там ещё меньше, чем +38.

**Рабочая гипотеза (не проверена): цель `sub` (598775226) считает клик по кнопке
перехода, а не факт подписки в мессенджере.** Тогда все расчёты цены подписчика
по целям Метрики завышают результат в 2–5 раз. Проверять механику цели в счётчике.
| 03.09.2026 ~06:30 | **2 188** | **+18** | со слов Тимофея | за 2 суток от 01.09 |

**Сверка 31.08 → 03.09 (≈2 суток 22 часа):** прирост канала **+75**,
`sub_max` за тот же интервал (с поправкой на Asia/Vladivostok) **≈72**.
Сходится почти один в один. Осторожно: `sub_max` считает КЛИКИ по кнопке,
а не подписки, и конверсия клик→подписка заведомо ниже 100 %. Значит часть
прироста всё же даёт посев, а часть кликов до подписки не доходит —
две погрешности гасят друг друга. Вывод, который можно сделать честно:
**цель `sub_max` не завышена, порядок величины верный.**
Предыдущий замер 31.08→01.09 давал соотношение 2:1 (57 против 28) —
одни сутки оказались нерепрезентативны, как и предполагалось.

**Сутки — слишком короткий интервал для вывода**, плюс замеры по MSK, а счётчик
живёт по Asia/Vladivostok (+7 ч), поэтому границы периода не совпадают.
Наблюдение, не факт: прирост канала примерно вдвое больше засчитанных целей —
похоже, посев даёт объём, сопоставимый с рекламой. Проверять на интервале
от недели, замеряя в одно и то же время суток.

**Назад сверить нечем** — значения на 18.08.2026 (старт подписных кампаний)
в реестре нет. Точка 2 113 — база для следующего замера.

Чтобы следующая сверка была чистой, нужны два числа: замер канала на дату
и **прирост от посева в группах за тот же период** (Тимофей обещал назвать).
Формула: `подписки с рекламы ≈ прирост канала − прирост от посева + отписки`.
Без второго слагаемого цифра `sub_max` остаётся непроверяемой.
