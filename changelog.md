# Журнал изменений

Только дописываем сверху вниз. Формат: дата — что — кто — зачем.

---

## 2026-08-20

- **07:40** — Тимофей выдал Claude **root-доступ по SSH на 195.2.74.207**
  (ключ `root@danilchenov.fvds.ru` в authorized_keys). Проверено, работает.
  Теперь правки страницы-прокладки и конфигов сайта возможны без ручных действий.
  Отзыв — удалением строки из authorized_keys.
- **07:35** — в реестр добавлены `projects/` (5 заготовок), `inbox/`, обновлён INDEX.

- **07:27** — создан реестр `/opt/knowledge`, git-репозиторий. Claude.
  Цель: единый источник правды между чатами.
- **утро** — на прокладке заменена пригласительная ссылка Telegram
  на `https://t.me/+WZfPxLy5sD1lZmMy`. Тимофей, вручную на 195.2.74.207.
  Проверено снаружи: обе кнопки отдают новую ссылку.
- поставлена разовая задача на 11:00 МСК — срез подписных РСЯ-кампаний.

## 2026-08-19

- **Сняты избыточные ограничения с подписных РСЯ-кампаний** (713638280, 713639252).
  Через API, Claude. Удалены 12 корректировок: десктоп −100 %, iOS −40 %,
  18–24, 35–44, 45–54, 55+. Оставлена только «до 18 лет −100 %».
  Запрещённые площадки сокращены с 569 до 1 (пустой список API не принимает,
  оставлена заглушка `11lordserial-0.xyz`).
  «Вовлеченные» приведены к «Максимум кликов» с недельным бюджетом 10 000 ₽
  без потолка цены клика, мониторинг сайта выключен.
  **Результат: показы пошли в тот же час после полутора суток нуля.**
- Из группы «Горячих» удалено контрольное условие 35706369.

## 2026-08-18

- Запущены кампании «Подписка РСЯ Горячие» (713638280, 5 000 ₽/нед) и
  «Подписка РСЯ Вовлеченные» (713639252, 10 000 ₽/нед).
- Созданы условия ретаргетинга 41983064 «Горячие без подписки» и
  41983669 «Вовлечённые без подписки».
- Запущен Виртуальный РОП на 5.35.99.93, добавлен крон скоринга заявок.
- **20.08.2026** — перенесён контекст чата «Заявки scoring система» в projects/virtualnyy-rop.md
- **20.08.2026** — перенесён контекст чата «Система анализа звонков и продаж» в projects/zvonki-transkribaciya.md
- **20.08.2026** — перенесён контекст чата «Аудит Яндекс.Директа и Метрики» в projects/reklama.md
- **20.08.2026** — перенесён контекст чата «Подключение API Директа и запуск тестовых кампаний» в projects/reklama.md
- **20.08.2026** — перенесён контекст чата «CRM Bitrix распределение менеджеров» в projects/bitrix-analitika.md
- **20.08.2026** — перенесён контекст чата «CRM Bitrix распределение менеджеров» в projects/bitrix-analitika.md (дополнение: боевая статистика за 4 дня, грабли эксплуатации, порядок обновления кода, решения по инфраструктуре)
- **20.08.2026** — фильтр машинного мусора в lead-router: правила, замеры на истории, решения. Записано в projects/bitrix-analitika.md
- **20.08.2026** — поле «Звонить после» в lead-router: регион по IP-городу и коду номера, справочник номерной ёмкости, пересчёт на Владивосток. Записано в projects/bitrix-analitika.md
- **20.08.2026** — повторные обращения в lead-router: тому же менеджеру без траты хода в круге, связь карточек комментариями. Отвергнуто схлопывание дублей — проверка показала, что деньги приходят со второго лида. Записано в projects/bitrix-analitika.md
- **20.08.2026** — зафиксировано разграничение: `/opt/knowledge` — реестр Автосендера,
  база `org` на 92.63.100.125 — оркестрация и оконный завод. Записано в decisions.md
- **20.08.2026** — онлайн-отбор в lead-router распространён на будни 10:00-18:00 по Владивостоку; вне окна раздаём всем. Записано в projects/bitrix-analitika.md
- **20.08.2026** — портрет менеджера: миграция `013_profile.sql` (функция рабочих минут,
  таблица присутствия, представления `v_lead_worked`, `v_manager_weekly`,
  `v_manager_result`, `v_manager_online_daily`), `presence.py` (опрос `IS_ONLINE`
  каждые 5 минут — у вебхука нет прав `timeman`), `profile.py` (недельный отчёт в
  Телеграм, крон пн 09:30 Влд), чек-лист качества разговора добавлен в промпт
  `extractor.py` с записью в `call_scores` (таблица существовала с начала, но
  в неё никто не писал — 0 строк), приоритет очереди расшифровки `db/asr_priority.sql`.
  Записано в projects/portret-menedzhera.md
- **20.08.2026** — вебхуку Битрикса выданы права `timeman`. Проверка: метод отвечает,
  но табель пустой — отдел продаж не отмечается (последние отметки 2025–июль 2026,
  у троих из шести нет вообще), `timecontrol` выключен. Приёмник готов:
  `presence.py` опрашивает `timeman.status`, данные копятся в `timeman_days`,
  столбец «Таб,ч» появится в отчёте автоматически, когда отметки пойдут.
- **20.08.2026** — месячная оценка менеджеров: `app/monthly.py` (сводка, ранг по восьми
  управляемым метрикам, выбросы, лучший и худший разговор со ссылками на карточки,
  разбивка длинного сообщения). Первый прогон разослан всем руководителям.
  Найдено: Мирошниченко — 0 исходящих звонков, 523 заявки с 1 июня, 352 из них
  не набрал никто; Попов — 12% брошенных против 4,7% у следующего.
  Записано в projects/portret-menedzhera.md
- **21.08.2026** — персональный разбор менеджера Владимира (М8) по расшифровкам:
  три повторяющиеся ошибки с цитатами из разговоров и ссылками на карточки,
  отправлен Тимофею и Андрею. Основное: следующий шаг с датой 3 раза из 26,
  за месяц взял 5 обещаний против 34–38 у остальных; на «дорого» соглашается
  и прощается. Инструмент отправки готового текста — `app/send_text.py`.
- **21.08.2026** — заведено направление «Эталонный скрипт»: пайплайн из четырёх
  промтов (карточка разговора → сравнение WON/LOST → сборка скрипта → валидация
  на отложенной выборке). Код в `/opt/ropbot/lab/`, таблицы `lab_sample`,
  `lab_cards`, `lab_bench`, продовые не тронуты. Разобрано 386 первых разговоров
  (WON 117+35, LOST 163+71), ошибок ноль, банк из 1587 проверенных цитат.
  Модель шага 1 выбрана по бенчу — `gemini-3.7-flash` с `thinkingBudget: 0`:
  отключение размышлений уронило выход в 6,2 раза при цитатности 98%.
  Записано в projects/etalonnyy-skript.md
- **21.08.2026** — на ProxyAPI кончился баланс, продовый разбор разговоров
  (`ropbot-llm-1`) стоял с 03:13 Влд и крутил 402 в цикле, последняя запись
  в `call_extractions` — 20.08 17:13. Тимофей пополнил. Отдельно: первый прогон
  промта 1 съел 5000 ₽ за десять минут, потому что не были отключены
  «размышления» модели (80% расхода — выходные токены). В `runner.py` вшит
  предохранитель `--max-rub`: считает расход на ходу и останавливается сам,
  не дожидаясь 402.
- **21.08.2026** — найден перекос контрольной группы по времени: WON-разговоры
  май-июль, LOST на 97,5% август. Причина — `asr_worker.py` берёт очередь
  `ORDER BY priority DESC, call_start DESC`, свежие первыми, а контрольная группа
  встала в очередь недавно. Сравнивать нельзя: при медиане цикла сделки 8,6 дня
  августовский отказ — быстрый отказ, майский — долгая несложившаяся работа
  (правило 6 реестра). `lab/match_queue.py` подобрал зеркальную пару — под каждый
  из 139 WON-разговоров два LOST того же месяца, источника и менеджера, 278 штук
  без промахов, подняты в очереди ASR приоритетами 9 и 8.
- **21.08.2026** — разбор «анатомия идеальной сделки», новый файл
  `projects/idealnaya-sdelka.md` + артефакт
  https://claude.ai/code/artifact/517671ce-8799-4814-82b8-ec72a3e0f799
  Окна: конверсии 08.2025–06.2026 (33 190 заявок / 355 покупок, база 1,07%),
  касания 06–07.2026 (7 828 / 88), поведение 20.02–30.06.2026 (6 450 / 83).
  Главное: при несостоявшемся первом дозвоне 5+ попыток дают 4,41% против 0,26%
  при ≤4 (×16,7, Фишер p=2,4e-06) — сильнейший управляемый фактор в базе.
  Найдены четыре дефекта данных: (1) `opportunity` не заполняется — 0 из 392
  выигранных лидов с суммой, определение «покупка+деньги» непосчитуемо;
  (2) `stage_history` пишет пустой `stage_id` для лидов — путь по статусам
  недоступен; (3) `work_minutes(a, NULL)` возвращает 0, из-за чего 677 заявок
  июня-июля без единого звонка получают `touch_min=0` и завышают отчёты
  о скорости реакции — нужен `AND NOT untouched`; (4) статус «Недозвон» ставится
  раньше звонка: 65,6% таких заявок не имели ни одного исходящего до закрытия,
  57,9% обзванивали уже после — воронку по статусам строить нельзя.
  Гипотеза скоринга v2b «смотрел лоты — хуже» на выигранных сделках
  не подтвердилась (1,30% против 1,27%); «поиск хуже рекламы» подтвердилась.
- **21.08.2026** — правило счёта №7: статус «Недозвон» на карточке не доказывает,
  что клиенту не звонили (замечание Тимофея, проверено на июне–июле). 26,1% заявок —
  дубли, медиана разрыва 0,87 суток, дубль достаётся тому же менеджеру лишь в 12,7%
  случаев. Из 2 647 «Недозвонов» 626 — дубли, у 94,7% из них есть исходящие по номеру
  в окне ±3 дня, у 86,3% — разговор в окне ±7 дней. Пересчёт дефекта №4 в
  `projects/idealnaya-sdelka.md`: «ни одного звонка вообще» — 60,1%, а не 65,6%.
  Записано в `decisions.md` и в правило 7 `claude/analytics-rules.md`.
  Обратное направление проверено: на выигранных сделках искажения нет (ответственный
  менеджер держит 95,8% времени разговоров).
- **21.08.2026** — почасовой крон `db/asr_priority.sql` (минута 7) затирал вручную
  поднятые приоритеты очереди расшифровки: из 139 зеркальных LOST-разговоров
  успело пройти 15, остальные вернулись в общий пул с приоритетом 5. В SQL
  добавлено условие `AND q.priority < 6` — приоритеты 6 и выше зарезервированы
  под ручные кампании и почасовым пересчётом не трогаются. Резервная копия
  прежней версии — `db/asr_priority.sql.bak`. Зеркальная выборка переподнята.
- **21.08.2026** — правило счёта №1.1: недозвон на непригодном номере не считается
  недозвоном (замечание Тимофея). За всю историю 1 301 такой «недозвон» (841 на битых
  номерах, 460 на заявках вообще без номера); июнь–июль 2026 — 64 штуки, у 44 нет
  ни одного исходящего. У 585 из 861 заявки с битым номером есть e-mail: позвонить
  нельзя, связаться можно. Отмечено, что `phone_kind` ошибается в обе стороны —
  по 20 из 64 «битых» номеров реально звонили, и с 2025 одна продажа пришла с номера,
  помеченного битым; `v_real_leads` отсекает junk и none целиком, поэтому 21 историческая
  продажа не видна ни в одном отчёте. Предложен отдельный статус «нет пригодного контакта»
  вместо «Недозвона». Записано в `decisions.md` и `claude/analytics-rules.md`.
- **21.08.2026** — KPI по восьми шагам сделки и разрез по семи менеджерам, отчёт
  https://claude.ai/code/artifact/86f6a6cb-6ded-4e90-b77c-966630aa6251, таблица
  в `projects/idealnaya-sdelka.md`. Окно 06–07.2026, 7 880 заявок, 89 покупок.
  Конверсии менеджеров статистически неразличимы (хи-квадрат p=0,48 по семи, p=0,70
  по шести активным) — рейтинг по конверсии не строим, сравниваем по процессу. Худшие
  шаги команды: 5+ попыток после недозвона 19,4% при цели 60% и «ни одного звонка
  по номеру» 11,5% при цели 5%. Открытый вопрос: М3 Николай Мирошниченко — 69% заявок
  не набирает, 437 отказов за два месяца, 319 из них без единого звонка; нужен ответ,
  роль это первичного фильтра или потеря заявок.
- **21.08.2026** — в скоринг добавлен признак «страницы доверия», боевая модель v2b переобучена, замер перезапущен
- **21.08.2026** — решение Тимофея: Николай Мирошниченко (9151) выведен из KPI команды
  до выяснения роли. Пересчёт без него: 7 394 заявки, 87 покупок, конверсия 1,18%;
  «ни одного звонка по номеру» падает с 11,5% до 7,8%, дозвон растёт с 83,6% до 87,2%,
  отказ без звонка с 16,9% до 8,0%. Различия конверсии между шестью незначимы (p=0,70).
  Отчёт обновлён, вопрос по М3 оставлен открытым в `projects/idealnaya-sdelka.md`.
- **21.08.2026** — стандарт «Идеальный менеджер»: шесть правил, выведенных из разбора
  «Идеальная сделка», плюс разбор входа по классам и шести менеджеров по стандарту.
  Отчёт: https://claude.ai/code/artifact/ee9111fe-7d61-4249-8f98-3fa0e689a848
  Новое в базе: `015_ideal.sql` — `v_lead_ideal` (признаки f_persisted, f_dense,
  f_deep_first, f_client_back, f_spread, f_multi на заявку) и `v_manager_ideal`
  (сводка менеджер × класс × месяц). Блок «Стандарт идеальной сделки» добавлен
  в еженедельный `profile.py`.
  Факты августа: 5+ попыток при недозвоне — 6,2% (норма 80%), плотность 7,2%,
  первый разговор 7+ минут 14,2%, клиент перезванивает сам 2,2%, диалог 3+ дня 8,5%.
  Приоритет A есть только у Попова и Симаненко (в 3,6× быстрее), у Владимира
  обратный — A медленнее C и D. Вход по классам ровный: A+B 32–41% у всех.
  Грабля: `013_profile.sql` роняла коллектор после появления `015_ideal.sql` —
  DROP VIEW v_lead_worked без CASCADE падал на зависимых v_lead_ideal/v_manager_ideal.
  Лечение: сносить зависимые из 015 в начале 013, они пересоздаются следующей миграцией.
- **21.08.2026** — поправка по reklama.md: снят пункт 11 (GoalId 13 — режим оптимизации по ценности, не служебная цель); добавлен чекпоинт 18–20.08
- **2026-08-21** — срез подписных кампаний за 19–21.08: 52 подписки, CPA 146 ₽.
  Решено не переключать стратегию на «Максимум конверсий», обоснование в decisions.md.
- **2026-08-21** — ценность цели `sub` в приоритетных целях обеих подписных кампаний поднята с 0,30 ₽ до 500 ₽ (рабочая гипотеза: 100 000 ₽ маржи × 0,5 %% конверсии подписчик→оплата). Пересмотреть по фактическим данным.
- **21.08.2026** — эталонный скрипт продаж собран и подтверждён. Зеркальная
  выборка доехала (146 расшифровок), разобрано 386 первых разговоров.
  Итог на исходящих звонках мая-июля: 76 WON против 98 LOST, состав групп
  совпадает по месяцу, источнику и менеджеру, все отличия проверены внутри
  полос длительности. Главное отличие — конкретный следующий шаг: 68,4 %
  против 40,8 %, держится во всех четырёх полосах. Карточка оценки из пяти
  критериев проверена на отложенной выборке (23 против 27): AUC 0,737 против
  0,692 на обучающей — не подогнана. Три признака отброшены как артефакты:
  «клиент первым спросил цену» (период), «названа причина звонка» (направление
  звонка), «вытащена проблема клиента» (не воспроизвёлся на контроле).
  Артефакт: https://claude.ai/code/artifact/72aa5677-5951-42b0-a683-8c7e0db68a2b
  Записано в projects/etalonnyy-skript.md
- **21.08.2026** — из 149 проигранных первых разговоров мая-июля 38 помечены
  «Недозвон» при средней длительности записанного разговора 6,5 минут (24 из них
  дольше 8 минут), 10 — «Спам» при средней 5 минут и максимуме 29 минут,
  26 — «Неплатёжеспособный» при средней 5 минут. Подтверждает пункт реестра
  о недостоверности статусов; часть «проигранных» проиграна только на бумаге.
- **21.08.2026** — **составная оценка заявки запущена в бой.** Одна цифра 0–100
  в поле Битрикса `UF_CRM_SCORING` «Скоринг» (создано в этот день) сводит три
  блока: поведение на сайте до звонка (v2b), что было в разговорах (длина, число,
  растянутость, карточка эталонного скрипта) и отклик клиента (перезвонил ли сам).
  Складываются лог-шансы, поэтому отсутствующий блок даёт ноль — так решён вопрос
  половины заявок, невидимых в Метрике (решение Тимофея 21.08). Веса — логистическая
  регрессия на когорте 01.06–31.07.2026 (7 828 заявок, 89 продаж, база 1,14%),
  перекрёстная проверка по хешу телефона: **AUC 0,856 вне выборки**, калибровка
  сходится на всех полосах. Проверка по времени на 1–15 августа, которых веса
  не видели: AUC 0,746, корона 6,42% против базы 0,62%.
  Значок **♛** (U+265B) — заявкам в работе с баллом ≥60, где разговор уже
  состоялся: «живой клиент, не отпускать». ⚡/★ до звонка остались.
  Корона 👑 невозможна — четыре байта, Битрикс обнуляет название (проверено).
  Числа попыток менеджера в оценке нет сознательно: иначе брошенный лид получает
  низкий балл и его бросают вернее. Записано в projects/sostavnaya-ocenka.md
- **21.08.2026** — **модель v2b впервые проверена вне выборки** (`lab/v2b_cv.py`,
  переобучение пять раз с прятанием пятой части лидов). Держится, но слабее
  заявленного: AUC 0,648 против 0,689 внутри выборки; конверсия классов
  A 2,72% / B 1,70% / C 0,97% / D 0,45%, разрыв A/D **6,0× вместо 11,6×**.
  Цифры в lead-scoring.md поправлены. Работают в модели два признака из девяти —
  глубина просмотра и время на сайте; визиты, лоты, устройство и кампания
  вне выборки не добавляют ничего.
- **21.08.2026** — разбор разговоров стоял с 19:00 20.08 до 02:00 21.08 UTC:
  198 звонков `failed` с `402 Payment Required` на ProxyAPI. Доступ восстановился
  сам, очередь перезапущена вручную (623 звонка: 198 упавших плюс 425 за две
  недели — чтобы задним числом посчитать карточку разговора). Сторожа на 402 нет.
- **21.08.2026** — промт разбора звонка (`extractor.py`) расширен пятью полями
  карточки эталонного скрипта; балл карточки считает код (`app/card.py`),
  пишется в `call_scores.card_score`. Модель отвечает на вопросы, арифметика наша.
- **21.08.2026** — найдено 148 заявок с 1 июля, закрытых как проигранные при
  составной оценке ≥80 (историческая конверсия полосы 17%): 54 «Не готов купить»,
  52 «Дубль», 24 «Недозвон», 14 «Неплатежеспособный», 4 «Спам». Балл 80+ означает
  шесть и больше состоявшихся разговоров и 15+ минут суммарно — «Недозвон» и «Спам»
  на таких карточках стоять не могут.
- **22.08.2026** — **разбор «влияет ли время создания заявки на сделку»** (44 630 заявок
  01.01.2025–30.06.2026, пригодные номера, без `PARTNER` и `CALL`, 508 сделок, база 1,14%).
  **День недели — влияет, час суток — нет.**
  Пн–Ср 1,41% против Чт–Вс 0,91% — **1,56×**, χ²=27,0 p=0,00014. Держится порознь
  в 2025 (1,49×) и в 2026H1 (1,66×), выживает при счёте по клиенту с дедупом по
  телефону (1,77% против 1,24%, p=0,00006) и на выигранных сделках (Пн 1,25% / Вс 0,72%).
  Час создания: сырое отношение «окно 10–17 ВЛ против остальных» 1,52% против 0,97%
  (1,52×, p=0,000004), **но это артефакт географии**. Доля дальневосточных заявок
  прыгает с 2–7% ночью до 29–32% в 09:00–12:00 ВЛ, а ДВ конвертируется в 1,83 раза
  лучше европейской части (1,47% против 0,81%, p=0,027). После Мантел-Хензеля по
  четырём поясам эффект часа падает до OR 1,23 (p=0,32); тот же расчёт для выходных
  не меняет ничего (0,65×→0,64×). У европейских клиентов конверсия по **их** местному
  времени ровная: ночь 0,83 / утро 0,81 / день 0,85 / вечер 0,74%.
  **Механика проверена:** день недели — это не обработка. Число попыток (1,41–1,57),
  доля дозвонов (37,7–42,7%) и доля разговоров 2 мин+ (25,9–30,3%) по дням ровные,
  при том что медиана до первого звонка рвётся жёстко: 31–84 мин для заявок 10–17 ВЛ,
  8–19 часов для остальных, **41 час в субботу**. Разница в дне недели — разница
  в клиенте, а не в работе с ним.
  **Деньги:** Директ 01.02–30.06.2026, 7,11 млн ₽ с НДС. Воскресенье — второй бюджет
  (16,4%, 53 136 ₽/день) при худшей конверсии 0,78%. Индекс стоимости сделки
  (расход/день ÷ CR, нормировано на среду): Ср 1,00 / Вт 1,09 / Пн 1,25 / Чт 1,32 /
  Пт 1,40 / Сб 1,72 / **Вс 2,17**. Стоимость лида по дням при этом ровная, 853–1074 ₽ —
  переплата рождается за кликом и в отчётах Директа не видна.
  Рекомендация: временной таргетинг по дням недели, снять коэффициент с Сб/Вс,
  перенести во Вт/Ср, мерить через 6–8 недель по стоимости сделки.
  Отдельно: `PARTNER` (183 карточки, 34% «конверсии») смещён в будни — без его
  исключения уровень конверсии завышен (Пн–Ср 1,59% вместо 1,41%), хотя отношение
  между днями почти не меняется (1,58×→1,56×). Отчёт: артефакт «День недели против часа»,
  https://claude.ai/code/artifact/06e1d834-a96e-40a9-9c52-cd88878ff7f0

## 22.08.2026 — панели менеджеров по ссылке, пилот «Качество разговоров»

**Сделано.** На сервере поднят веб-доступ: nginx, сайт `/etc/nginx/sites-available/dash`,
корень `/opt/ropbot/web`, в ufw открыты 80 и 443 (раньше снаружи был только SSH).
Панели раздаются по секретным ссылкам `/d/<32 hex>/`: каталог без листинга,
корень отдаёт 404, `robots.txt` запрещает всё, заголовки `noindex` / `no-referrer` /
`no-store`. Токены — новая таблица `dash_tokens`.

Генератор `/opt/ropbot/web/dash.py` собирает статический HTML, `web/build.sh`
пересобирает по крону `*/15` под `flock`, запись атомарная. Код **не запечён
в образ** — подаётся в контейнер через stdin, поэтому правка страницы не требует
пересборки и не роняет расшифровку.

Первая страница — «Качество разговоров» по карточке эталонного скрипта, окно
14 дней: средний балл против отдела и против прошлой недели, доля сильных и слабых
разговоров, пять критериев с весами и штрихом среднего по отделу, столбики по дням,
последние 40 разговоров с разбором и ссылкой в Битрикс. Телефоны маскированы.
Пилот на Пономаре (15077), у него больше всех разобранных разговоров.
Цифры сверены независимым запросом: 212 разговоров, балл 33,6, отдел 33,9 — сходится.

**HTTPS пока нет.** `certbot` вернул `too many certificates (50) already issued
for "fvds.ru" in the last 168h`: `danilchenov.fvds.ru` — поддомен общего домена
хостера, недельный лимит Let's Encrypt выбирают чужие клиенты. Это повторится
при каждом продлении. Поставлен `web/certbot_retry.sh`, крон `17 * * * *`,
снимает себя после успеха. Правильное решение — A-запись `rop.autosender.ru`
на 5.35.99.93, решение за Тимофеем. **После выпуска сертификата сменить токен
пилота** — он ходил по HTTP открытым текстом.

Buypass Go SSL как запасной центр сертификации не подошёл: `api.buypass.com`
не резолвится через DNS хостера (через 8.8.8.8 резолвится). Правку
`/etc/systemd/resolved.conf` откатили, менять резолверы на боевой машине не стали.

**Побочно: OOM в 14:01 МСК.** Во время установки пакетов ядро убило python
в `ropbot-asr-1` (2,8 ГБ RSS), контейнер перезапустился сам, SSH не отвечал
около 10 минут (load average доходил до 45). Данные не пострадали. Причина —
машина и так работает у потолка: два ASR-воркера держат 4 ядра на бэкфилле
расшифровок, 1,5 ГБ в свопе. Установка nginx+certbot стала последней каплей.
Панели к нагрузке почти ничего не добавляют (nginx в простое, сборка страницы
~2 секунды раз в 15 минут), но **любые apt-операции на этой машине теперь
делать только при остановленном бэкфилле**.

Подробно — `projects/paneli-menedzherov.md`.

## 22.08.2026, вечер — панель менеджера стала трёхвкладочной

Решение Тимофея: одна ссылка на человека, страницы переключаются вкладками.
`web/dash.py` переписан, `web/build.sh` собирает три файла на токен:

| Вкладка | Файл | Что показывает |
|---|---|---|
| Заявки | `index.html` | очередь дня: ни одного звонка / не дозвонились / тишина от 3 дней |
| Разговоры | `razgovory.html` | карточка эталонного скрипта, как раньше |
| Правила | `pravila.html` | пять правил идеальной сделки + приоритет по классам |

**Заявки.** Три блока по типу действия, а не один список: позвонить впервые,
дожать недозвон, вернуться к повисшему разговору. Внутри блока порядок —
♛ → ⚡A → ★B → C → D → без класса, затем по времени ожидания. Строка заявки
собирается из имени клиента и марки (разбор `title` вида «★ 44197 / Василий / Kia»,
значок снимается — он и так слева ярлыком), города по IP и бюджета со сроком
из текста квиза. Показано, если по номеру звонил другой менеджер.
Окно звонков открывается от **первой** карточки номера (правило 7) — иначе
дубль выглядел бы брошенным. Факт по Пономарю на 22.08: 156 заявок в работе,
41 без единого звонка, 3 недозвона, 13 ♛, тишина от 3 дней у 82.

**Правила.** Факт против нормы и против отдела, полоса со штрихами.
Знаменатели взяты как в разборе «Идеального менеджера»: упорство — от заявок
без разговора, глубина первого разговора — от заявок с разговором, остальное —
от всех. Сверено с реестром: расхождение в пределах разницы окон.
Шестое правило отдельной таблицей: медиана рабочих минут до контакта по классам.
Пономарь — A 252 мин, B 324, C 390, D 373: приоритет есть, но норма 15 минут
не выполняется даже близко.

**Среднее по отделу считается не по всем.** В сравнение входят менеджеры
со 100+ заявками за окно, у которых собственные исходящие есть больше чем
по половине заявок. Иначе Мирошниченко (466 заявок, 318 без касания) тянет
среднее вниз и все выглядят лучше, чем есть. Правило написано на самой странице.

Мелочи по дороге: `plural()` получал дробное значение и печатал «252 минута»
при округлении до 252 — теперь округляем до передачи. Строка заявки раньше
показывала «Venyoo.ru» и «г Улан-Удэ» — бесполезно, переделана.

Проверено рендерингом в браузере на ширине 390 px в светлой и тёмной теме:
горизонтальной прокрутки нет, вёрстка не рассыпается.

Мост к серверу отваливался на ~15 минут (закрылось приложение на маке).
За это время крон успел один раз собрать `index.html` новым кодом со старым
`build.sh` — вкладки временно давали 404. Починено.
- **22.08.2026, дополнение** — **проверка «эффекта дня недели» деньгами, по дню клика.**
  Вопрос Тимофея: даст ли перераспределение бюджета Директа на будни/Пн–Ср рост сделок
  и снижение стоимости цели «Заказ оплачен». Взята цель 255698794 с атрибуцией
  «последний значимый переход» к **дню клика** (бюджет управляет кликами, а не заявками;
  воскресный клик часто даёт понедельничную заявку). Директ 01.08.2025–30.06.2026:
  15,36 млн ₽, 388 оплаченных заказов.
  **Эффект по дню клика гораздо слабее, чем по дню заявки:** Пн–Ср vs Чт–Вс всего
  1,20× оплат на рубль (p=0,08, не значимо), будни vs выходные 1,05×. CPA оплаты:
  Вт 33,9 / Ср 36,5 / Вс 37,0 / Пн 38,2 / Пт 45,4 / Чт 45,8 / Сб 46,4 тыс ₽ —
  пуассоновские ДИ всех дней пересекаются. **Воскресенье по дню клика нормальное**
  (вопреки CRM-таблице по дню заявки, где оно худшее) — слабые дни по деньгам Чт/Пт/Сб.
  Эффект нестабилен: авг–дек 2025 — 1,03×, янв–июн 2026 — 1,37×. Выходные клики дешевле
  (созданный заказ 1 254–1 309 ₽ против 1 668 ₽ в Пн) и частично компенсируют конверсию.
  **Рекомендация смягчена:** не перекройка, а обратимый тест — корректировки −20…−30%
  на Чт/Пт/Сб, +10…+20% на Вт/Ср, воскресенье не трогать, 8–12 недель, мерить по CPA
  цели «Заказ оплачен». Ожидание при верных точечных оценках: +5–7 оплат на 1 млн ₽
  перенесённых, но ДИ включает ноль. Вчерашняя рекомендация «снять с Сб/Вс» отозвана
  в части воскресенья. Обновлены артефакт и projects-док vremya-zayavki.md.
- **22.08.2026** — ревизия эталонного скрипта (Fable 5): все цифры пересчитаны
  независимым кодом `lab/audit.py` — совпали. Найден упущенный приём: видео как
  аргумент безопасности (трансляция офиса / видеозвонок / видеообзор машины) —
  38,2% у WON против 18,4% у LOST, p = 0,0035, все полосы длительности, все
  шесть менеджеров. Проверка регулярками по репликам менеджера, 0 ₽. В документ
  добавлены: видео-блок в разделе «Доверие», строка в таблицу измеренного,
  12-й пункт чек-листа, CI AUC контрольной выборки [0,592; 0,867], оговорка
  о слабости критерия «срок покупки» (p = 0,19). Карточка оценки намеренно
  не тронута — зафиксирована до находки, видео-приём помечен кандидатом
  следующей ревизии. Артефакт обновлён по прежней ссылке.

## 22.08.2026, поздний вечер — вкладка «Скрипт»

Четвёртая вкладка панели: соответствие эталонному скрипту 0–100.

**Расходов не добавляет.** Балл собирается из уже оплаченных разборов модели
(`call_scores.raw`): extractor на каждый разговор сохраняет флаги «цена названа»,
«потребность выяснена», «кто решает», «следующий шаг с датой», «возражения
отработаны». Страница — чистая арифметика по этим флагам, пересчёт каждые
15 минут вместе с остальными.

**Чек-лист из 8 пунктов.** Пять из карточки эталонного скрипта, веса по
измеренному разрыву WON/LOST: следующий шаг 22 (половинный зачёт, если шаг
предложен без конкретики), кто решает 14, выгода к словам клиента 13
(половина за 2+ выгоды без привязки), дата и время 12, срок покупки 9.
Три экспертных, весом ниже: потребность 12, цена 9, возражения 9.
Пункт «возражения» выпадает из знаменателя, если возражений не было.
Балл разговора = 100 × набранное/применимое. Балл менеджера = среднее
по разговорам за 14 дней.

**Структура против перегруза:** герой-балл → «Твои слабые места» (только 3
пункта, где теряется больше всего балла: вес × недобор; формулировки во втором
лице + «как исправить») → разбор двух разговоров недели (лучший «Так держать»
и худший «Есть что исправить» от 3 минут, ссылка в Битрикс, ✓/✗ по пунктам,
две подсказки «Исправить», резюме под спойлером) → график по дням → полный
чек-лист под спойлером → короткое «как считается».

**Заодно похудели «Разговоры»:** блок «Из чего складывается балл» снят — его
роль в более полном виде играет «Скрипт». «Разговоры» теперь журнал звонков.

Факт по Пономарю: 49,3 из 100 (отдел 48,4). Слабые места: следующий шаг 44%
(теряет 12 баллов из 22), кто решает 14%, дата и время 24%. Сильное:
потребность 94%, цена 85%, возражения 71%.

На вырост (не сделано): живой коуч-комментарий модели к одному звонку в день —
~0,3 ₽/звонок, ~50–60 ₽/мес на отдел, если шаблонных подсказок окажется мало.
- **22.08.2026** — ревизия скоринга: исправлены недосчитанная сессия у свежих лидов (enrich +45 мин), двойная запись маркера, мёртвая зона крона 2–45 дней; зафиксирована оговорка про страницы входа

## 22.08.2026, ~16:40 МСК — HTTPS выпущен, токен пилота сменён

Тимофей не смог открыть панель: браузер молча подменял http на https, а 443-й
был пуст. Диагноз подтверждён: по IP (`http://5.35.99.93/...`) открывалось,
по имени — нет, при том что в access-логе nginx внешние заходы были.

В 13:38 UTC открылось окно недельного лимита Let's Encrypt на `fvds.ru` —
скрипт-ловушка (попытка раз в 20 секунд) поймал его с первого раза.
Сертификат выпущен, certbot настроил редирект 80→443, продление через
`certbot.timer` (у продлений отдельная квота, лотерея с чужим лимитом
больше не грозит). Ловушка и её крон удалены.

Как и было решено, токен пилота сменён (старый ходил открытым текстом):
новый `477049e5ce8f085e87a28b0daf6ad1c8`, старый каталог удалён, ссылка
теперь `https://danilchenov.fvds.ru/d/477049e5ce8f085e87a28b0daf6ad1c8/`.

Вопрос 72 (имя для панелей) можно закрывать наполовину: сертификат есть,
но при желании иметь красивую ссылку `rop.autosender.ru` — по-прежнему
нужна A-запись.

Примечание: команды мосту дольше 60 секунд режутся таймаутом — длинное
запускать только `setsid nohup ... &` с логом в файл (уже записано
в infrastructure, наступил на это снова).
- **22.08.2026** — **проверка составной оценки после ревизий скоринга и скрипта.**
  Калибровка на когорте июнь–июль держится (AUC 0,878, полоса 80–100: обещано
  18,7%, факт 17,1%). Карточка оценки разговора ревизией скрипта не тронута —
  блок «разговоры» без изменений. Найдено и исправлено два несоответствия:
  1) блок «поведение» считался по замороженному `lead_scores.p_est`, хотя веса
  подбирались по живому расчёту из score_weights + lead_visits. Из-за мёртвой
  зоны крона у большинства июнь–июльских заявок замороженной оценки вообще не
  было — блок поведения у них молчал. composite.py переведён на живой расчёт
  (beh_map), замороженные lead_scores не тронуты — они эталон сверки 2 сентября.
  3 762 заявки сдвинулись, в среднем на +1,2 балла, максимум 17; насыщение
  шкалы упало со 174 до 67 заявок на 100 баллах; корон стало 85 (было 72).
  2) 212 августовских заявок были обогащены раньше 45 минут (грабля
  «недосчитанная сессия», исправленная в enrich), и NOT EXISTS не давал их
  перечитать. Разовый `app/refetch_early.py`: 210 перечитано, у 113 данных
  стало больше. Замороженные оценки не переписывались.
  Проверка по времени (1–15 августа): AUC 0,759, корона 5,3% против базы 0,67%
  (×7,9); верхняя полоса обещает больше факта — месяц незрелый, сверка 2.09.

## 2026-08-22 — блок «Актуальные цены и модели»
- Новый генератор `/opt/ropbot/lab/prices_models.py` (запуск через stdin в
  `ropbot-collector-1`). Считает по скользящему окну 6 месяцев:
  (1) вопрос-ответ по бюджетным корзинам «какую машину можно купить до N ₽» —
  основа реальные покупки (WON-сделки, поле «Марка модель» UF_CRM_1490965625),
  при малой выборке дополняется запросами из разговоров (call_extractions);
  (2) пары «хотел на старте — купил» (желание: квиз/form_model/первый разговор);
  (3) топ покупаемых моделей.
- Выводы: `/root/out/actual_prices_models.json` и `.md`. Никуда не публикуется —
  по решению Тимофея пока «просто считаем».
- Крон: 1-го числа каждого месяца 03:40 MSK, лог `/var/log/ropbot/prices_models.log`.
- Оговорки по данным: если в поле сделки перечислено несколько машин — это список
  пожеланий, в «купил» не берётся (пропущено 13 сделок из 225); извлечение
  бюджетов/моделей из разговоров работает с августа 2026, окно дорастёт само;
  в CRM «Марка модель» сделки обычно копируется из лида, поэтому реальных смен
  марки почти не видно — расхождения начнут появляться из сравнения ранних
  разговоров с финальной сделкой по мере накопления расшифровок.

## 2026-08-22 (позже) — «Актуальные цены и модели»: только марка + модель
Решение Тимофея: записи, где известна лишь марка без модели, в блоке не нужны.
В `prices_models.py` добавлена `is_full_model()` — требует распознанную марку в
начале названия И осмысленный хвост-модель. Фильтр применён к покупкам, к моделям
из разговоров и к обеим сторонам пар «хотел—купил». `brand_of()` больше не выдаёт
нераспознанное слово за марку (было: «Discovery» считалось маркой).
Результат пересчёта: из 225 выигранных сделок с полной моделью 56 (отброшено 27
«одна марка» и 7 списков из нескольких машин), пар 51, разговоров с бюджетом 820.
Патч-скрипт `lab/patch_full_model.py`, бэкап предыдущей версии `.py.bak`.
Замечено к правке: одна и та же машина пишется по-разному (Stepwgn / Stepwagn /
Степвагон; Vezel / Vezer) — топ моделей дробится. Нормализация написаний не сделана.
- **22.08.2026** — ревизия «анатомии идеальной сделки» после обновлений эталонного
  скрипта, скоринга и составной оценки. Противоречий нет, обновлено три места:
  ось «что в разговорах» закрыта зеркальной выборкой скрипта (было «не хватает
  данных»); сверка v2b переписана на результат вне выборки (два работающих
  признака — совпали с lift-таблицей анатомии); чек-лист связан с полем «Скоринг»
  и короной ♛, в пункты добавлены формула следующего шага и видео-приём.
  Улика к правилу 7 из выборки скрипта: 38 «Недозвонов» с записанным разговором
  в среднем 6,5 минуты. Артефакт обновлён той же ссылкой, детали в
  `projects/idealnaya-sdelka.md`.
- **22.08.2026** — ревизия портрета менеджера после обновлений недели. Отчёт
  «Идеальный менеджер» поправлен: конверсии классов заменены на вневыборочные
  (A/D ×6 вместо ×11,6 внутривыборочных), правило 6 дополнено короной ♛.
  Проверено: еженедельный крон profile.py стоит (пн 09:30 Влд), отчёт собирается
  со всеми 8 блоками, блок «Обещания» и «Как ведёт разговор» наполнились
  (1 262 разбора). Найдено: два разных «балла разговора» (LLM 0–10 в еженедельнике
  и карточка скрипта 0–100 в панелях) — решено сводить к карточке.

## 22.08.2026, ночь — лента «Контроль» для РОПа (ТЗ inbox/tz-kontrol-ropa.md исполнено)

Отдельная РОП-ссылка (токен `kind='rop'`, без portal_user_id), две вкладки.

**«Контроль»** — только сбои по живым заявкам, два списка по ТЗ:
«Закрыли живого» (балл ≥60, закрыт в отказ за 7 дней, по клиенту больше нигде
не работают) и «Горячий стынет» (балл ≥60, разговор был, 48 часов без
исходящего по номеру клиента). Сортировка по баллу, бейдж с баллом
(красный ≥80), строка: клиент/марка, менеджер, статус закрытия из crm_dict,
разговоры и минуты, ссылка в Битрикс; у стынущих — часы тишины и красное
«клиент перезванивал ×N». Внизу — счёт недели по менеджерам и инструкция.

**«Менеджеры»** — сводная строка на каждого: в работе / без звонка / ♛ /
скрипт-балл 14 дней / открытые «закрыл живых» / «стынет». Попутно вычищен
разбор имён: у трёх менеджеров в `managers.last_name` пустая строка вместо
NULL, `coalesce` не срабатывал — собираю имя конкатенацией.

**Механика без кнопок.** Журнал `rop_control` (миграция `app/sql/016_rop_control.sql`,
применена вручную — в образ попадёт при следующей пересборке). Синк
`web/control.py` запускается первым шагом `web/build.sh` (крон */15):
резолюции returned (лид переоткрыт), reassigned (сменился ответственный),
called (появился исходящий), confirmed_dead (отметка в поле «Контроль РОПа»),
closed (стынущий закрыли — кандидат в «закрыли живого»), expired (5 дней без
реакции → счётчик «пропущено»). Повторный вход — не раньше 7 дней. Один клиент —
одна карточка (свежайшая), дубли по правилу 2/7 отсечены.

**UF-поле создано:** `UF_CRM_ROP_CONTROL` (ID 1989), enumeration:
101 «Мёртвый, закрыт верно» / 103 «Вернуть» / 105 «Передать». Добавлено в
`UF_LEAD` в `crm_sync.py`; в работающий контейнер доставлено через `docker cp`
+ restart collector (без пересборки образа — бэкфилл ASR не прерывался).
⚠️ При следующем `docker compose build` правка уже в файле на хосте.

**Приёмка:** 1) HTTPS открывается, списки живые, ссылки в Битрикс ✓;
2) синтетический тест резолюций: запись closed_alive у лида в работе →
returned, запись going_cold у лида с недавним исходящим → called, тестовые
записи удалены ✓; 3) дубли: лид 221813 (балл 80) закрыт, по номеру карточка
221831 в работе — в ленту не попал, в списке 1 ноль дублей ✓; 4) журнал 18+63
открытых = плитки страницы ✓.

**Отклонение от контрольных цифр ТЗ:** список 2 при первом запуске 63, а не
30–35. Дубли (3) и PARTNER проверены — остальное реальный накопленный завал:
32 карточки со тишиной больше 14 дней, 15 из них — больше месяца. Механизм
expired рассосёт его за 5 дней, дальше ожидается нормальный темп.
Список 1 = 18 — в диапазоне.

Константы SCORE_MIN=60, COLD_HOURS=48, EXPIRE_DAYS=5, REENTER_DAYS=7 —
наверху `web/control.py`, крутить там.
- **22.08.2026** — единая шкала балла разговора: в еженедельном profile.py (блок
  «Как ведёт разговор») и в месячном monthly.py балл LLM 0–10 заменён карточкой
  эталонного скрипта 0–100 (call_scores.card_score) — та же шкала, что на панелях.
  v_manager_weekly: колонка llm_score → card_score (+carded). Отбор образцового
  и провального разговора в monthly оставлен на чек-листе разбора, подпись уточнена.
  Грабля: ручное переприменение 013 требует следом 015 — 013 сносит v_lead_ideal.

## 23.08.2026, ~00:50 МСК — кнопка «убрать» в ленте и мастер-ссылка

**Кнопка «✕ убрать»** — первое и единственное действие прямо на странице
(остальное по-прежнему через Битрикс). По просьбе Тимофея: РОПу нужно чистить
ленту. Механика: крошечный сервис `web/actd.py` (python stdlib, 127.0.0.1:8077,
systemd `ropbot-actd`), nginx проксирует `location = /act`. POST {token, rid}
→ проверка формата (hex32/int — инъекции исключены), право только у токенов
kind rop/admin → запись в rop_control закрывается резолюцией **`dismissed`**
(добавлена в CHECK, миграция 016 дополнена). Карточка гаснет сразу, в счёте
недели идёт в колонку «Убрано» вместе с confirmed_dead. Убранное и «мёртвое»
в ленту не возвращаются никогда (правка enter() в control.py); прочие резолюции
— через 7 дней, как было. Проверено: нажатие через HTTPS ставит dismissed
(тест откачен), чужой токен получает forbidden.

**Мастер-ссылка Тимофея** — токен kind='admin', страница-хаб со ссылками на все
панели: РОП «Контроль отдела» + восемь менеджеров (токены выданы всем в отделе
[5], включая Мирошниченко и Выдренкова — их страницы полупустые, но живые).
Сборка теперь: 1 хаб + 2 РОП + 8×4 менеджерских = 35 страниц, полный прогон
~3 минуты раз в 15 минут под flock — наслоения нет.

Токены не перечисляю здесь — реестр в `dash_tokens`, хаб их показывает.

## 23.08.2026, ~01:10 МСК — сертификат перевыпущен под старый Safari

Тимофей снова увидел «небезопасно». По access-логу: Chrome с его мака открывает
панели нормально, Safari — нет. Мак на Catalina (10.15.7): её Safari не доверяет
ECDSA-цепочке Let's Encrypt (корень ISRG Root X2 добавлен только в macOS 13),
а Chrome ходит по своему хранилищу корней, поэтому в нём работало.

Сертификат перевыпущен RSA-ключом (`certbot certonly --nginx --cert-name
danilchenov.fvds.ru --key-type rsa --force-renewal`) — цепочка теперь через
ISRG Root X1, которому Catalina доверяет. Перевыпуск на то же имя считается
продлением и под чужой недельный лимит fvds.ru не попадает. В renewal-конфиге
закреплено `key_type = rsa` — продления останутся RSA.

Урок: на этом парке устройств (старые маки/айфоны менеджеров возможны тоже)
ECDSA-серты Let's Encrypt не использовать.

## 23.08.2026, ~01:20 МСК — разбор «не открывается»: VPN и мёртвая IP-ссылка

Три причины наслоились, разобраны по логам и ловушкой tcpdump:

1. **Safari на Catalina не доверял ECDSA-цепочке** — вылечено перевыпуском
   сертификата RSA (запись выше).
2. **VPN Тимофея.** При включённом VPN часть выходных узлов вообще не имеет
   маршрута к серверу: tcpdump за 4 минуты не поймал ни одного пакета.
   Другой выход (45.146.154.208) — доходит и работает. Без VPN — работает.
   На сервере блокировок нет и не было; по просьбе Тимофея его VPN-адреса
   (195.2.74.207, 46.17.43.181, 89.124.100.142, 89.125.146.134, 89.127.203.220)
   добавлены в ufw как явные allow-правила с комментарием — страховка от
   любых будущих ограничений. Заметка: 89.125.146.134 — это наш же релей.
3. **Старая тестовая ссылка по IP умерла** после включения certbot-редиректа:
   80-й порт с Host≠домена отвечал 404. Починено: теперь любой заход по IP
   или старой ссылке получает 301 на https://danilchenov.fvds.ru с сохранением
   пути.

Памятка при жалобе «панель не открывается»: 1) не VPN ли; 2) не старая ли
ссылка (по IP или с отозванным токеном); 3) какой браузер и года ли устройство.

## 23.08.2026, ~01:30 МСК — зеркало rop.autosender.ru: панели работают под VPN

Решение проблемы «с VPN не открывается»: часть выходных узлов VPN Тимофея
не имеет маршрута к московскому серверу. Вместо борьбы с маршрутами подняли
**зеркало на заграничном релее 89.125.146.134** — он же один из узлов его VPN,
то есть изнутри туннеля доступен всегда.

Тимофей добавил A-запись `rop.autosender.ru` → 89.125.146.134.

На релее: nginx (поставлен), сертификат Let's Encrypt **RSA** (webroot,
`renew_hook = systemctl reload nginx`, автопродление таймером certbot).
⚠️ **443 занят xray (сам VPN), поэтому панели на порту 8443** — ссылки вида
`https://rop.autosender.ru:8443/d/<токен>/`. Порт 80 отдан ACME и редиректу.

Проксирование на Москву: `proxy_pass https://5.35.99.93` с
`Host: danilchenov.fvds.ru` и SNI (`proxy_ssl_name`), `proxy_redirect`
переписывает ответы обратно на зеркало. Проверено: все страницы 200,
включая POST /act — кнопка «убрать» через зеркало работает.

**Боты не индексируют:** зеркало отдаёт свой `robots.txt` с `Disallow: /`,
на всех страницах заголовок `X-Robots-Tag: noindex, nofollow, noarchive`
(добавляется и на московском сервере, и на релее), корень `/` → 404,
листинга каталогов нет. Ссылки нигде не публикуются, найти их можно только
зная токен.

Прямой московский адрес `danilchenov.fvds.ru` продолжает работать — он быстрее
(без крюка за границу), зеркало нужно только тем, у кого включён VPN.

## 23.08.2026, ~02:00 МСК — дашборд РОПа: десктопная вёрстка и история за 4 месяца

**Состав отдела теперь = активные токены.** Мирошниченко (9151), Владимир (15079)
и Выдренков (10077) не работают — токены выключены, каталоги удалены. Один
рубильник: `dash_tokens.active=false` убирает человека из хаба, сводной, дашборда
и из всех сравнений «по отделу» (в `dash.py` вместо `managers.department='[5]'`
теперь подзапрос по активным токенам). В отделе осталось пятеро.

**Широкая вёрстка для РОПа и хаба.** `shell(..., wide=True)` → `.wrap.wide`
(1500 px, плотные таблицы, `.cols2`). Менеджерские страницы остались мобильными:
их смотрят с телефонов. Ниже 1100 px широкий режим схлопывается в обычный.

**Три новых блока на вкладке «Менеджеры»** (окно 4 месяца, май–август):

| Блок | Что показывает |
|---|---|
| Конверсия по месяцам | заявка → продажа по месяцу создания заявки, теплокарта, под цифрой «N из M, K в работе», итог по отделу строкой |
| Звонки после 18:00 | менеджер × день недели, переключатель месяцев (4 таблицы, JS прячет лишние) |
| Работа в выходные | сколько суббот и воскресений с исходящими разговорами, и сколько разговоров |

Конверсия и выходные стоят в две колонки, вечерние звонки (9 столбцов) — во всю
ширину. Теплокарта одноцветная (синяя шкала), значение всегда подписано цифрой.

**Оговорки прямо на странице:** текущий месяц не вызрел (медиана цикла 8,6 дня),
поэтому под каждой цифрой конверсии видно, сколько заявок ещё в работе;
сравнивать между людьми можно, с прошлым месяцем — нет.

**Выходные считаются по звонкам, а не по онлайну** — `v_manager_online_daily`
живёт с 20.08, истории нет (в базе 3 дня). Когда накопится месяц-другой,
добавить онлайн отдельной строкой. `v_manager_timeman_daily` пуста вовсе —
рабочее время из Битрикса не приходит, проверить отдельно.

Данные сверены: у Симаненко в августе 197 вечерних звонков и по SQL, и на
странице; конверсия отдела май 1,45 / июнь 1,19 / июль 1,30 / август 0,60 (не
вызрел). Проверено рендерингом при 1600 px в светлой и тёмной теме,
горизонтальной прокрутки нет.
- **22.08.2026** — эталонный скрипт: версия 2 с требованиями Тимофея.
  Квалификация до расчёта (срок/бюджет/автомобиль, коэффициент 0,7), ветка
  отложенного клиента, обязательная безопасность, обязательное предложение
  договора. Требования проверены на данных (`lab/audit3.py`): договор —
  сильное подтверждение (60,5% WON против 35,7% LOST, p = 0,0011, второе
  по силе отличие); анкетная квалификация в лоб победу не предсказывает
  (LOST задают вопросы списком чаще), поэтому в скрипте — «знать до расчёта»,
  а не «спросить анкетой». Карточка оценки заменена на v2 (100 баллов ×
  коэффициент), ветка Б оценивается мини-карточкой 40/30/30. v2 прокси-AUC
  0,70 на контроле — на уровне валидированной v1. Артефакт обновлён по прежней
  ссылке. Осталось: параметры v2 в profile.py/monthly.py.

## 23.08.2026, ~02:35 МСК — менеджерская страница: одна вкладка «Скрипт»

По ТЗ Тимофея менеджеру остаётся только «Скрипт» (страницы заявок, разговоров
и правил остались в коде, из вкладок и сборки убраны; вернуть — одна строка
в TABS и build.sh). Вёрстка широкая, как у РОПа; при одной вкладке навигация
не рисуется. index.html менеджера теперь и есть «Скрипт».

**Содержание страницы:**

- **Балл за текущий месяц** (герой; пересчёт фактически каждые 15 минут),
  плитки: прошлый месяц, прошлая неделя (пн–вс), % успешных разговоров
  (≥50 баллов) за текущий и прошлый месяц. Дельта к прошлому месяцу
  показывается только при 15+ разобранных разговорах в нём.
- **Слабые места** — топ-4 по значимости (вес × недобор), сверху самый дорогой;
  у каждого «как исправить» и сколько баллов недобирается.
- **Сильные места** — пункты не из слабых, где менеджер на уровне отдела или
  ≥60%, сверху самый важный по весу карточки. Пересечение со слабыми исключено.
- **Разбор лучшего и худшего разговора недели** (от 3 минут, последние 7 дней)
  с вердиктом: почему лучший/худший, сколько баллов принесли/потеряли
  перечисленные пункты, «обратить внимание» с формулами исправления.
- **«Как звучит идеальный разговор»** — 8 учебных блоков от открытия до финала
  «жду от вас X — я сделаю Y, созвон в четверг в 19:00». Каждый блок: реплики +
  пояснение с измеренным разрывом WON/LOST. Включён видео-приём из ревизии
  22.08 (38% против 18%). Пример честно помечен как учебный: имена условные,
  формулы из эталонного скрипта.

**Известное ограничение:** история LLM-разборов начинается с августа
(окно extractor 21 день), поэтому «прошлый месяц» у всех пока 5–10 разговоров —
на странице это помечено. С сентября сравнение месяцев станет полноценным.

**Грабли патча:** replace заменил первое вхождение `<nav>` в самом определении
nav_block → `UnboundLocalError` в shell, страницы РОПа перестали собираться
(менеджерские собирались — у них ветка else). Замечено по dash.log, починено;
предыдущие версии страниц всё это время висели без обновления, атомарная
запись отработала как задумано.

Склонения месяцев: добавлен MONTHS_PREP («в июле», не «в июлье»).

## 23.08.2026, ~03:20 МСК — карточка v2 на панелях, первые разговоры, конверсия без фильтров

Три правки по замечаниям Тимофея.

**1. Конверсия у РОПа расходилась с его данными.** Причина двойная: (а) счёт шёл
через v_lead_worked — PARTNER и городские номера отфильтровывались; (б) главное —
продажа приписывалась месяцу СОЗДАНИЯ заявки, а Тимофей считает по месяцу
ЗАКРЫТИЯ. Решение: таблица конверсии теперь по всем заявкам и всем продажам
(включая PARTNER), рядом добавлена таблица «Продажи по месяцу закрытия» —
отдел: май 51 / июнь 36 / июль 53 / август 33 — сходится с «кассой месяца».
На странице объяснено, на какой вопрос отвечает каждая таблица.
Правило 5 реестра (PARTNER вне сравнений) остаётся в силе для аналитики
ИСТОЧНИКОВ; здесь решение руководителя — видеть все покупки.

**2. Дашборд «Скрипт» переведён на карточку v2** (реестр, «Версия 2», 22.08):
следующий шаг 25 · квалификация авто/бюджет/срок 18 (по 6) · договор 15 ·
безопасность 12 · состав решения 10 · привязка выгоды 10 · дата и время 10;
меньше 2 из 3 квалификации — балл × 0,7. «Договор» и «видео» — регулярки по
расшифровке (проверено: 603 и 371 из 1215 разговоров за 30 дней), «безопасность» —
регулярка-прокси (безопасн|гарант|защит|юрлиц|реквизит|юридическ или видео) до
расширения промта extractor. Ветка Б (отложенный клиент) пока оценивается общей
карточкой — написано на странице. Образцовый разговор перестроен: 9 блоков,
добавлены квалификация до расчёта, безопасность проактивно, предложение договора.

**3. Оценивается только первый содержательный разговор.** Случай, который
заметил Тимофей: 20-минутный разговор 18.08 про пункты договора получил 0 —
но это был НЕ первый контакт, весь скрипт прошёл раньше. Теперь балл, слабые
места и разборы недели считаются только по разговорам, до которых по номеру
клиента не было разговоров от 60 секунд (история звонков есть с 20 мая — для
клиентов старше её оговорка несущественна: разборы идут с августа). Первых
содержательных — 769 из 1215 разобранных за 30 дней. Заголовки разборов теперь
«Лучший/Худший ПЕРВЫЙ разговор недели», у худшего в вердикте видно штраф ×0,7.

Сводная РОПа (колонка «Скрипт») тоже на v2 и первых разговорах.
Факт по Пономарю после пересчёта: 42,6 в августе (отдел 42,0), 208 первых
разговоров; худшее место — следующий шаг 41%, договор 46%.

## 23.08.2026, ~03:50 МСК — продажи считаются по сделкам, не по лидам

Тимофей усомнился в «Продажах по месяцу закрытия» — и был прав. Вскрылась
системная ошибка счёта: **закрытый «успешный» лид — это конвертация в сделку,
а не продажа.** После конвертации сделка живёт в среднем 31–47 дней и может
проиграться. Проверка на данных: из 62 июльских «успешных» лидов выиграно
сделок пока 16, 39 ещё в работе; из 39 августовских — 0 выиграно.
Плюс 2–7 сделок в месяц заводятся вообще без лида и в лидовом счёте не видны.

**Правки на дашборде РОПа:**

- «Продажи по месяцу закрытия» теперь по **выигранным сделкам** (stage_semantic='S',
  closedate, ответственный сделки): отдел май 36 / июнь 52 / июль 53 / август 36 —
  бьётся с учётом Тимофея («сделок 52 в июне»). Добавлена строка «Другие
  ответственные» (сделки вне пятёрки), итог = все продажи компании.
- Конверсия: продажа = лид с выигранной сделкой; «в работе» включает
  конвертированные лиды с нерешённой сделкой. Предупреждение усилено: полный
  цикл заявка→деньги ~1,5–2 месяца, два последних месяца всегда дозревают.

Проверено: передач сделок между менеджерами нет (ответственный лида и сделки
совпадает во всех выигранных с лидом). Симаненко с почти нулевыми продажами
по сделкам — не аномалия: он в отделе с июня, его конвертации ещё зреют
(203 в работе от июльских заявок).

⚠️ Для будущих разборов: **«355 покупок» и прочие конверсии прошлых аудитов
считались по лидам 'S' — то есть по конвертациям.** Для старых вызревших
когорт конвертация ≈ продажа (83–93%), но для свежих периодов и для денег
считать по deals. Кандидат на правило счёта №8 в analytics-rules.

## 23.08.2026, ~04:10 МСК — решение руководителя: продажа = перевод лида в сделку

После разбора «лид S — конвертация, не деньги» Тимофей зафиксировал
определение для панелей: **продажа засчитывается менеджеру в момент, когда
лид переведён в сделку.** Дальнейшая судьба сделки на счёт менеджера не
влияет — работа менеджера заканчивается конвертацией.

Дашборд РОПа пересчитан обратно на лидовый счёт, но уже осознанно:

- Конверсия: числитель — лиды 'S' по месяцу создания заявки; все источники.
- «Продажи по месяцам»: конвертации по месяцу перевода в сделку,
  ответственный лида + строка «Другие ответственные»:
  отдел май 54 / июнь 48 / июль 62 / август 37.
- Обе подписи на странице называют определение прямо: «продажа = лид
  переведён в сделку (решение руководителя)».

Вопрос 77 остаётся в силе для ДЕНЕГ: выручку и «вернули → купил» (rop_control.won)
считать по выигранным сделкам. Панельная «продажа» и денежная «выручка» —
теперь два осознанно разных определения.

## 23.08.2026, ~11:00 Влд — таблица «Сейчас»: стадии сделки вместо метрик ленты

Просьба Тимофея: после «В работе» показать финальные стадии, убрать «♛»,
«Закрыл живых» и «Стынет» (дублировали вкладку «Контроль»).

Новые столбцы — статусы лида в Битриксе: **12 «На торгах», 10 «Ждем договор»,
11 «Ждем оплату обеспечительного»** (у Тимофея — «ждём предоплату»). Все три
входят в «В работе», это её разрез по финальным стадиям. Добавлена итоговая
строка «Отдел».

**Показано количество заявок, а не сумма в рублях:** поле `opportunity` в лидах
не заполнено ни у одной заявки на этих стадиях (проверено: 0 из 144). Когда/если
суммы начнут проставлять — переключить на `sum(opportunity)` одной строкой.

Факт на 23.08: на торгах 19 (Томаш 8, Пономарь 5), ждём договор 17
(Симаненко 8), ждём предоплату 4. У Симаненко 93 заявки в статусе «Ждем
паспорт» — не выводим, но цифра заметная, стоит спросить, нужен ли столбец.

Грабли: итоговая строка складывалась в порядке накопления, а не в порядке
столбцов — «Без звонка» 382 встало под «На торгах». Починено, порядок кортежа
теперь совпадает с порядком вывода.
- **22.08.2026** — характеристики ведения разговора в портрете менеджера:
  `017_traits.sql` (v_call_traits / v_manager_traits) — шесть черт как
  перегруппировка карточки эталонного скрипта: Глубина 25, Доведение 22,
  Квалификация 23, Конкретика 21, Настойчивость 9; Видео — вне процента,
  кандидат следующей ревизии карточки. Сводный процент = card_score, шкала
  одна с панелями. Блок «Характеристики разговора» добавлен в еженедельный
  profile.py, секция «Портрет разговора» — в отчёт «Идеальный менеджер».
  Факт за 30 дней: конкретика (дата+время) 5–12% у всех — главная дыра;
  глубина лучшая у Симаненко 40%, худшая у Жернова 19%; видео 42% у Попова
  против 18% у Жернова.

## 23.08.2026, ~11:30 Влд — «В работе» показывал сумму всех статусов (ошибка)

Тимофей открыл карточку Пономаря: в CRM статус «В работе» — 36 заявок, панель
показывала 180. Столбец назывался «В работе», а считал **все открытые статусы**
(`status_semantic='P'`): Отложен 75 + Необработанный 37 + В работе 33 +
Позвонить 27 + На торгах 5 + Ждем договор 3 = 180.

Таблица «Сейчас» перестроена: каждый статус лида — свой столбец, плюс отдельное
«Всего открыто». Столбцы: Всего открыто · Не обработан · Позвонить · В работе ·
Отложен · Ждём паспорт · На торгах · Ждём договор · Ждём предоплату ·
Без звонка · Скрипт, со строкой «Отдел».

**Фильтры со статусов сняты** — цифры должны сходиться с фильтром в CRM
напрямую (раньше junk-номера и PARTNER срезали по 1–2 заявки, и сверка
не сходилась). Правило 1 оставлено только в «Без звонка»: там непригодные
номера исключены осознанно — набирать нечего. На странице это написано.

**Что вскрылось из-под суммы:** «Отложен» — 437 заявок по отделу, из них
346 у Симаненко; «Ждём паспорт» — 94, из них 88 у него же. Два самых больших
бака отдела были не видны. У Попова 71 заявка в статусе «Позвонить».

Урок: агрегатные `status_semantic` удобны для аналитики, но на панелях,
которые сверяют с CRM, показывать надо ровно те статусы, что видит человек
в фильтре Битрикса.

## 23.08.2026, ~12:00 Влд — навигация «← Все панели»

Тимофей попросил возможность вернуться на мастер-ссылку с любой страницы.

Тонкость: страницы статические и одинаковы для всех, поэтому просто вписать
ссылку на хаб нельзя — её увидел бы и менеджер, а это доступ ко всем панелям.

Решение без бэкенда: хаб при открытии кладёт свой путь в `localStorage`
(ключ `ropbot_hub`), остальные страницы при загрузке проверяют этот ключ и,
если он есть, показывают ссылку «← Все панели» над заголовком. В разметку
страниц токен хаба **не попадает** (проверено: `grep` по всем собранным
файлам — 0 совпадений вне самого хаба). У менеджера, который на хабе не бывал,
ключа нет и ссылки не видно. Всё в try/catch — при отключённом хранилище
страница просто без ссылки.

Проверено рендерингом в двух чистых профилях браузера: без визита на хаб
ссылка скрыта, после визита — видна и ведёт на верный путь.

Заодно хаб причёсан: две колонки, у РОПа теперь обе страницы отдельными
ссылками («Лента Контроль» и «Дашборд Менеджеры»), пояснение обновлено под
одну вкладку у менеджеров.

## 23.08.2026, ~12:30 Влд — цифры дашборда стали ссылками в CRM

Просьба Тимофея: клик по цифре открывает список лидов Битрикса с готовым
фильтром. Сделано в трёх таблицах (74 ссылки на странице):

- **«Сейчас»** — фильтр по менеджеру и статусу:
  `/crm/lead/list/?apply_filter=Y&ASSIGNED_BY_ID[]=<uid>&STATUS_ID[]=<статус>`.
  «Всего открыто» перечисляет все незакрытые статусы.
- **Конверсия** — конвертированные (`STATUS_ID[]=CONVERTED`) + диапазон
  `DATE_CREATE` за месяц (формат дат dd.mm.yyyy).
- **Продажи** — то же, но по `DATE_CLOSED`.

Таблицы звонков ссылок не получили: телефония Битрикса не фильтруется
по часу и дню недели.

Столбец **«Без звонка» убран** по просьбе Тимофея.

⚠️ Формат URL-фильтра Битрикса проверить кликом: если портал игнорирует
параметры, вероятные альтернативы — без квадратных скобок
(`ASSIGNED_BY_ID=15077`) либо с явным `FILTER_ID`.

Грабли: патч из 10 замен упал на последней (не совпал отступ), файл при этом
не записался — сработало то, что запись идёт после всех замен. Переприменил
целиком; проверять `assert` на каждом фрагменте до записи оказалось полезно.

## 23.08.2026, ~12:50 Влд — ссылки в CRM открываются отдельным окном

Тимофей: фильтры применяются, но нужно открытие в новом окне. Проверка показала,
что все 74 ссылки уже несут `target="_blank"` — просто Safari по своей настройке
«открывать страницы во вкладках» превращает `_blank` во вкладку текущего окна.

Добавлен перехватчик кликов в общий скрипт `shell()`: для любой ссылки
с `target="_blank"` вызывается `window.open(href, "_blank", "noopener,noreferrer,
width=1500,height=950,...")` — наличие размеров заставляет браузер открыть
именно окно, а не вкладку. Модификаторы (cmd/ctrl/shift/alt) и клики средней
кнопкой не перехватываются — привычное поведение сохранено. Если попап
заблокирован и `window.open` вернул null, `preventDefault` не вызывается
и работает обычный переход по ссылке.

Проверено в headless-браузере: открывается вторая страница контекста,
исходный дашборд остаётся на месте.

Работает и для ссылок «карточка в Битриксе» на ленте «Контроль» и странице
менеджера — скрипт общий для всех панелей.

## 23.08.2026, утро — Светлане: лучший и худший разговор, каждое утро в 10:00

Запрос Тимофея: бот шлёт Светлане (chat_id 7487296664) два разговора — самый
лучший и самый худший, со ссылкой на карточку в CRM, разговоры всегда разные,
выборка — прошлая и текущая недели.

Сделано: `/opt/ropbot/app/bestworst.py`. Крон на хосте
`0 3 * * 1-5` (03:00 MSK = **10:00 Влд**, по будням), лог
`/var/log/ropbot/bestworst.log`. Код **в образ не запечён**, подаётся в контейнер
через stdin (`docker exec -i ropbot-collector-1 python - < app/bestworst.py`) —
как `web/dash.py`, поэтому правка не требует `docker compose build`.

Как выбирается:

- ранжирование по `call_scores.card_score` (карточка эталонного скрипта, `app/card.py`);
- окно — от понедельника **прошлой** недели 00:00 по Владивостоку до «сейчас»;
- отбор: отдел = активные `dash_tokens kind='manager'`, `phone_kind` не `junk/none`
  (правило 1), длительность ≥ 120 сек, есть привязка к карточке CRM,
  `outcome <> 'нецелевой'`;
- при равенстве баллов (стобалльных много) берётся более длинный разговор,
  затем более свежий;
- **повторов нет:** каждый отправленный `call_id` пишется в `tg_sent`
  (`kind='call_best'` / `'call_worst'`) и больше никогда не выбирается для этого чата.
  Решение Тимофея 23.08: этого достаточно, менеджера и клиента отдельно не разводим.

В сообщении на каждый разговор: менеджер, дата и время, длительность, направление,
что зачлось и чего нет по пяти критериям, следующий шаг, резюме и **ссылка на
карточку в Битриксе** — запись слушается там же, в ленте.

Почему не прямая ссылка на запись: `record_url` ведёт на `cloudpbx.beeline.ru`
и без авторизации отдаёт **404** (проверено). Свой плеер на панели обсуждали —
Тимофей выбрал ссылку в Битрикс.

Проверено: тестовая отправка Тимофею прошла (звонки 24408 / 25017), повторный
прогон выдал уже другую пару — защита от повтора работает.

Оговорка на будущее: карточка считается только у разговоров от 90 секунд
за последние 21 день. Если разбор встанет (например, нулевой баланс ProxyAPI),
выборка начнёт беднеть молча — письмо всё равно уйдёт, но из старых разговоров.

## 23.08.2026, ~13:10 Влд — откат перехватчика окон

Тимофей: «видимо я открывал раньше» — ссылки и так открывались в новой вкладке,
он смотрел страницу до пересборки. Перехватчик кликов с `window.open` удалён,
осталось штатное `target="_blank"` на всех 74 ссылках.

Урок на будущее: прежде чем чинить поведение по жалобе, сверять время сборки
страницы с временем, когда её открывали (в шапке панели стоит «Обновлено …»).

## 23.08.2026, ~13:40 Влд — вкладка «Дашборд» у менеджера, «Контроль» у РОПа убрана

Решение Тимофея:

- **У РОПа осталась одна страница** — «Менеджеры» (теперь это `index.html`).
  Лента «Контроль» с экрана убрана; при одной вкладке навигация не рисуется.
  Страница `page_kontrol`, журнал `rop_control` и `web/control.py` живы
  и продолжают работать — данные копятся, резолюции ставятся. Вернуть ленту:
  строка `("kontrol", "kontrol.html", "Контроль")` в `ROP_TABS` + пункт
  в `build.sh`. Кнопка «убрать» (`actd`) пока без применения.
- **У менеджера две вкладки:** «Скрипт» (index) и новая «Дашборд»
  (`dashboard.html`) — та же таблица «Сейчас», что у РОПа, но одной строкой,
  по себе, со ссылками в CRM. Ниже пока ничего.

Таблица «Сейчас» вынесена в общую `now_section()` + `now_data()`: РОП и менеджер
считают её одним кодом, расхождений между страницами быть не может.
Из запроса убран лишний LATERAL по звонкам — он остался от столбца «Без звонка»
и больше не нужен, запрос стал заметно легче.

Хаб: у РОПа теперь одна ссылка «Дашборд отдела». Устаревший
`menedzhery.html` удалён (отдаёт 404).

Сборка: 1 хаб + 1 РОП + 5×2 менеджерских = 12 страниц.

## 23.08.2026, ~14:05 Влд — порядок вкладок менеджера: Дашборд, Скрипт

«Дашборд» стал первым и главным (`index.html`), «Скрипт» переехал
в `skript.html`. Ссылка менеджера не меняется — открывается дашборд.
Старые `dashboard.html` удалены (404), чтобы не отдавать копию.

## 23.08.2026, ~06:20 МСК — минутное обновление дашбордов, сборка ускорена в 7 раз

Вопрос Тимофея: «дашборд должен обновляться каждую минуту — это не проблема
по ресурсам?» Замерили, прежде чем менять.

**Что оказалось дорого — не запросы, а запуск процессов.** Полная сборка
12 страниц занимала 21 с, из них SQL всего ~2 с: `NOW_SQL` 0,14 с,
`fetch_scored` 14 дней 0,77 с (60 дней — 1,12 с), конверсия 0,08 с,
звонки за 4 месяца 0,12 с. Остальные 19 секунд — двенадцать раз подряд
стартующий python внутри `docker exec` (по ~1,5 с на запуск).

**Сделано:**

1. Режим `dash.py --bundle now|all` — все страницы в одном процессе, отдаются
   tar-потоком в stdout, хост распаковывает и раскладывает `mv` (атомарно,
   временный каталог на том же разделе). Внутрипроцессный кэш `_CACHE`:
   `fetch_scored` и `now_data` считаются один раз на всю пачку, а не по разу
   на страницу. Полная сборка **21 с → 3,2 с**, «живая» пачка (6 страниц) — 2,0 с.
2. `web/build_now.sh` — только живые страницы (дашборд менеджера + страница
   РОПа), крон `* * * * *`. `web/build.sh` — всё остальное, `*/15`.
   Оба берут один `flock -n`, поэтому наложения не бывает.
3. **`POLL_INTERVAL` 300 → 60** в `.env`. Без этого минутная пересборка
   бессмысленна: данные приходили из Битрикса раз в 5 минут, страница бы
   обновлялась ежеминутно теми же цифрами. Цикл опроса занимает ~2 с
   и делает ~15 REST-запросов — до лимита Битрикса (2 запроса/сек) далеко.

**Грабли: `docker compose restart` не перечитывает `.env`.** После restart
внутри контейнера остался `POLL_INTERVAL=300`, синк так и шёл раз в 5 минут.
Нужен `docker compose up -d collector` (пересоздаёт контейнер). Проверено, что
правка `crm_sync.py`, доставленная через `docker cp`, пересоздание пережила —
compose поднял контейнер из того же слоя.

**Цена в ресурсах:** сборка 2,3 с + опрос Битрикса 2 с = **~4 с из 60,
то есть 7% одного ядра из четырёх**. Load average до и после — 4,0 (машину
держит бэкфилл ASR). Память без изменений. Проверено: за сутки после правки
в `dash.log` ни одной ошибки.

## 23.08.2026 — очередь «Начни с этих» на дашборде менеджера

Решение Тимофея: свежая заявка важнее всего — клиент только что оставил её
и ещё у телефона. На вкладке «Дашборд» менеджера над таблицей «Сейчас»
добавлен блок-очередь (web/dash.py: queue_day / queue_block, до 10 строк):

1. свежие (<24 ч, разговора не было) — новые сверху; без единой попытки
   дольше 2 ч — красным;
2. горячие ♛/составная ≥60 с тишиной по исходящим от 24 ч (с 36 ч красным —
   фора в сутки до ленты РОПа, у той порог 48 ч);
3. ⚡A/★B без единого звонка (старше суток);
4. недозвоны A/B, 1–4 попытки из 5.

Данные — тот же QUEUE_SQL со страницы «Заявки» (~75 мс на менеджера,
дашборд пересобирается каждую минуту — успевает). Проверка: очередь Томаша
35 заявок = 12+10+11+2, сошлось с контрольным SQL.

Попутно: телефоны в названии лида («Пропущенный вызов 79…») теперь маскируются
в describe(); «city not found» из ip_city не показывается; починен CSS —
`.lead-w b.hot` (красная подсветка не работала из-за специфичности,
касалось и страницы «Заявки»). Бэкап до правок: /tmp/dash.py.bak.

## 23.08.2026 (позже) — очередь «Начни с этих» выключена

Тимофей посмотрел вживую: «выглядит не очень, потом лучше что-нибудь
придумаю». Вызов queue_block убран из page_mgr_dash, дашборд менеджера
снова — только таблица «Сейчас». Код queue_day/queue_block оставлен
в dash.py, включается одной строкой. Попутные починки остаются в силе:
маска телефонов в названии лида, скрытый «city not found»,
CSS `.lead-w b.hot`.

## 23.08.2026, ~16:50 Влд — личные ссылки на панели разосланы

Тимофей: выдать менеджерам ссылки на дашборд, у них не должно быть навигации,
как у него и у Светланы. Проверено — навигации нет: у менеджера в шапке только
свои «Дашборд» и «Скрипт», хаб «Все панели» показывается лишь тому браузеру,
где в localStorage лежит адрес хаба (у Тимофея). Решение 23.08: ничего не убираем.

Разослано ботом (`app/send_dash_links.py`, отметка в `tg_sent`, `kind='dash_link'`):

| Кому | Чат | Токен |
|---|---|---|
| Станислав Жернов (8829) | 7649613048 | `21aa9ffd0bd7c72b0bda374f5ecba9f2` |
| Николай Попов (11807) | 8071854767 | `e339b6b4ee182c86fee1648dad88f076` |
| Дмитрий Пономарь (15077) | 8489078114 | `477049e5ce8f085e87a28b0daf6ad1c8` |
| Светлана (страница РОПа) | 7487296664 | `7f4d4e1805673d4605e42a052e13cd41` |

Ссылки отдаются по **https** — сертификат на `danilchenov.fvds.ru` выпущен 22.08 вечером,
`certbot_retry.sh` сам снялся с крона. Все пять страниц проверены curl'ом: 200 и верное имя.

Попутно: у `Stanislavautosender` в `tg_users` не был проставлен `manager_id` —
поставлен 8829, иначе бот не знал, чья это панель.

**Александр Томаш (@AutosenderAl) в боте не зарегистрирован** — бот не может написать
первым по юзернейму. Его панель готова (`2240008f4cf41c6aa9225e3e188c8df6`),
ссылка уйдёт, как только он нажмёт Start у @ironbossAS_bot. Решение Тимофея: ждём.

⚠️ Грабли: `tg.send(..., markdown=False)` отправляет `parse_mode: null` и Телеграм
отвечает `Bad Request: unsupported parse_mode`. Первые четыре отправки упали именно на
этом (в `tg_sent` остались строки с `ok=false`), повтор с Markdown прошёл. Починить
в `app/tg.py`: не класть ключ `parse_mode` в запрос, если он пустой. Требует
`docker compose build`, поэтому отложено — правку делать вместе со следующей пересборкой.
- **23.08.2026** — сводки разговоров в карточках Битрикса, одобрено Тимофеем.
  Новый модуль `app/b24_summary.py` + миграция `013_b24_summary.sql`
  (таблица `b24_summary_comments`): после разбора каждого нового звонка
  extractor собирает сводку ВСЕХ состоявшихся разговоров клиента (по
  phone_e164, не по карточке — дубли видят полную историю) и пишет ОДИН
  комментарий в таймлайн карточки, дальше только обновляет его
  (`crm.timeline.comment.update`), защита от лишних вызовов по md5-хэшу.
  Резюме разговора уже извлекалось extractor'ом (`call_scores.summary`,
  1477 штук накоплено) — добавленная стоимость ~0 ₽, запись в Битрикс
  бесплатна (лимит 172800/сутки). Недозвоны в сводку не входят, показываются
  последние 15 разговоров. Проверено на живом лиде 222191 (комментарий
  1448593): создание, чтение, обновление, идемпотентность. Охват: только
  новые звонки (решение Тимофея); архив ~1700 разговоров можно долить
  разовым прогоном ~500-600 ₽. Бэкап прежнего extractor:
  `app/extractor.py.bak-summary`. Внимание: в app/sql теперь два файла
  с номером 013 (b24_summary и profile) — обе применены, идемпотентны.

## 24.08.2026, ~11:15 Влд — Томашу ссылка отправлена

Александр Томаш зарегистрировался в @ironbossAS_bot ночью 24.08
(чат 7652073781, `manager_id` 11357 проставился сам). Ссылка
`https://danilchenov.fvds.ru/d/2240008f4cf41c6aa9225e3e188c8df6/` отправлена тем же
`app/send_dash_links.py` (в список добавлен 11357), остальным повторно не ушло —
сработала проверка по `tg_sent`. Панель проверена: 200, «Дашборд · Александр Томаш».

Теперь ссылки есть у всех четверых менеджеров и у Светланы. Без ссылки остался
только Михаил Симаненко (8831) — токен `c223895dbd9a58c8ad1561c51c940d6c` заведён,
но в боте он не зарегистрирован и в списке от Тимофея его не было.

## 24.08.2026, ~11:45 Влд — блок «Дела на сегодня» на дашборде менеджера

Просьба Тимофея: показать, по скольким лидам на сегодня запланировано «Дело»,
цифра — ссылка в список лидов Битрикса.

Считаем по таблице `activities`: `provider_id = 'CRM_TODO'` — это и есть «Дело»,
которое менеджер ставит руками. Остальные типы в таблице не подходят:
`VOXIMPLANT_CALL` (82 тыс.) — звонки, `CRM_TASKS_TASK` — задачи без срока,
`CRM_SMS` / `CRM_EMAIL` — переписка. Фильтр: незакрытые, `end_time` приходится
на сегодня по владивостокскому времени, ответственный — сам менеджер.
**Считаются лиды, а не дела:** на одном лиде дел бывает несколько.

Показывается крупной цифрой + строкой «и ещё N лидов с просроченным делом»
(без ссылки — счётчик Битрикса для просрочки отдельный, не проверял).
Запрос дешёвый — 0,08 с, попал в общий кэш сборки; минутная пачка
как была ~2 с, так и осталась.

**Цифры на 24.08 вскрывают проблему:** у Пономаря 58 дел на сегодня и
**258 просроченных**, у Симаненко 28 и 229. У Жернова, Попова и Томаша
просрочка 25–59. То есть механизм напоминаний в отделе фактически не работает:
дела ставят, но не закрывают. Кандидат в ленту «Контроль» отдельным списком.

⚠️ Ссылка использует счётчик дел Битрикса `ACTIVITY_COUNTER[]=4` — формат
не проверен кликом. Если фильтр не применится, пробовать значения 2, 6, 8
(в Bitrix\Crm\Counter\EntityCounterType это IDLE / PENDING / OVERDUE и их
комбинации).

## 24.08.2026, ~13:20 Влд — «Запланированные дела в этом месяце» и точный список дел

Три просьбы Тимофея одним заходом.

**1. Вторая таблица на «Контроле отдела»** — под «Сейчас», по тем же менеджерам:
«Поставлено дел» (сколько напоминаний `CRM_TODO` менеджер завёл в карточках
лидов), «Клиентов», «Разговоров», «Касаний на клиента».

Окно: `max(1-е число текущего месяца, PLAN_START = 24.08.2026)`. То есть сейчас
счёт идёт с сегодняшнего дня, а 1 сентября окно само сдвинется на начало месяца
и цифры обнулятся — руками ничего править не нужно. История до 24.08 в
`activities` есть (с 2022 года), но Тимофей просил считать с сегодня, поэтому
она в таблицу не входит.

**Определение «касания» выбрано мной, поправимо:** касание = состоявшийся
исходящий звонок (`direction = 'out'`, `duration > 0`). Недозвоны не считаются
ни в «разговоры», ни в «клиентов». «Касаний на клиента» = разговоры / разные
номера. Цифры на 24.08: у всех 1,1–1,3 — то есть с клиентом в среднем говорят
один раз и больше не возвращаются. Если Тимофей имел в виду касание шире
(звонок + письмо + сообщение), менять в `PLAN_CALL_SQL`.

**2. Ссылка «Дела на сегодня» у менеджера — переделана.** Было
`ACTIVITY_COUNTER[]=4`, и это была ошибка: счётчик открывал **все** просроченные
дела, а не те лиды, из которых сложилась цифра (у Пономаря 59 против 258).
Фильтра «дело со сроком сегодня» в URL списка лидов Битрикса нет — проверить
нечего, поэтому цифра теперь раскрывает **точный список этих лидов прямо на
странице** (`<details>`, время дела + название + телефон, строка ведёт
в карточку). Список строится из тех же строк, что дали цифру, — разойтись
не может по построению.

**Код:** `dash.py` — `PLAN_TODO_SQL`, `PLAN_CALL_SQL`, `plan_data()`,
`plan_section()`, `TODO_LIST_SQL`, `todo_list()`; патчи `web/patch_15.py`,
`web/patch_16.py`. Запросы в общем кэше сборки, минутная пачка не затронута
(новые таблицы только на полной сборке РОПа).

## 24.08.2026, ~13:45 Влд — из таблицы «Сейчас» убран столбец «Всего открыто»

Просьба Тимофея: столбец дублировал раскладку по статусам и мешал читать строку.
Убран у РОПа и у менеджеров одним местом — `now_section()` рендерит `cols[1:]`,
`NOW_HEAD[1:]`, `STATUS_COLS[i+1]`; жирное начертание первой ячейки снято.
`NOW_SQL` не тронут — первая цифра просто не выводится, индексы статусов целы.
Пояснение под таблицей переписано: «вместе они и есть все незакрытые заявки».
Патч `web/patch_17.py`.

## 25.08.2026, ~11:00 Влд — «кривой сертификат» у менеджеров: чужая A-запись на fvds.ru

Жалоба Тимофея: у менеджеров дашборд не открывается, браузер ругается
на сертификат. **Наш сертификат ни при чём** — RSA, выдан 22.08, действует
до 20.11, локально `Verify return code: 0 (ok)`.

**Причина.** В зоне FirstVDS у `danilchenov.fvds.ru` **две A-записи**:

    danilchenov.fvds.ru. 3600 IN A 5.35.99.93      ← наш сервер
    danilchenov.fvds.ru. 3600 IN A 78.24.220.133   ← чужой

Проверено на авторитативном ns1.firstvds.ru, не кэш. 78.24.220.133 — это
`crm.rkit.ru`, сервер другого клиента FirstVDS, отдаёт сертификат на своё имя
(SAN только `crm.rkit.ru`). Браузер выбирает адрес случайно, поэтому примерно
половина открытий по старой ссылке = «сайт небезопасен». Зона общая,
провайдерская — править её мы не можем.

**Решение Тимофея:** живём на зеркале `https://rop.autosender.ru:8443/d/<токен>/`
(домен его, reg.ru, сертификат до 20.11, проверено — 200). Новую рассылку
пока не делаем.

**Что сделано с нашей стороны:**

1. В `location /d/` сайта `dash` добавлен `302` на зеркало для всех, кроме
   релея 89.125.146.134 и localhost (иначе петля: релей ходит к нам
   с `Host: danilchenov.fvds.ru`). То есть менеджер со старой ссылкой,
   если долетел до нашего IP, сам оказывается на рабочем имени.
   Проверено: локально 200 (без редиректа), через зеркало 200 без петли,
   снаружи по старой ссылке 302 → финал 200 на зеркале.
   Бэкап конфига — `/etc/nginx/sites-available/dash.bak-*`.
2. `app/send_dash_links.py`: `BASE` переведён на зеркало — любая будущая
   рассылка уйдёт с рабочим адресом.

⚠️ **Остаётся:** у Жернова (8829), Попова (11807), Пономаря (15077) и Томаша
(11357) в телеграме лежат ссылки на `danilchenov.fvds.ru` (`tg_sent`, 23–24.08).
Редирект их лечит только когда попадают на наш адрес; во второй половине
случаев они по-прежнему увидят предупреждение. Полное лечение — перерассылка
(`python3 app/send_dash_links.py`, повторная отправка защищена `tg_sent`,
для перевыпуска нужно снять отметку). Тимофей решил пока не рассылать.
- **25.08.2026** — КТ2: неделя теста, CPL 3320 (тренд к цели), автотаргетинг обходит фразы; понедельничные правки бюджетов не внесены

## 25.08.2026, ~11:45 Влд — перерассылка ссылок на зеркало

Тимофей разрешил разослать. Скрипт `app/resend_dash_links.py` (отдельный
`kind='dash_link_v2'` в `tg_sent`, чтобы защита от повторов первой рассылки
не мешала, но вторая тоже не ушла дважды). Перед отправкой все пять адресов
проверены через зеркало — 200, сертификат чист.

Ушло пятерым, все ok=True: Жернов (8829), Попов (11807), Пономарь (15077),
Томаш (11357), Светлана. Текст: «ссылка поменялась, старая иногда открывалась
с ошибкой „сайт небезопасен“, дело в адресе, а не в вашем браузере».

Без ссылки по-прежнему Михаил Симаненко (8831): токен
`c223895dbd9a58c8ad1561c51c940d6c` есть, в боте не зарегистрирован.

## 2026-08-25 — Слепок работы + починка истории статусов лидов

- **Найдено и исправлено:** `stage_history` у лидов лежала с пустым `stage_id` —
  Битрикс в `crm.stagehistory.list` для лидов отдаёт поле `STATUS_ID`, а
  `crm_sync.py` ждал `STAGE_ID`. Правка в `app/crm_sync.py` (запечётся при
  следующей пересборке образа; сейчас не пересобираем — идёт бэкфилл ASR).
  До пересборки дыру закрывает `app/stagehist_fix.py`: разовый бэкфилл за
  120 дней выполнен, крон `13,43 * * * *` дозаполняет свежее (--days 3).
  После пересборки образа крон-строку stagehist можно убрать.
- **Новая вкладка РОПа «Слепок работы»** (`/d/<токен РОПа>/slepok.html`):
  по 10 последних заявок каждого менеджера, взятых в работу, — вся история
  клиента одной строкой слева направо: заявка (🌙 если в нерабочее время),
  звонки с договорённостью из разбора моделью, серии недозвонов одной плашкой,
  смены статуса, незакрытые дела, финал. Между событиями — паузы; красным
  пауза от 48 ч и первый отклик дольше часа от начала рабочего дня.
  Код — блок «Слепок работы» в `web/dash.py` (page_slepok), собирается
  в общем бандле build.sh раз в 15 минут. Бэкапы: `web/dash.py.bak-slepok`,
  `app/crm_sync.py.bak-stagehist`.

## 25.08.2026 (день) — слепок работы менеджеров: этап 2, ежедневный снимок

- Новое направление `projects/slepok-menedzherov.md`: слепок → эталон → разрывы
  → цена. Таблица параметров этапа 1 согласована Тимофеем («строй, потом
  поправим»); решение Тимофея: снимок обновляется каждый день.
- `app/sql/018_snapshot.sql` — таблица `manager_snapshot` (снимок × менеджер ×
  период 7d/30d/mom/mat × параметр × страта A/B/C/D/X/all/std).
- `app/snapshot.py` (через stdin, в образ не запечён) — 24 параметра: скорость,
  настойчивость, плотность, глубина, договорённости, гигиена CRM, растянутость,
  горячие (короны), конверсия вызревших, портрет разговора (T-блок + прокси).
  Нормировка: страты классов + прямая стандартизация std по составу отдела.
- Крон ежедневно 23:15 MSK (06:15 Влд). Ретроспектива понедельниками
  08.06–24.08 + 25.08: 13 дат, 7 268 строк.
- Сверка первого снимка пятью независимыми SQL — сошлось точно (C1/C2, H1/H2,
  REZ, GI3, D1).
- `app/extractor.py`: промт разборщика расширен задачей 4 «портрет разговора»
  (9 полей + 2 цитаты: инициатива, уступки, отработка первого отказа,
  квалификация до цены, обоснование цены, проактивная безопасность).
  Поля идут в `call_scores.raw`. Цена разбора ~0,24 → ~0,30 ₽.
- **Образ пересобран** (стоп ASR ~1 мин, `up -d --scale asr=2`) — новый промт
  в бою; побочно вступил исправленный `crm_sync.py` (STATUS_ID). Крон
  `stagehist_fix` оставлен до проверки, снять через пару дней.

## 25.08.2026 (день, продолжение) — слепок: этапы 3–5

- Этап 3: эталон отдела — Жернов (устойчивый топ-2 конверсии вызревших в двух
  периодах + лучший процесс; Попов — лидер цифры при антиобразцовом процессе,
  не эталон). Валидация параметров вне выборки (7 821 заявка, сплит по хешу):
  рычаги — П3 плотность, Н3 настойчивость, Д1 следующий шаг, Т4 техники
  (веса 35/30/20/15); следствия — Р, Г; НЕ подтверждены — скорость отклика
  (перевёрнута) и дозвон за 3 дня. Подробно в projects/slepok-menedzherov.md.
- Этапы 4–5: вкладка РОПа «Эталон» (etalon.html) — кружки-донаты по
  параметрам, эталон синим, стрелки динамики, карточки «Разрывы и цена»
  (консервативная цена в сделках/мес с вилкой). На дашборде менеджера — блок
  «Три точки роста» без рейтинга. Патч web/etalon_patch.py, бэкап
  dash.py.bak-etalon. Снимку добавлены параметры P3/H3 (доли 5+), ретро
  пересчитана.
- Замер эффекта — scheduled task 25.09.2026 09:30 Влд (сдвиг параметров и
  конверсии, отчёт Тимофею).

## 25.08.2026 (вечер) — QA-оценка разговоров и дашборд «Качество»

- Новое направление `projects/qa-ocenka-razgovorov.md`: 16 метрик по четырём
  блокам (A структура 25% / B диалог 30% / C результат 35% / D эмоции 10%),
  балл 0–10 с цитатой-доказательством на каждую метрику.
- `app/sql/019_qa.sql` — таблица `call_qa`; `app/qa.py` — счёт баллов блоков,
  интеграла, ролей и механической A3 (доля речи по словам, монолог по
  таймкодам); `app/extractor.py` — задача 5 промта, тот же вызов модели.
- **Цена замерена на 28 разборах:** вход 2 293 → 2 574 токена, ответ 320 → 394.
  От зафиксированной цены 0,24 ₽/разговор это ≈0,28 ₽ (+0,04 ₽), то есть
  +90–110 ₽/мес при 80 разговорах в день. Отдельным проходом было бы втрое дороже.
- Расшифровка теперь подаётся построчно и пронумерованной, в промт передаётся
  число строк; роли нормализуются (`qa.norm_roles`). Без этого доля речи не
  считалась ни у одного звонка: модель ошибается в счёте строк.
- Вкладка РОПа «Качество» (`kachestvo.html`, `page_kachestvo` в web/dash.py):
  рейтинг с лидером, портреты по блокам, столбцы со средним, диаграмма
  разброса с квадрантом «Лидеры», тепловая карта 16 метрик, карточки с
  сильными сторонами, зонами роста и цитатами лучшего и слабого звонка,
  вывод с рекомендациями обучения. Палитра прогнана валидатором dataviz:
  радар разнесён на малые множители (шесть цветных полигонов неразличимы
  в тёмной теме и при дальтонизме).
- Образ пересобран дважды (промт + qa.py), конвейер проверен на живом звонке.
  Архив задним числом не пересчитываем — решение Тимофея.

## 25.08.2026 (вечер, продолжение) — недельный план «кому что тренировать»

- `app/sql/020_qa_advice.sql` + `app/qa_advice.py`: детальный разбор по каждому
  менеджеру — доля речи и монолог с направлением («снижать до 60%», «сокращать
  монолог с 209 до 90 секунд»), три задачи с текущим баллом и ориентиром,
  возражения по девяти категориям с долей отработки и приёмом, конкретные
  неотработанные формулировки клиентов, сильные стороны, цитата на планёрку.
- Крон `10 2 * * 1` MSK (пн 09:10 Влд): рекомендации замораживаются на неделю,
  внутри недели не дёргаются. Страница показывает дату среза и дату следующего
  пересчёта.
- Блок «Вывод» на вкладке «Качество» переписан: короткий список метрик заменён
  развёрнутыми карточками (`qa_plan_block` в web/dash.py, бэкап
  `dash.py.bak-advice`, патч `web/qa_advice_patch.py`).
- Первая находка: «подумаю» отрабатывается в 49–65% разговоров у всех пятерых —
  самая слабая категория отдела; «дорого» идёт лучше (74%).

## 25.08.2026 (поздний вечер) — A3 разнесена на долю речи и монолог

- Вопрос Тимофея по цифре 6,2 показал, что в одном балле смешаны две разные
  вещи. Теперь A3 — только доля речи (10 внутри нормы 40–60%, минус балл
  за каждые 5 п.п. отклонения), A4 — только монолог (10 до 90 секунд, минус
  балл за каждые 20 секунд сверх). Блок A из четырёх метрик, вес прежний.
- Миграция `021_qa_a4.sql` (колонка a4), правки `app/qa.py` (бэкап
  `qa.py.bak-a4`), `app/extractor.py`, `web/dash.py` (бэкап `dash.py.bak-a4`),
  `app/qa_advice.py`.
- **Старые оценки пересчитаны без обращения к модели** (33 разговора): доля
  речи и длина монолога уже лежали в `call_qa`, менялась только шкала.
- Образ пересобран, страницы пересобраны, недельный план пересчитан.
- Видно сразу: Жернов A3 6,6 / A4 9,9 (говорит много, но короткими кусками),
  Томаш A3 3,3 / A4 5,5 (93% речи, монолог 237 секунд).

## 25.08.2026 (ночь) — аудит качества разбора и правки

Аудит на данных (покрытие, цитаты, роли, вырожденность, противоречия). Вывод:
модель не главная проблема, потери механические. Сделано почти бесплатно:
- лимит расшифровки в промте 8000 → 24000 символов (резалось 16% разговоров,
  самых длинных; модель не видела закрытие). +~25 ₽/мес.
- `qa.verify_quotes`: цитата-доказательство обязана быть в расшифровке, иначе
  выбрасывается (было 13% выдумки). Из старых карточек вычищено 60 цитат.
- C5 «расширение чека» убрана (88% N/A, разброс 0,00), веса блока C
  пересчитаны на 4 метрики, 33 карточки пересчитаны.
- B5 и D1: строгие якоря в промте (разброс был 1,3 — не различали людей).
- возражения: закрытый список категорий в промте (`objection_cats`), 42%
  формулировок раньше не попадали ни в одну категорию; недельный план берёт
  категорию модели, для старого — регулярку.
- образ пересобран, дашборд (16 метрик), недельный план и реестр обновлены.
- смена модели отложена как измеряемый эксперимент (тест 100 разговоров ~100 ₽);
  приоритет — пропускная способность ASR (59% корпуса ещё не расшифровано).
- бэкапы: extractor.py.bak-audit, qa.py.bak-audit, qa_advice.py.bak-audit.

## 25.08.2026 (ночь) — бенч моделей, переход на 3.7-flash

- Настоящий бенч на 20 разговорах (10 длинных): текущая lite против кандидатов.
  gemini-3.7-flash (thinkingBudget=0) — цитатность 97% против 78% у lite,
  на длинных 95% против 82%, роли 30% против 15%, разброс ниже, ошибок ноль.
  3.1-pro-preview выбыла (400 на нашем формате).
- **Переход на gemini-3.7-flash** (LLM_MODEL в .env), `ask()` сам ставит
  thinkingBudget=0 для thinking-моделей (THINKING_OFF). Порог из аудита
  (95%+ цитатности) взят.
- Боевая проверка: 100% дословных цитат на новых разборах.
- Смета: ~0,7 ₽/разговор против ~0,28; разбор ~1600–1700 ₽/мес вместо ~700,
  система с VPS ~3,2 тыс ₽/мес. Откат — одна строка в .env.
- Бэкап extractor.py.bak-audit сохранён; следующий выигрыш — стерео-записи
  (роли/доля речи) и пропускная способность ASR, не модель.

## 25.08.2026 (ночь) — прирост за сегодня в таблице + переразбор на 3.7

- Таблица «Сейчас» (страница РОПа и дашборд менеджера): рядом с цифрой статуса
  зелёная стрелка ↑N — сколько заявок вошло в статус сегодня (stage_history,
  время Влд). `DELTA_TODAY_SQL`, `delta_today()`, класс `.up-today`. Строка
  «Отдел» суммирует прирост. Бэкап dash.py.bak-delta.
- Переразбор всех разговоров менеджеров ≥90с за 14 дней (1114 звонков) на
  3.7-flash — чтобы метрики разговоров (Качество, Скрипт, card_score)
  отражали новую модель. Темп ~10/мин, ~1,8 часа, ~745 ₽ разово. Пересчёт
  QA-плана и пересборка — отложенной задачей по завершении.

## 25.08.2026 (вечер) — разбор встал: баланс ProxyAPI + агрессивный переразбор

- Проверка переразбора на 3.7 не прошла 5 раз подряд из-за офлайна компьютера
  Тимофея (ropbot недоступен). Когда связь вернулась — выяснилось, что разбор
  моделью ВСТАЛ ~13:45 UTC и не работает.
- Причина двойная: (1) кончился баланс ProxyAPI — 40 задач «402 Payment
  Required» в 15:00, дальше API отбивает всё через 429, включая одиночный
  запрос к flash-lite; (2) массовый переразбор 1114 разговоров разом на более
  медленную 3.7 выбил лимит запросов — 931 задача «429 Too Many Requests».
  Урок: переразбор дозировать/троттлить, не заливать всё сразу.
- Итог: балл «Скрипт»/QA переразобраны лишь на 152 из ~1114 (смешаны 3.7 и
  lite на дашборде). 973 задачи в llm_queue failed — НЕ потеряны, вернутся
  в pending, когда баланс пополнят. Свежих неразобранных всего 7 (мало звонков).
- ДЕЙСТВИЕ ТИМОФЕЯ: пополнить баланс ProxyAPI. После этого — вернуть failed
  в pending дозированно (не всё разом). Кандидат в задачи: сторож на 402/429
  в логе (реестр давно предлагал).
- Очередь НЕ трогал (API мёртв, чтобы не усугублять 429). Поставлена отложенная
  проверка на переподхват.

## 26.08.2026 (ночь) — баланс пополнен: сторож + добор запущены

- ProxyAPI ожил после пополнения (пробный запрос 200).
- **Сторож баланса** `app/balance_watch.py` (крон */15): минимальным пробным
  запросом ловит 402 (или устойчивый 429 при вставшем разборе) и шлёт
  Тимофею в @ironbossAS_bot (chat 460128042, фолбэк — все is_boss).
  Антиспам через tg_sent kind='balance_low', не чаще раза в 3 часа. Баланс
  напрямую не читается (эндпоинт /proxyapi/balance даёт 403), детект по факту
  остановки. Тестовое сообщение о подключении сторожа отправлено и дошло.
- **Добор:** 972 упавшие задачи (429/402) возвращены в pending (attempts=0).
  Сервис разбирает на 3.7 — при живом балансе ошибок 429/402 НЕТ, значит
  прошлый 429 был вторичным к исчерпанию баланса, троттлинг не требуется.
  Дневной лимит 600 → сегодня ~600, остаток завтра. Балл «Скрипт»/QA
  наполняются по мере добора.

## 26.08.2026 — «Слепок работы» убран с экрана, добавлена вкладка «Замеры»

- Решение Тимофея: визуальную ленту «Слепок работы» с админки РОПа убрать.
  Вкладка снята из ROP_TABS, страница из PAGE_SET ("all" и "now"),
  slepok.html удалён. Код page_slepok в web/dash.py ЖИВ — вернуть двумя
  строками (комментарий у ROP_TABS). Бэкап web/dash.py.bak-slepok-off.
- Механизм сбора (ежедневный снимок manager_snapshot + вкладка «Эталон»)
  ОСТАВЛЕН — решение Тимофея после уточнения: «Эталон» питается снимком.
- Новая вкладка РОПа **«Замеры»** (zamery.html): памятка, какие процессы
  и замеры идут фоном, с какого числа и когда к каждому вернуться
  (12 позиций: слепок/эталон, QA, портрет разговора, скоринг, составная
  оценка, скрипт v2, разговоры Светлане, журнал «Контроль», дела месяца,
  починка статусов, бэкфилл/добор, сторож баланса). Содержимое — список
  ZAMERY наверху блока в web/dash.py; ОБНОВЛЯТЬ РУКАМИ при каждом
  запуске/остановке процесса — страница обещает актуальность.
- **26.08.2026** — поставлен сторож `/opt/knowledge/watch.sh`: 12 проверок каждые 30 минут,
  алерты в Telegram при поломке и восстановлении, еженедельное подтверждение живости.
  Расширен `audit.sh`: оба сервера, история снимков по датам, дифф со вчера, рабочие числа.
  Описано в services.md. **Автозаписи в реестр сознательно нет** — см. decisions.md.

## 26.08.2026 — баланс сгорел снова: смета 3.7-flash была в 5–6 раз занижена

- Утром добор 972 задач на 3.7-flash сжёг баланс за 34 минуты (01:46–02:19 UTC):
  457 разборов, 1,12M токенов вход + 168k выход. В 02:19 UTC — 402 Payment
  Required, дальше вторичные 429. Сторож отработал, Тимофей уведомлён 03:30 UTC.
- КОРЕНЬ: смета в qa-ocenka-razgovorov.md считала по ~0,09 ₽/1k вход и
  ~0,35 ₽/1k выход — это цены уровня 3.5-flash. Реальные цены ProxyAPI на
  gemini-3.7-flash: 455 ₽/1M вход, 2275 ₽/1M выход (проверено по
  proxyapi.ru/pricing 26.08). Т.е. вход дороже в 5 раз, выход в 6,5 раз.
- Реальная цена разбора: ~1,9–2 ₽/разговор (не 0,7 ₽). Утренний добор — ~890 ₽.
  Прогноз на потоке 80 разг/день: ~4700 ₽/мес (не 1600–1700).
- В llm_queue 525 failed (остаток добора) — их возврат в pending будет стоить
  ещё ~1000 ₽. Не возвращать без решения Тимофея.
- Варианты: (а) остаться на 3.7 и принять ~5 тыс ₽/мес; (б) откат на lite —
  одна строка LLM_MODEL; (в) 3.7 через Batch API ProxyAPI (−50%, разбор
  не срочный) — требует доработки extractor.py.
- РЕШЕНИЕ ТИМОФЕЯ (26.08): остаёмся на 3.7-flash с пониманием ~4,7 тыс ₽/мес.
  Остаток переразбора (525 звонков) НЕ доразбираем — только новые. Им поставлен
  status='skipped' в llm_queue: не попадают в добор и не считаются сторожем
  как «вставшая очередь» (иначе ночью были бы ложные алерты), повторно
  не enqueue'ятся. Смету в qa-ocenka-razgovorov.md считать устаревшей —
  реальные цены см. выше.

## 26.08.2026 (день) — сервер завис на ~1 час: OOM из-за ASR; стерео-проверка

- 07:20–08:20 MSK сервер не отвечал (ssh без баннера, панели 504 через релей),
  ребут не понадобился — отвис сам. Причина: воркер whisper раздулся до
  ~3,6 ГБ RSS (total-vm 10,5 ГБ) на длинной записи (18 мин аудио), машина
  ушла в своп, load до 34. OOM-killer убил python в asr дважды (06:48 и
  08:05 MSK), после второго раза asr-2 перезапустился и всё ожило.
  Контейнеры все живы, очередь разгребается.
- РЕКОМЕНДАЦИЯ (не применено): mem_limit ~2.5g на asr-контейнеры в
  docker-compose, чтобы прожорливый воркер умирал сам и не топил хост.
- **Стерео от Билайна — проверено фактически.** Билайн заявил «запись в АТС
  по умолчанию стерео», но файлы, которые отдаёт Битрикс (record_url),
  сегодня по-прежнему **mp3 8 кГц МОНО** (ffprobe трёх свежих записей за
  сутки: channels=1; в transcripts все 4401 запись stereo=false).
- Наша сторона ГОТОВА: asr_worker.py уже автоматически детектит стерео
  (probe_channels) и расшифровывает каналы раздельно с метками
  manager/client. Как только начнёт приходить стерео — роли и доля речи
  станут точными без правок кода.
- Мяч у Билайна/Битрикса: нужно, чтобы в Битрикс выгружалась двухканальная
  запись. Аргумент для поддержки: запись звонка, доступная по record_url
  в CRM, — mp3 8000 Гц 1 канал.

## 2026-08-26 — проект «Пульс Синергосмото» перенесён в реестр

- Из отдельного чата перенесён контекст дашборда «Пульс Синергосмото»:
  создан `projects/puls-sinergosmoto.md` (схема `pulse` в базе ropbot,
  артефакт-дашборд, копия на 92.63.100.125 → https://rop.autosender.ru/c266426ba3ac3db3/,
  находки за 90 дней, план ежедневной scheduled task).
- Висит: смена A-записи rop.autosender.ru → 92.63.100.125 (Тимофей),
  затем enable_https.sh; создание scheduled task 09:30 Владивостока.

## 2026-08-26 — «Пульс» выложен в панель РОПа, план с DNS отменён

- Решение Тимофея: никакой смены A-записи и сервера оркестрации — дашборд
  «Пульс Синергосмото» живёт в существующей панели РОПа.
- Страница собрана из pulse.assets (build_page.py + kpi.csv из pulse.daily_kpi)
  и выложена: https://rop.autosender.ru:8443/d/7f4d4e1805673d4605e42a052e13cd41/puls.html
  (файл /opt/ropbot/web/d/7f4d.../puls.html, релей отдаёт 200). build.sh её
  не трогает. Рабочий каталог сборки — /root/pulse/.
- В runbook.md (pulse.assets) добавлен раздел 6а — выкладка на 8443; копия на
  92.63.100.125 помечена отменённой.
- projects/puls-sinergosmoto.md переписан под новую схему публикации.
- Висит: создать ежедневную scheduled task (09:30 Владивостока); прибраться
  на 92.63.100.125 (низкий приоритет).

## 2026-08-26 (вечер) — «Пульс» встроен вкладкой в панель РОПа

- В dash.py (бэкап dash.py.bak-puls): пятая вкладка РОПа «Пульс» → puls.html
  (в ROP_TABS; dash.py её не собирает — статика через /root/pulse/publish.sh,
  который добавляет обёртку и таб-бар к pulse.html из pulse.assets).
- На вкладку «Замеры» добавлена запись, что и откуда снимает ежедневный срез
  «Пульс» (список ZAMERY в dash.py).
- runbook.md в pulse.assets: раздел 6а теперь ссылается на publish.sh.
- Панели пересобраны, всё отдаётся по 8443 (200).
- Scheduled task создать не удалось: застрявшее правило разрешения
  create_trigger блокирует и новый чат — нужно снять правило руками.

## 2026-08-26 — пин-код на «Пульс»

- Вкладка «Пульс» панели РОПа закрыта кодом доступа: nginx пускает к puls.html
  только с кукой puls_key (иначе 302 на pin.html, страницу ввода). Кука на
  90 дней. Конфиг: /etc/nginx/sites-enabled/dash (бэкап /root/dash.nginx.bak-pin),
  значение кода — только в конфиге, в реестре не храним. Остальные вкладки
  панели — без изменений.
- **26.08.2026** — статус для Тимофея: скоринг копит замер штатно (v2b 2189
  оценок, предварительно A 1,65% против 0,21–0,26% у B/C и нуля у D, но продаж
  в замере всего 7 — судить рано, сверка 2.09); extractor с 21.08 разметил
  1905 разговоров с полями карточки v2; зрелых августовских WON первых
  разговоров пока 14 — перевалидация скрипта отложена до ~10 сентября.
  Поставлена одноразовая напоминалка: systemd-таймер `remind-skript-v2`
  на хосте, 10.09 02:30 MSK (09:30 Влд) шлёт Тимофею в Telegram через
  Iron BOSS текст из `/opt/ropbot/remind_v2.py` (лог `/var/log/ropbot/remind.log`).
  Попутно замечено: в боте теперь 7 человек — зарегистрировались 4 менеджера
  (Николай, Станислав, Дмитрий, Александр), manager_id у них не привязан.
- **27.08.2026** — привязка manager_id в боте закрыта. Уточнение ко вчерашней
  записи: менеджеры оказались УЖЕ привязаны — бот сделал это сам при их
  регистрации по фамилии, вчерашнее «не привязан» было написано без проверки
  колонки. Автопривязка проверена на тёзках и верна: Николай → Попов (1890
  звонков за 30 дней; Мирошниченко — 0), Дмитрий → Пономарь (2237 звонков;
  Выдренков — 0). Доделано вручную: Тимофей → 9187, Светлана → 1. Без привязки
  остался только директор Андрей Александрович — его нет в managers, это норма.
  Открытый вопрос №8 направления звонков (регистрация менеджеров) закрыт:
  в боте 7 человек, из 8 активных менеджеров зарегистрированы 4.
- **25.08.2026** — директору Андрею отправлен в Telegram-бот вывод по заявкам
  выходных (message_id 104): конверсия заказ→оплата Сб–Вс 2,9 % против 4,8 %
  в будни, причина — первый звонок по субботним лидам через 40 часов;
  рекомендация — дежурный менеджер с нормативом «звонок в течение часа»,
  рекламу не отключать. Скрипт: /opt/ropbot/app/send_weekend_note.py.
  Контрольная точка эффекта — ~22.09.2026.

## 25.08.2026, ~12:30 Влд — СМС от менеджеров (SMSGate): вкладка «Сообщения» и каркас

Решение Тимофея: СМС клиентам будут уходить с личных телефонов менеджеров
через приложение SMSGate (sms-gate.app), режим **Cloud server** — уже
установлен на телефоны. Правило №1: **новая заявка → СМС-приветствие от
закреплённого менеджера, не чаще одной отправки на номер за месяц**
(одна отправка может занять 2 СМС). Запуск отложен до полной настройки.

Сделано:
- `app/sql/022_sms.sql` — применена: `sms_devices` (реквизиты Cloud server
  по менеджеру; ⚠️ пароли лежат в БД, в реестр не выносить) и `sms_outbox`
  (очередь и журнал: rule, lead_id, phone_e164, parts, status
  planned/sent/delivered/failed/skipped, external_id).
- Вкладка **«Сообщения»** на панели РОПа (patch_18): сводка за месяц по
  менеджерам (отправок / СМС / доставлено / ошибки / пропущено / телефон
  подключён) + лента последних 60 отправок с текстом, маской номера и ссылкой
  на карточку. До запуска страница пишет «рассылка выключена» и перечисляет,
  у кого нет реквизитов. Вторая вкладка слева, собирается в bundle all.

Осталось до запуска: реквизиты Cloud server всех телефонов → sms_devices;
текст СМС согласовать; демон отправки (enqueue по новым лидам + отправка
через api.sms-gate.app, глобальный выключатель); тест на номер Тимофея;
вебхук/опрос статусов доставки.

⚠️ Отдельно предупредить: одинаковый текст на много номеров с обычной SIM
могут зарезать спам-фильтры оператора — текст персонализируем именем
менеджера, объёмы у нас небольшие (новые заявки), но следить за «failed».

## 27.08.2026, ~15:00 Влд — текст СМС-приветствия утверждён

Тимофей выбрал вариант со ссылкой на канал MAX:

    Здравствуйте! Это {Имя}, Синергос Мото — принял вашу заявку,
    скоро позвоню. Наш канал в MAX: max.ru/channel_autosender

122 знака с самым длинным именем (Александр) = 2 СМС (лимит склейки из двух —
134). «https://» в ссылке намеренно нет — телефоны делают её кликабельной
и так, экономия 8 знаков. Имя менеджера подставляется — заодно тексты
различаются между отправителями, меньше похоже на веерную рассылку
для спам-фильтров операторов.

Переходы по ссылке не меряются (в MAX аналитики по ссылкам нет, вариант
с прокладкой Тимофей не выбрал). Попутно решено: автоматических исходящих
в Telegram с личных аккаунтов менеджеров НЕ делаем — риск бана личного
аккаунта за первые сообщения незнакомым; мостик в мессенджеры — только
ссылкой в СМС, дальше клиент пишет первым.

До запуска остались: реквизиты Cloud server (логин+пароль из SMSGate
с каждого телефона) → sms_devices; демон отправки с выключателем;
тест на номер Тимофея.

## 27.08.2026, ~15:20 Влд — СМС: бренд «Автосендер», мостик — личный MAX

Две правки Тимофея к шаблону: представляемся «Автосендер» (не «Синергос
Мото» — записано в decisions.md), и вместо канала MAX зовём написать
менеджеру в MAX лично. Оба варианта текста (с личной ссылкой max.ru/u/…
или «по этому номеру») — 119–120 знаков, 2 СМС. Для варианта «по номеру»
у менеджеров MAX должен быть заведён на рабочей SIM.

## 27.08.2026, ~15:40 Влд — финальный текст СМС-приветствия (без ссылок)

Решение Тимофея: никаких ссылок, текст собрать под рост дозвонов. Итог:

    Здравствуйте! Это {Имя}, Автосендер, по заявке на мотоцикл.
    Позвоню с этого номера — сохраните. Сюда же можно звонить и писать.

131 знак с самым длинным именем (Александр) = 2 СМС (лимит 134, запас 3).
Логика каждого элемента: имя + «Автосендер» + «по заявке» — легитимизация
(это не спам, вы сами оставляли заявку); «позвоню с этого номера —
сохраните» — главный рычаг дозвона: следующий звонок приходит не с
незнакомого номера, а с подписанного; «сюда же можно звонить и писать» —
обратный канал тем, кому неудобно отвечать на звонки. MAX не упоминаем
(решение «без ссылок вообще» — и без приглашений в мессенджеры).

Эффект мерить так: доля отвеченных исходящих (duration > 0) по лидам
с СМС против лидов до запуска — окно появится само после включения.

## 27.08.2026, ~16:00 Влд — текст СМС: финал от Тимофея

Тимофей продиктовал окончательный вариант (заменяет все предыдущие):

    Здравствуйте! Это {Имя}. Автосендер. Получил вашу заявку,
    свяжусь в ближайшее время. Сохраните мой контакт.

111 знаков с самым длинным именем = 2 СМС, запас 23 знака. Без ссылок,
без упоминания мессенджеров. Этот текст и вшивается в отправщик.

## 27.08.2026, ~13:15 Влд — первый тест СМС через SMSGate: доставлено за 4 секунды

Тимофей прислал реквизиты телефона Станислава Жернова (login M2L02F,
device slQCGI4VnfzLC70Z6tL67, Xiaomi) — занесены в `sms_devices` (8829).
Тестовый номер теперь **+79147198114** (заменил ранее названный …0692193).

Отправка через POST api.sms-gate.app/3rdparty/v1/messages (basic auth
устройства, формат `{"textMessage":{"text":…},"phoneNumbers":[…]}`).
Цепочка Pending → Processed → Sent → **Delivered за 4 секунды**. Запись
в `sms_outbox` id=1 (rule='test'), видна на вкладке «Сообщения» у РОПа:
у Жернова «подключён», в ленте текст с маской номера.

Осталось: реквизиты остальных четырёх телефонов; демон-отправщик по правилу
new_lead (с выключателем); подтверждение Тимофея, что тест дошёл как надо.

## 27.08.2026, ~13:30 Влд — текст СМС: в конец добавлено юрлицо

Правка Тимофея — в конце подпись юрлица «ООО Sinergos»:

    Здравствуйте! Это {Имя}. Автосендер. Получил вашу заявку,
    свяжусь в ближайшее время. Сохраните мой контакт. ООО Sinergos

125 знаков = 2 СМС. (В сообщении Тимофея было «ООО. Sinergos» — точка после
ООО убрана как опечатка; если это намеренно, вернуть.) Этот текст — финальный
для отправщика.

## 27.08.2026, ~13:40 Влд — SMSGate: подключён телефон Пономаря

Реквизиты Дмитрия Пономаря (15077): login X1NG9E, device yTzUxBVEmGfjZZJSc31B9
(realme RMX3501RU). Пароль начинается с дефиса — это не опечатка, проверено
обоими вариантами (с дефисом 200, без — Unauthorized). Занесён в sms_devices,
устройство в облаке на связи. Подключены 2 из 5: Жернов, Пономарь.

## 27.08.2026, ~13:50 Влд — автоотправка СМС новым лидам ВКЛЮЧЕНА

Решения Тимофея: включить сразу для подключённых телефонов (Жернов, Пономарь);
слать круглосуточно; при состоявшемся разговоре до отправки — не слать.
Плюс задержка 5 минут после создания заявки.

`app/smsd.py`, крон каждую минуту под flock. **Выключатель — файл
`/opt/ropbot/sms_on`**: `rm` = стоп немедленно, `touch` = пуск. Защиты:
только `phone_kind='mobile'`; лиды старше 6 часов не трогаются (после простоя
не бахнет пачкой); константа LAUNCH='2026-08-27 13:50+10' — созданное до
запуска не рассылается никогда; дедуп 30 дней по номеру (failed/skipped
не блокируют повтор); идемпотентность по (lead_id, rule).

Пропуски пишутся в sms_outbox со статусом skipped и причиной («уже
поговорили», «телефон менеджера не подключён», «на этот номер уже писали») —
РОП видит их на вкладке «Сообщения». Статусы доставки догоняются опросом
API за последние 2 суток. Имя в шаблон — managers.name без префикса «М№».

Сухой прогон перед включением: 0 строк к отправке (LAUNCH в будущем) —
корректно. Первые две минуты крона — без ошибок.

## 27.08.2026, ~14:30 Влд — SMSGate: подключены Симаненко и Томаш (4 из 5)

По скриншотам Тимофея (подпись «Регистрация Михаила и Александра», порядок
скринов = порядок имён): Михаил Симаненко (8831) — login C8VHM3, Xiaomi,
device yqAPXTqEZrJV4f42Ek-6B; Александр Томаш (11357) — login PRLEV9,
realme RMX5313RU, SIM …8124, device Jt5De0bC8AMuccTiccRKI. Оба
авторизуются, устройства в облаке на связи. ⚠️ Привязка «кто есть кто» —
по порядку скринов; попросил Тимофея подтвердить. Если наоборот — поменять
местами portal_user_id в sms_devices.

Подключены 4 из 5. Остался Николай Попов (11807).

## 27.08.2026, ~14:45 Влд — СМС: не слать, если лид уже закрыт

Просьба Тимофея: если за 5 минут задержки менеджер поставил «Спам» — СМС
не отправлять. Реализовано чуть шире: перед отправкой проверяется
`status_semantic='F'` — любой провальный статус (Спам 31, Дубль 27,
Недозвон JUNK, Неплатежеспособный 14, «Не готов» 33). Менеджер разобрался
раньше нас — молчим. В ленте это skipped «лид закрыт: Спам». Статусы
подтягиваются коллектором раз в 60 сек, так что смена статуса в CRM
долетает до проверки вовремя.

## 27.08.2026, ~14:05 Влд — первая боевая СМС ушла (после сдвига отсечки)

Тимофей: «Пономарю пришла заявка, СМС не вижу». Причина: заявка 223815
(Сергей, Toyota) создана в 13:47:18, а LAUNCH стоял 13:50 — лид не попал
под отсечку на 3 минуты. LAUNCH сдвинут на 13:45, крон подхватил: СМС ушла
в 14:01 с телефона Пономаря и уже **delivered** (id=2 в sms_outbox).

Попутно: номера в leads/calls хранятся БЕЗ «+» (оба одинаково, join
«уже поговорили» корректен), но в API SMSGate номер теперь уходит
с добавленным «+» на всякий случай.

## 27.08.2026, ~15:05 Влд — SMSGate: подключён Попов. ВСЕ 5 ТЕЛЕФОНОВ В СТРОЮ

Николай Попов (11807): login ZNNIX_ (подчёркивание в конце — часть логина,
без него Unauthorized), device Dg-tkX5VC_-qhcfxw7YkT, Xiaomi, SIM …6568.
Авторизация проверена, устройство на связи.

Итог: все пять менеджеров подключены (Жернов M2L02F, Симаненко C8VHM3,
Томаш PRLEV9, Попов ZNNIX_, Пономарь X1NG9E). СМС-приветствие новым лидам
работает на весь отдел. Первая боевая — лид 223815 Пономаря, delivered.
Ждёт подтверждения: привязка Михаил/Александр по порядку скринов.

## 27.08.2026, ~15:30 Влд — привязка Симаненко/Томаш исправлена (реквизиты наоборот)

Боевая проверка вскрыла: заявка Михаила ушла с телефона Томаша — привязка
по порядку скринов оказалась перепутанной. По команде Тимофея реквизиты
поменяны местами: Михаил Симаненко ↔ PRLEV9 (realme, SIM …8124),
Александр Томаш ↔ C8VHM3 (Xiaomi). Один клиент получил «Это Михаил»
с номера Томаша — Тимофею предложено, чтобы Михаил перезвонил сам.

## 27.08.2026, ~20:40 Влд — СМС: первый день, 53 заявки обработано

Проверка по просьбе Тимофея: за день 53 строки в sms_outbox —
**43 доставлено, 6 отправлено (ждут статуса), 4 пропущено** по правилам:
2 «уже поговорили», «лид закрыт: Недозвон», «лид закрыт: Не готов купить».
Ошибок 0. Шлют все пять телефонов, после обмена реквизитами — с верных
номеров. Система работает штатно.

## 27.08.2026, ~21:00 Влд — вкладка «Сообщения» обновляется каждую минуту

Тимофей: СМС долго не появляются на панели РОПа. Причина — soobshcheniya.html
собиралась только полной сборкой (крон */15), минутная пачка её не трогала.
Добавлена в PAGE_SET["now"]["rop"] — теперь лента отстаёт от отправки максимум
на минуту. Первая сборка с прогревом кэша 5,9 с (разово), обычная — прежние ~2 с.

## 27.08.2026 — вкладка РОПа «Тайминги» под пин-кодом

Просьба Тимофея: видеть скорость реакции после ключевых статусов.
Новая вкладка «Тайминги» (taymingi.html) на страницах РОПа, окно 60 дней:

- «После „Ждем паспорт"» — первый исходящий звонок и число звонков
  от входа в статус до смены статуса.
- «После „Ждем договор" — до „На торгах"» — то же, но окно до выхода
  на торги (через «Ждем оплату» включительно) либо до закрытия заявки.
- Сводка по срезу, строка на менеджера (медиана первого звонка, звонков
  в среднем, «без звонка»), разворот — таблица по каждой заявке со ссылкой
  в Битрикс.

Доступ по пин-коду 5542 (решение Тимофея): содержимое страницы зашифровано
(PBKDF2 200k → HMAC-SHA256-поток, XOR), расшифровка в браузере через
WebCrypto, пин и данные в HTML открытым текстом не лежат. Пин запоминается
в localStorage браузера. Работает только по https. Пин-код хранится в
dash.py (TIMINGS_PIN), в выдачу не попадает.

Код: блок TIMINGS_* + page_taymingi/_locked в web/dash.py (вставлен перед
ROP_PAGES), вкладка в ROP_TABS и PAGE_SET all. Бэкап dash.py.bak-taymingi.
Цифры сверены контрольным SQL (срез «договор»: 175 заявок, медиана первого
звонка 38 мин, 0,5 звонка на заявку — сходится).

Попутно замечено: danilchenov.fvds.ru теперь 302-редиректит на
https://rop.autosender.ru:8443 — в paneli-menedzherov.md ссылки старые.

## 27.08.2026 (позже) — «Тайминги» v2: рейтинг и «как надо»

Тимофею таблица показалась непонятной. Переделано:

- Наверху блок «Как надо: образец лучшего» — лучший менеджер по срезу
  «Ждем договор» (выбирается по данным: доля доведённых до торгов, от 10
  заявок), его цифры, мини-таблица «звонок в сутки / позже / без звонка →
  доля дошедших до торгов» и рецепт из трёх шагов.
- Вместо простыней — рейтинг-таблица на срез: менеджер, заявок, «звонит
  через» (медиана), звонков на заявку, заявок без звонка; в срезе договора
  ещё «довёл до торгов» и время до торгов. Лучший подсвечен.
- Сырые заявки убраны под общий разворот «Развернуть все заявки».
- Окна короче 5 минут (TIMINGS_MIN_WINDOW, «статус-однодневки» — поставил
  и тут же перевёл) исключены из средних по звонкам: звонок туда физически
  не помещается. В развороте такие строки помечены. Конверсия в торги
  считается по всем заявкам, как и раньше.

Цифры на 27.08: лучший — Жернов (звонит через 10 мин, 47% до торгов,
типично за 6 ч); «звонок в первые сутки» 62% до торгов против 29% без
звонка. Бэкап dash.py.bak-taymingi2.

## 27.08.2026 (вечер) — «Тайминги» v3: воронка по шагам и «Ждем предоплату»

По просьбе Тимофея страница перестроена в три шага: «Ждем паспорт» →
«Ждем договор» → «Ждем предоплату» (в CRM «Ждем оплату обеспечительного»,
статус 11) → «На торгах». По каждому шагу рейтинг менеджеров: звонит через,
звонков на заявку, без звонка, довёл дальше (перескок через шаг считается),
шаг занял, «задним числом».

Главная находка при проверке: у менеджеров разная дисциплина статусов.
Пономарь проставляет лидовые статусы пачкой задним числом (у него «Ждем
договор» живёт медианно ~5 минут и сразу CONVERTED — 34 из 41), Жернов
ведёт статусы в реальном времени. Отсюда два следствия, зашитых в страницу:

- колонка «Задним числом» (окно статуса < 5 мин) и оговорка, что долю
  доведённых между менеджерами сравнивать в лоб нельзя — разные привычки;
- «лучший»/образец выбираются только среди тех, у кого задним числом
  не больше четверти заявок; образец в карточке «Как надо» — по скорости
  реального первого звонка (сейчас Жернов: 10 мин, 1,6 звонка,
  25 из 30 до торгов/покупки за ~4 ч; отдел: медиана 74 мин, 80 из 111
  заявок без единого звонка на участке).

ВАЖНО для прежних выводов: вчерашне-утренние цифры «Пономарь доводит до
торгов 10%» занижали его результат — его заявки уходят в CONVERTED минуя
статусы 11/12. Сравнение менеджеров по статусным конверсиям без поправки
на дисциплину статусов — некорректно.

В карточку добавлен четвёртый пункт рецепта: ставить статусы в момент
события, иначе отчёты слепнут. Мини-таблица «звонок в сутки vs без звонка»
убрана — на этих данных она врала. Бэкап dash.py.bak-taymingi3.

## 27.08.2026 (ещё) — «Тайминги»: срез «Сколько звонков — до торгов»

По просьбе Тимофея добавлен четвёртый блок: заявки, вышедшие «На торги»
за 60 дней (135 шт.), и по каждой — число исходящих звонков от создания
заявки до выхода на торги. Таблица по менеджерам: медиана и среднее звонков,
медиана разговоров (от 30 сек), медиана пути в днях; разворот по заявкам.
Код — TORGI_SQL и _tm_torgi_block в web/dash.py.

Цифры на 27.08: у большинства 2–4 звонка и 4–6 дней пути; Жернов — 4 звонка
и 1,5 дня; у Мирошниченко по 6 заявкам на торгах в телефонии ноль исходящих
(помечено на странице «звонков не видно» — звонит мимо CRM или переписка).

## 27.08.2026 (ночь) — сервер повис от OOM, лимит памяти для ASR; «Тайминги»: «Довёл до этапа»

Сервер 5.35.99.93 около 15:20–16:40 MSK перестал отвечать (SSH и HTTPS
принимали TCP, но не отвечали; панели отдавались из кэша nginx частично,
потом 504 с релея). Тимофей перезагрузил через панель FirstVDS.

Причина по journalctl -b -1: глобальные OOM-kill — оба контейнера ASR
(faster-whisper) раздувались до ~3.3–3.5 ГБ RSS каждый (total-vm ~10 ГБ);
убийства в 01:54, 14:46, 15:23, 16:12 MSK. Началось ещё ночью — сборки
«Таймингов» ни при чём (они в collector, ~120 МБ).

Мера: в docker-compose.yml сервису asr добавлен deploy.resources.limits.
memory: 3g (бэкап docker-compose.yml.bak-memlimit, применено up -d --scale
asr=2). Теперь при раздувании OOM убивает только процесс внутри контейнера,
тот перезапускается (restart: unless-stopped), сервер целиком не виснет.
Открытый вопрос: почему воркер ASR доходит до 3.5 ГБ — утечка или длинные
записи; посмотреть при повторении по логам asr.

Попутно по просьбе Тимофея на «Таймингах» заголовки колонок «Довёл до
договора/до предоплаты/до торгов» заменены на единое «Довёл до этапа».
Проверено: страница 200, старых заголовков нет.

## 27.08.2026 (поздно) — «Тайминги»: блок «Только выигранные»

Просьба Тимофея: те же таблицы шагов, но исключительно по выигранным.
Внизу страницы добавлены три таблицы «Выигранные · Шаг 1/2/3» — только
заявки со статусом «Успешная сделка» (status_semantic='S'), окно 150 дней
(TIMINGS_WON_DAYS; история статусов в базе есть с 2017, покрытие
март–август 2026 плотное — прежняя запись «глубже конца апреля не
поднимали» устарела). Правила счёта те же; «довёл до этапа» там ожидаемо
~100%, о чём страница предупреждает. Реализация: параметры days/won
в _tm_rows + плейсхолдер {won} в TIMINGS_SQL. Бэкап dash.py.bak-won.

## 28.08.2026 (за полночь Влд) — «Тайминги»: вкладки-окна неделя/месяц

Просьба Тимофея: добавить вкладки «текущая неделя» и «текущий месяц»,
метрики обнуляются с началом периода. Сделано тремя страницами под тем же
пин-кодом: taymingi.html (60 дней), taymingi-w.html (с понедельника),
taymingi-m.html (с 1-го числа); переключатель — внутри расшифрованного
содержимого, в шапке РОПа по-прежнему одна вкладка «Тайминги». Все блоки
(3 шага, «звонки до торгов», «только выигранные», карточка «Как надо»)
считаются в окне вкладки; на 60-дневной вкладке «выигранные» остаются
за 150 дней. Пустые окна страница переживает («пока пусто»), карточка
«Как надо» на малых окнах сама прячется. Блок «Тайминги» в dash.py
переписан начисто (v5: параметр since вместо days), build_page нормализует
tab-ключ. Бэкапы dash.py.bak-won, dash.py.bak-tabs. Пин прежний.

## 28.08.2026 — «Тайминги»: балл менеджера и новый выбор «лучшего»

Просьба Тимофея: лучшего назначать по скорости первого звонка после смены
статуса и количеству звонков в рамках статуса, веса вывести из продаж.

Анализ (все окна статусов 8/10/11 со звонком, апрель–август 2026, n=179;
глубже истории нет — статусы созданы в апреле):
- скорость первого звонка → продажа: corr 0.38; ≤15 мин → 66% продаж,
  позже суток → 10% (монотонный градиент);
- число звонков в окне → продажа: corr −0.15; 1–2 звонка ≈ 37–38%,
  3+ ≈ 18% (буксующие заявки копят звонки — причинность обратная).

Итоговый балл (0–100): 70% скорость (шкала 15м/1ч/4ч/24ч → 100/70/45/30/10)
+ 30% охват (был ли звонок в окне). Число звонков сверх первого сознательно
не премируется. Свежие открытые окна без звонка в балл не входят.
Таблицы отсортированы по баллу, колонка «Балл» первая, «лучший» — верхний
с ≥5 живыми окнами и ≤¼ задним числом; образец в карточке «Как надо» — тоже
по баллу. Объяснение весов — в шапке страницы. Константы TIMINGS_W_SPEED /
TIMINGS_W_CALLS, шкала _tm_speed_pts. Бэкап dash.py.bak-score.

Проверка интуиции Тимофея: текущий месяц, шаг «паспорт → договор» — лучший
теперь Попов (охват), а не Пономарь (скорость при слабом охвате). Сошлось.

## 28.08.2026 — «Тайминги»: блок «От заявки до первого звонка» (рабочее время)

Просьба Тимофея. Новый блок сразу под карточкой «Как надо», на всех трёх
вкладках-окнах: по каждому менеджеру медиана и среднее РАБОЧЕГО времени
от создания заявки до первого исходящего звонка (окно 9:00–18:00 Влд, все
дни; ночная заявка отсчитывается с 9 утра, звонок вне окна = 0 задержки).
Плюс «за 15 раб. минут» и «без звонка вообще»; разворот по заявкам
(до 80 последних на менеджера). Исключены непригодные номера, PARTNER,
источник «Звонок», спам. Код: FT_SQL, _tm_workmin, _tm_first_touch_block
в web/dash.py; константы TIMINGS_WORK_FROM/TO. Бэкап dash.py.bak-ft.

Цифры за август: медианы по менеджерам 5,6–9 рабочих часов до первого
звонка, за 15 минут отзванивается 13–24% заявок.

## 28.08.2026 — «От заявки до первого звонка»: только заявки 9–18

Уточнение Тимофея: ночные и вечерние заявки в срез не берём — считаются
только созданные в рабочем окне 9:00–18:00 Влд (фильтр по времени создания
в FT_SQL). Тексты блока обновлены.

- 28.08: в блоке «От заявки до первого звонка» подсвечен лучший (самая быстрая медиана, выборка от 5 звонков).

- 28.08: во всех рейтингах «Таймингов» отмечены 2-е (синий) и 3-е (красный) места; лучший — по-прежнему зелёный с подсветкой строки.

- 28.08: в шагах «Таймингов» столбцы балла («Звонит через», «Звонков за стадию») подсвечены голубым; колонка переименована с «на заявку» на «за стадию» — звонки считаются в окне конкретного статуса.

- 28.08: в рейтингах «Таймингов» добавлены 4–6 места, все жёлтые.

- 28.08: в рейтингах «Таймингов» добавлены 4–6 места (жёлтые бейджи, класс tm-badge4).

- 28.08: места добавлены и в «Сколько звонков — до торгов» (по скорости пути); пороги призов смягчены: от 3 заявок и не больше половины задним числом — иначе в половине таблиц оставался один призёр.

- 28.08: «Тайминги» — новый блок «Шаг 4 · На торгах → Успешная сделка» (S4_SQL, _tm_step4_block): первый звонок после торгов, звонков в среднем, медиана интервала между звонками, без звонка, дошло до сделки, задним числом. Мест по решению Тимофея пока нет — назначит сам. Бэкап dash.py.bak-step4.

- 28.08: шаг 4 «Таймингов» получил балл и места: 70% звонки (шкала 0/1/2/3-4/5+ → 0/40/60/80/100, ориентир Жернов) + 30% скорость — гипотеза Тимофея, подтверждена данными (после предоплаты скорость не коррелирует с продажей, сопровождение звонками даёт 94% против 82%). Бэкап dash.py.bak-s4score.

- 28.08: акценты столбцов: «Звонит через» в шагах 1–3 и «Звонков в среднем» на стадии торгов подсвечены лёгким красным (tm-eff2, 7%), второй зачётный столбец остался голубым.

- 28.08: над зачётными столбцами мелкие подписи «важность 1» (красный) и «важность 2» (синий).

- 28.08: над «важность 1» в шагах 1–3 серая подпись «эталон 30–60 мин» (звонок в первый час после статуса: 37% продаж против 6% позже суток).

- 28.08: подпись «эталон 30–60 мин» оставлена только в таблицах шага 1 (обычной и по выигранным), из остальных убрана (параметр etalon в _tm_rank_table).

- 28.08: бейджи 4–6 мест переименованы: «Застенчивый», «Продавец-наблюдатель», «Лидофоб» (жёлтые, во всех таблицах с местами).

- 28.08: «Тайминги» — новый блок «Шаг 0 · Заявка → Ждем паспорт» (S0_SQL, _tm_step0_block), стоит после «От заявки до первого звонка». Балл: 60% звонки (0/1-2/3-4/5+ → 0/40/70/100) + 40% скорость; по данным 150 дней (9849 заявок со звонком) настойчивость сильнее скорости (5+ звонков → 5% продвижения против 2% при 1-2), интервал с исходом не связан — колонка справочная. Важность 1 — звонки (красный), важность 2 — первый звонок (голубой). Страница выросла до ~1,2 МБ — расшифровка на пару секунд дольше. Бэкап dash.py.bak-step0.

## 28.08.2026 (ночь) — второе зависание: своп ASR закрыт; шаг 0 уточнён

Сервер завис второй раз за день: лимит RAM 3g на ASR держал, но контейнерам
оставался доступен своп (memswap по умолчанию 2×memory) — вдвоём выели
4 ГБ свопа, сервер ушёл в кому через диск. Тимофей ребутнул через панель.
Исправлено насовсем: в docker-compose.yml у asr mem_limit: 3g +
memswap_limit: 3g (своп запрещён), cpus перенесён из deploy на уровень
сервиса (deploy.memory конфликтует с mem_limit). Применено, контейнеры
пересозданы, инспект: mem=swap=3g.

Шаг 0 по просьбе Тимофея: скорость первого звонка считается только по
будним заявкам 10:00–18:00 Влд (флаг day_ok; по ночным/выходным окнам
балл — только звонковая часть); столбцы «Первый звонок» и «Интервал»
поменяны местами. Бэкап dash.py.bak-s0b.

- 28.08: блок «Только выигранные заявки» убран со страницы «Тайминги» полностью (решение Тимофея; код в dash.py.bak-nowon).

- 28.08: вкладка «Тайминги» переименована в «KPI» (файлы и ссылки те же, taymingi*.html).

## 28.08.2026 — вкладка РОПа «Visual»: гонка менеджеров

Просьба Тимофея: обобщённая модель измерения менеджера и наглядные линии
роста. Сделано:

- **Модель (баллы дня, аддитивные):** до 10 за новую живую заявку
  (60% настойчивость, 40% скорость первого звонка, скорость только по
  будним 10–18); до 10 за смену статуса 8/10/11 (70% скорость, 30% охват,
  задним числом — 0); по 2 за звонок на открытых торгах (макс 6/день на
  заявку). Веса согласованы с KPI.
- **Снимок:** web/visual_daily.py, крон /etc/cron.d/ropbot-visual
  (23:55 Влд), таблица visual_points (day, manager, pts, parts),
  upsert за день — перезапуск безопасен. Лог /var/log/ropbot/visual.log.
- **Вкладка Visual** (visual.html) рядом с KPI, тот же пин-код 5542,
  содержимое шифруется как у KPI. SVG-график: накопленные линии с
  31.08.2026 (все с нуля, без обнуления — решения Тимофея), цвет на
  менеджера, подписи на концах линий, сетка по понедельникам; таблица
  «Счёт» (вчера/всего); до старта — плашка «гонка стартует 31 августа».
- Бэкап dash.py.bak-visual.

- 28.08: Visual — демо с выдуманными цифрами показано и выключено (VISUAL_DEMO=False), страница снова ждёт старта 31.08; боевые баллы копятся кроном с понедельника.

- 28.08: в модель Visual добавлен бонус 100 баллов за завершённую сделку (deal→WON, впервые за день; именно WON, не конвертация лида — по конвертациям лидирует Пономарь, по завершённым Жернов). visual_daily.py + текст на странице обновлены.

## 28.08.2026 — вкладка KPI у менеджеров, смена пин-кода таймингов

- На дашборде каждого менеджера новая вкладка **KPI** (`kpi.html`): содержимое
  «Таймингов» за 60 дней, без пин-кода и без переключателя недели/месяца.
  dash.py: таб в `TABS`, `page_kpi` (кэш на процесс сборки — контент общий),
  `PAGES`, `PAGE_SET.all.manager`. Бэкап: `web/dash.py.bak-kpi`.
- Пин-код страницы «Тайминги» на РОП-ссылке сменён 5542 → 7788 (решение
  Тимофея). У тех, кто открывал со старым пином, localStorage не совпадёт —
  страница снова спросит пин.
- Рассылка ссылок на KPI: `app/send_kpi_links.py` (kind='kpi_link' в tg_sent),
  ушла Жернову, Томашу, Попову, Пономарю. Симаненко в боте не зарегистрирован —
  не получил; после регистрации в @ironbossAS_bot повторный запуск скрипта
  дошлёт только ему.

## 28.08.2026 — досылка Симаненко (ожидает его регистрации в боте)

Два видео (мотивация Болдуина, работа с возражениями) разосланы четверым
менеджерам в личку ботом. Симаненко бот написать не может — он не нажимал
«Старт» (юзернейм @managerMikhal, но бот не может писать первым).
Готов скрипт `app/send_mikhail_pending.py`: после его регистрации
(«Старт» + фамилия) один запуск дошлёт KPI-ссылку и оба видео, повторные
запуски ничего не задублируют (tg_sent). Автокрон ставить не стали.

## 28.08.2026 — Симаненко получил всё

Нажал «Старт», фамилию не прислал — привязан вручную по юзернейму
(tg_users.manager_id=8831). send_mikhail_pending.py отработал: ушли
KPI-ссылка и оба видео. Теперь все пять менеджеров в боте и с панелями.

## 28.08.2026 — окно «Текущий месяц» на вкладке KPI менеджеров

Внутри вкладки KPI появился переключатель «60 дней / Текущий месяц»
(`kpi.html` / `kpi-m.html`, обе без пин-кода). Месячное окно считается
с 1-го числа и обнуляется каждое 1-е — 1 сентября начнёт собираться заново.
dash.py: `_kpi_page` с кэшем по периоду, `page_kpi_m`, `PAGES`, `PAGE_SET`,
в `build_page` kpi-m подсвечивает вкладку KPI в шапке.

## 28.08.2026 — Шаг 0: колонка «Предлагал договор»

В таблице Шага 0 (заявка → «Ждем паспорт») на «Таймингах»/KPI новая колонка:
доля заявок с разговором (≥30 сек), где менеджер предложил заключить/оформить/
подготовить/выслать договор. Считается регулярным выражением по расшифровкам
(как «видео» в чертах разговора), LLM-флага в разборе нет — «agreement» там
про договорённость о следующем контакте, не про договор. Выборочная проверка
вхождений — все по делу. Пороги подсветки: ≥50% зелёное, <25% красное
(от 3 заявок). В балл шага пока не входит. В развороте по заявкам — колонка
«Договор» (предлагал/нет). База за 60 дней: Симаненко 49%, Пономарь 35%,
Жернов 34%, Томаш 32%, Владимир 31%, Попов 30%.
- **28.08.2026** — дежурства по графику (duty.yaml, окно 12:00-18:00, круг не двигается) и итоговая логика повторных обращений: перенос содержимого в первую карточку с удалением повтора. Записано в projects/bitrix-analitika.md

## 29.08.2026 — подвкладка «По дням» на KPI менеджеров

Третья подвкладка KPI (`kpi-d.html`): кнопка на каждый день текущего месяца,
по клику — все блоки KPI по заявкам/переводам выбранного дня (открыт сегодня).
Звонки по событию дня считаются до следующего статуса, даже позднего, поэтому
прошлые дни «дозревают»; окно обнуляется 1-го числа. Технически: во все SQL
таймингов добавлена верхняя граница %(until)s (по умолчанию — завтра, старые
окна не изменились), `_kpi_day_body`/`page_kpi_days` в dash.py, переключение
дней — на странице без перезагрузки. Сборка страницы ~12 сек, в бандле
считается один раз на всех менеджеров (кэш процесса). Страница ~3 МБ
в конце месяца — если станет тяжёлой, срежем развороты по заявкам.

## 29.08.2026 — тайминги/KPI: только отдел продаж

Светлана вышла в «лучшие» Шага 0: на ней висело 5 живых нераспределённых
заявок (Venyoo/WEB, статус NEW), а таблицы брали «текущего ответственного»
без фильтра по отделу. Во все блоки таймингов добавлен фильтр
managers.department='[5]' (`_tm_dept5` в dash.py) — как на «Качестве».
Заявки на РОПе/админах в эти таблицы больше не попадают.

## 29.08.2026 — места не даются при нулевом балле

На дневном срезе Томаш стал «лучшим» Шага 1 с баллом 0 и без единого звонка:
в подиум пускали всех с 3+ заявками, а Пономарь с баллом 79 не прошёл порог
(одна заявка). Теперь во всех рейтингах по баллу (шаги 0–4) нулевой балл
мест не получает; если допущенных нет — подиум пустой.

## 29.08.2026 — Visual: без продаж, старт с 15.07

Решения Тимофея: из балла Visual убраны продажи (было 100 за deal→WON),
остальное без изменений (заявки дня, скорость по статусам 1–3, сопровождение
торгов). Старт гонки сдвинут с 31.08 на 15.07. Прошлое досчитано
`web/visual_backfill.py` — та же модель, но в границах каждого дня (как
посчитал бы ночной снимок в 23:55: звонки и закрытия после конца дня не
видны); 46 дней перезаписаны в visual_points. `visual_daily.py` продажи
больше не считает. Поставлен крон ночного снимка: 55 13 * * * UTC
(23:55 Влд) → /var/log/ropbot/visual.log.

## 29.08.2026 — KPI менеджеров: срез «Текущая неделя»

Четвёртая подвкладка KPI (`kpi-w.html`): окно с понедельника, обнуляется
каждый понедельник — как taymingi-w у РОПа, но без пин-кода. Порядок
подвкладок: 60 дней · Текущий месяц · Текущая неделя · По дням.

## 29.08.2026 — Visual: недели вместо накопления, эталон

Решение Тимофея: график не накопительный, а по неделям — точка недели =
баллы за неделю, просадка видна падением. Добавлен эталон: parts->'ideal'
в visual_points (10 за заявку, 10 за окно статуса, 2 за ежедневное касание
каждого открытого торга — тот же поток, работа строго по правилам).
На странице: гонка по неделям с серым пунктиром «Эталон» (средний личный
эталон на менеджера), ниже график «% от личного эталона» (факт ~20–30%,
пороги подсветки в таблице: ≥35% зелёное, <20% красное), таблица недели.
Продажи из подписи убраны (в балле их уже нет). Демо-режим удалён.
visual_daily.py и visual_backfill.py считают ideal; прошлое пересчитано
с 15.07. Подпись «Старт 31 августа» исправлена на 15 июля.

## 29.08.2026 — Visual: веса перекалиброваны по реальным продажам

Тимофей дал фактические продажи по менеджерам март–август. Проверка моделей
(Spearman рангов внутри месяца, июнь–август, n=18 менеджеро-месяцев):
- текущий балл (s0+s123+s4): 0.20 синхронно / 0.15 с лагом в месяц;
- чистая «середина+торги» (10*s123+5*s4): 0.62 / 0.42 — гипотеза Тимофея
  о «толкании середины воронки» подтверждена (образец — Жернов);
- внедрено сбалансированное: s0 без изменений + s123 x10 (окно до 100)
  + s4 x5 (звонок по торгам 10, до 30/день): 0.47 / 0.22.
Качественная перестройка s0 (разговор 7 мин, договор в балл) ранжирование
НЕ улучшила (новичок с лучшими разговорами взлетает выше продаж) — s0
оставлен как был. Эталон пересчитан теми же весами (10/100/10).
Бэкфилл с 15.07 перезаписан, visual_daily обновлён, страница пересобрана.
Сырьё проверки: месячные метрики менеджеров и корреляции — в этом чате;
звонки в базе с 20.05, поэтому проверка только июнь–август.

## 30.08.2026 — Visual: недельный контроль вместо гонки линий

Страница пересобрана под задачу «увидел отклонение — принял меры»:
1) Таблица «Эта неделя»: балл с понедельника к текущему дню против своей
   нормы (среднее трёх прошлых недель к этому же дню недели), отклонение %,
   % от эталона, факт продаж недели, вердикт (выше темпа / в норме /
   ниже темпа −10..−30% / принять меры <−30%). Худшие сверху.
2) «По неделям»: панель на менеджера (small multiples вместо спагетти
   линий), столбики — баллы недели, единая шкала у всех, последний
   столбик — текущая неделя (полупрозрачный, пунктирная обводка),
   под каждой неделей — фактические продажи (deal→WON, из stage_history).
   Ховер на столбике: баллы, % от эталона, продажи.
Линейные графики гонки и «% от эталона» убраны. Стиль — по dataviz-скиллу
(столбцы ≤24px со скруглённым верхом, один масштаб, подписи чернилами).

## 30.08.2026 — Visual: сортировка по эталону, кликабельный разбор, линии

По просьбе Тимофея: 1) таблица «Эта неделя» отсортирована по % от эталона
(лучшие сверху), колонка % — второй столбец; 2) любая строка кликается —
раскрывается разбор по составляющим (шаг 0 / середина / торги против
своей нормы к этому дню) с конкретикой, что промониторить; порог показа
проблемы — составляющая просела на 25%+; 3) блок «По неделям» — снова
линии менеджеров (баллы недели, не накопительно), точки с ховером
(баллы, % от эталона, продажи недели). Панели-столбики убраны.

## 30.08.2026 — Visual: блок «Путь к эталону» + оценка оптимальной нагрузки

На Visual добавлен третий блок: % от личного эталона по неделям, менеджеры
линиями, пунктир — цель 35% (уровень лучших недель отдела).
Разовый анализ насыщения (будни с 01.06, 374 менеджеро-дня):
- касание новых заявок в день поступления: ~62% стабильно до 35 заявок/день,
  при 36+ падает до 51%, медиана первого звонка растёт 604→734 мин;
- середина воронки деградирует раньше: окна статусов со звонком в сутки
  47,6% при ≤15 заявок/день → 42,8% при 16–25 → 37,1% при 26–35.
Вывод (наблюдение, не эксперимент): комфортная нагрузка ~до 25 живых
заявок/день, после — жертвуют серединой воронки; 36+ — сыпется всё.

## 2026-08-31 — «Пульс»: дособраны 26–30.08

- Первый удачный запуск ежедневной задачи (26–30.08 мак был офлайн по утрам).
  Дособраны все 5 дней: CRM, Директ (60 строк daily_campaign), Метрика;
  перезалиты 45 дней сделок; понедельничный пересчёт конверсии по дням недели.
  Изменений настроек кампаний и аномалий нет. Вкладка «Пульс» обновлена.
- build_page.py: период в шапке теперь считается из данных (был захардкожен
  «28.05–25.08»); обновлённые build_page.py и aggregates.json сохранены
  в pulse.assets (md5 сверены).
- Артефакт на claude.ai из рабочего чата не обновляется: сеть окружения
  блокирует *.frame.claudeusercontent.com — Тимофею предложено добавить домен
  в allowed domains окружения.
=== 2026-08-31 КТ3: вердикты Я-5 (остановить) / Я-6 (оставить), срез теста нед-2 — projects/reklama.md
- **31.08.2026** — замер Telegram: 6 795 подписчиков (было 6 762 на 18.08),
  прирост +33 за 14 дней против 54 засчитанных целей `sub_tg` — сходимость 61–70 %.
  MAX сверке не поддаётся: идут постоянные посевы в группах.
- **31.08.2026** — проверено: корректировки ставок для товарных кампаний через API
  недоступны (`bidmodifiers.add` → ошибка 8800 «Кампания 706323819 не найдена»),
  как и `campaigns.get`. Корректировка +30 % на десктоп ставится только руками
  в интерфейсе Директа.
- **31.08.2026** — зафиксирован первый замер канала MAX: **2 113 подписчиков**
  (со слов Тимофея). Базы на 18.08 нет, назад не сверяется. Ждём от Тимофея
  прирост от посева в группах — без него `sub_max` (287 за 18–31.08) не проверить.
- **31.08.2026** — «ТЕСТ Поиск Ядро», группа «Корея» (5788362242): автотаргетинг
  переведён с «запросы с упоминанием вашего бренда» на «без упоминания бренда».
  Правка сделана в интерфейсе (журнал изменений Директа подтверждает: с 29.08
  менялась только эта группа). Причина: автотаргетинг ловил лишь «автосендер
  корея» — 41 показ за 16 дней против 22 503 у Японии. Проверка 08.09.
- **31.08.2026** — «ТЕСТ Поиск Ядро», группа «Китай» (5788362243): включена
  категория автотаргетинга `ACCESSORY` (сопутствующие запросы). Через API,
  подтверждено чтением. Цель — поймать кластер «лучшие/проверенные компании
  по привозу авто», который Яндекс сам показывает в примерах этой категории.
  Порог отката: цена созданного заказа по группе выше 3 500 ₽. Проверка 15.09.
- **31.08.2026** — в projects/reklama.md добавлен раздел «Календарь проверок
  и пороги решений»: все идущие эксперименты с датами и условиями отката,
  невыполненные пункты аудита, правила «одно изменение за раз».

## 2026-08-31 — разбор: почему у Симаненко много неудачных СМС
Вопрос Тимофея. Факт: у М2 Михаил Симаненко (portal_user_id 8831) 46 отправок
со статусом failed против 0–2 у остальных менеджеров. 43 из 46 — один инцидент.

Что показал шлюз (GET /messages/{id} у sms-gate.app, скрипты `lab/sms_why.py`
и `lab/sms_why2.py`):
- Все сообщения, поставленные в очередь с 30.08 10:15 Влд, висели в состоянии
  Pending до 31.08 11:21 Влд — телефон 25 часов не забирал очередь.
- В 11:21:48–55 (7 секунд) телефон разом обработал весь накопленный хвост,
  в порядке от новых к старым. Первые ~10 ушли и дошли, остальные 43 получили
  от Android `RESULT_ERROR_LIMIT_EXCEEDED (Failed because we reached the
  sending queue limit)` — системный лимит исходящих СМС на приложение.
- Оставшиеся 3 отказа (28–29.08, единичные) — другая причина: SMS-центр
  Билайна (+79037011111) вернул коды 64 и 73.

Вывод: виноват не сервер и не оператор. Наша сторона отработала без единой
ошибки отправки (`grep "не ушла" smsd.log` — пусто), сообщения приняты облаком.
Телефон Михаила (realme RMX5313RU, SIM ...8124) сутки не выходил на связь —
типично для realme/Xiaomi, где оптимизация батареи выгружает фоновые приложения.

Важная деталь для чтения данных: `created_at` в `sms_outbox` — время постановки
в очередь, `final_at` — когда МЫ узнали статус. По ним кажется, что сбой шёл
весь день 30.08 и «починился» к вечеру; на самом деле весь день копилась
очередь, а разбор её случился утром 31.08.

Что чинить (не сделано, решения Тимофея нет):
1. На телефоне: отключить оптимизацию батареи для SMSGate, разрешить автозапуск.
2. Сторож: сейчас отвал телефона не виден — сообщения тихо копятся в 'sent'.
   Нужен сигнал РОПу, если у устройства N сообщений в 'sent' дольше X минут.
   На 31.08 22:00 в 'sent' дольше часа: Симаненко 9, остальные по 3–4.
3. `poll_status` смотрит назад только 2 суток — что провисело дольше, останется
   в 'sent' навсегда. Таких уже 8 штук.
4. Текст 125 знаков = 2 СМС, вдвое быстрее упирается в лимит Android.
   Укоротить до 70 знаков — вдвое меньше нагрузки.
5. Протухшие не отменяются: приветствие «получил вашу заявку, свяжусь» пыталось
   уйти через сутки после заявки. Нужен TTL на сообщение.
6. 43 клиента приветствие не получили. Досылать или нет — вопрос открыт.

## 2026-08-31 (вечер) — починка СМС после разбора по Симаненко
Решения Тимофея: срок годности 1 час, текст СМС не менять, сигнал — только
руководителям. Досылать потерянные 43 приветствия не стали.

Правки (патчи в `lab/patch_smsd_*.py`, бэкапы `app/smsd.py.bak*`):
1. **`ttl` при отправке (3600 с)** — документация шлюза прямо советует всегда
   его ставить, иначе облако держит сообщение до суток. Теперь приветствие,
   не ушедшее за час, не уйдёт вообще — вместо «получил вашу заявку» через день.
2. **Причина отказа сохраняется дословно** — раньше писали своё «шлюз: Failed»
   и теряли смысл. Теперь из `recipients[0].error`: лимит Android, отказ
   SMS-центра оператора и протухшее по ttl — это три разные болезни.
3. **Новый финальный статус `no_report`** — телефон отправил, оператор отчёт
   о доставке не вернул. Это нормальный исход, а раньше такие навсегда
   оставались в 'sent': нашли 18 штук возрастом от 5 до 100 часов, из-за них
   сторож ругался на все пять телефонов. Закрываем через `REPORT_WAIT_H=1`.
4. **Обработка 404 от шлюза** — сообщение от 27.08 висело четвёртые сутки:
   шлюз о нём забыл, `raise_for_status` бросал исключение, и до разбора
   состояния дело не доходило. Закрываем как `no_report` после `FORGET_DAYS=3`.
5. **Окно опроса статусов 2 суток → 7** (`POLL_DAYS`).
6. **Сторож `app/sms_watch.py`** — крон каждые 15 минут под тем же
   выключателем `/opt/ropbot/sms_on`, лог `/var/log/ropbot/sms_watch.log`.
   Сигналит в Телеграм руководителям (`tg_users.is_boss`), если у телефона
   ≥3 отказов за 3 часа или хоть одно сообщение висит дольше 90 минут.
   Смотрит только на свежее (до 24 часов) — иначе давний хвост сигналил бы
   вечно. Повтор по одному телефону не чаще раза в 6 часов (`tg_sent`,
   kind='sms_stuck'). Ключи `--test` и `--force`.
7. **Панель РОПа** (`web/dash.py`, бэкап `dash.py.bak-noreport`): статус
   `no_report` показывается как «ушла, отчёта нет», колонка «Доставлено»
   стала «Ушло» и считает `delivered + no_report`, иначе цифры не сходились.

Проверено на данных: 18 зависших закрылись в `no_report`, в 'sent' осталось 9,
ошибок в `smsd.log` нет, отправка идёт (доставка за 4 секунды). Сторож после
правки сигналит только по Симаненко — и по делу: у него **прямо сейчас**
3 сообщения висят Pending дольше 90 минут, телефон снова не забирает очередь.

Осталось проверить: реально ли шлюз применяет `ttl` (подтвердится, когда
зависшие протухнут — поставлена отложенная проверка) и дошёл ли сигнал
сторожа в Телеграм.

## 01.09.2026 — тест трёх метрик квалификации (бюджет / сроки / безопасность)

Прогон `app/test_qual3.py` на 40 случайных первых содержательных разговорах
(≥90 сек) за 25 дней. В базу ничего не писалось, старые разборы не трогали.
Стоимость факт: 54 ₽ (1,35 ₽ за разговор, gemini-3.7-flash).
Результат: бюджет спросил менеджер 52%, клиент дал рамку сам 15%,
не установлен 33%; сроки спросил 55%; безопасность (трансляция/видеосвязь) 32%.
Ручная сверка: 10 из 13 случаев «бюджет не установлен» проверены по тексту —
все верны, включая три, где слово «бюджет» в разговоре есть, но относится
к цене машины («в этот бюджет он и встает»), а не к деньгам клиента.
Ложных срабатываний безопасности на видеообзоре авто — ноль.
Разрез по менеджерам (выборки 5–11, только как гипотеза): Попов спрашивает
бюджет сам в 11% и сроки в 22% при лучшей в отделе безопасности 56%;
Симаненко — бюджет 82%. Формулировки в прошедшем времени
(«вы когда планировали покупкой заниматься») LLM ловит — регулярка не ловила,
это и был довод против шаблонного поиска.

## 01.09.2026 — внедрены метрики квалификации (бюджет / сроки / безопасность)

По итогам теста. В промпт `app/extractor.py` добавлены три поля (без цитат,
решение Тимофея): `budget_q` (manager_asked / client_stated / no),
`timeline_asked`, `safety_shown`. Определения — из чата 31.08–01.09: цена лота
и суммы менеджера бюджетом не считаются; сроки в любой форме, включая
прошедшее время; безопасность — только трансляция офиса или видеосвязь,
видеообзор машины не в счёт. Проверка на живом звонке: поля возвращаются,
QA-блок (14 метрик) и roles целы, JSON в лимит укладывается.
Бэкфилл НЕ делали: очередь `llm_queue` помнит обработанные звонки
(status=done + ON CONFLICT DO NOTHING), поэтому пересчёта старых нет —
поля появляются только у новых разговоров. Образ llm пересобран.
В KPI Шаг 0 — три колонки по первому содержательному разговору (≥90 сек)
в окне заявка → «Ждем паспорт»: «Бюджет» (установлен; мелким — доля, где
спросил сам менеджер), «Сроки», «Безопасность»; в развороте по заявке —
колонка «Квалификация» (Б/С/В с подсказками). Пороги: бюджет ≥70 / <40,
сроки ≥60 / <30, безопасность ≥40 / <15. В балл не входят.
Бэкапы: `app/extractor.py.bak-qual3`, `web/dash.py.bak-qual3`.
Цена: тест 54 ₽ + проверка 5 ₽; прирост к ежемесячным расходам ~600–700 ₽.

## 2026-09-01 — MAX: второй замер, цель `sub_max` подписки не отражает
- Со слов Тимофея: **2 112 подписчиков** (был 2 113 на 31.08). За сутки **−1**.
- За тот же промежуток Метрика засчитала `sub_max` (598773571): 23 за 31.08
  и 15 за 01.09 — то есть **38 «подписок» против фактических −1**.
  Всего с 18.08 цель насчитала 311, `sub_tg` за тот же период 55.
- Вывод: `sub_max` — цель типа `action`, ловит клик по ссылке на канал, а не
  факт подписки. Как метрику подписок использовать нельзя. Для Telegram
  сходимость была 61–70 % (замер 31.08), здесь — порядок расхождения.
- **Проверено: на оптимизацию ставок это не влияет.** Обе подписные кампании
  РСЯ («Горячие» 713638280, «Вовлеченные» 713639252) идут на `WB_MAXIMUM_CLICKS`,
  цель в стратегии не задана. Директ покупает клики, а не подписки — то есть
  бюджет на кривую цель не оптимизируется. Расход с 18.08: «Горячие» 10 880 ₽,
  «Вовлеченные» 23 894 ₽, вместе ~34,8 тыс. ₽.
- Оговорка: два замера — не ряд. −1 может быть шумом (отписки, посевы в группах
  идут постоянно). Но 38 против −1 списать на шум нельзя.
- Нужно: замерять подписчиков ежедневно в одно время хотя бы неделю. Без ряда
  вопрос «окупаются ли подписные кампании» не закрыть.

## 2026-09-01 — проверка вчерашней починки СМС
- **`ttl` работает**: появился отказ с причиной `TTL expired` от шлюза —
  ровно то, ради чего правка делалась (протухшее приветствие больше не уходит
  клиенту через сутки).
- **Статусы разложились**: за 20 часов delivered 100, no_report 9, skipped 6,
  failed 1 (тот самый TTL), sent 1. Вечного хвоста в `sent` больше нет.
- **Сторож работает**: `sms_watch.log` — «все телефоны в порядке (5 шт.)».
  Телефон Симаненко ожил сам, сигналов не было. Отправка сторожа в Телеграм
  на живом случае ещё не проверялась — сработает при следующем отвале.
=== 2026-09-01 Замер правок 01.09: база до, критерии, точки 02.09/07.09/28.09 — projects/reklama.md
- **01.09.2026** — замер MAX: **2 170** подписчиков (было 2 113 на 31.08), прирост
  +57 за сутки. Метрика за тот же интервал насчитала ≈28 `sub_max`. Разница
  предположительно посев, но интервал в одни сутки и расхождение часовых поясов
  (замеры MSK, счётчик Asia/Vladivostok) не позволяют считать это сверкой.

## 2026-09-01 — запрет передачи лидов между менеджерами (lead-router)
- Менеджер больше не может перекинуть свой лид коллеге: сторож закрепления
  возвращает ответственного и пишет пометку в карточку. Разрешено только
  `synergosmoto@gmail.com` (РОП Светлана).
- Штатного права на это в Битриксе нет — смена ответственного входит в общее
  «Изменение». Подписка на событие тоже не вышла: `event.bind` входящему вебхуку
  запрещён (`WRONG_AUTH_TYPE`), события только для OAuth-приложений.
- Поэтому опрос: `/etc/cron.d/lead-router-ownership`, каждые 5 минут,
  `router.py --ownership-sweep 1` под `flock`. Реакция — до 5 минут.
  Мгновенной станет, если завести на портале исходящий вебхук `ONCRMLEADUPDATE`
  на `/event/<секрет>` — обработчик уже написан.
- Перед включением запомнены ответственные у 2000 последних лидов
  (`--ownership-init 2000`).
- Проверено на проде на техлиде #225521: откат, пометка, журнал, повторный проход.
  Лид удалён. Тестов в наборе 43, все зелёные.
- Новое в `state.db`: таблицы `ownership`, `ownership_log`. Заведена ротация логов
  `/etc/logrotate.d/lead-router`.
- Бэкапы прежней версии: `/root/router.py.bak8`, `/root/config.yaml.bak8`.
- Осталось прежним долгом: перегенерировать код вебхука Битрикса, светившийся
  на скриншотах.
- **02.09.2026** — предварительная сверка скоринга: обе модели монотонны, v2b концентрирует лучше (топ-15% = 44% продаж), группа «≈» не расслоилась, пророчества нет
=== 2026-09-02 Каннибализация ТК: клоны фидов подтверждены, ранжир CPA, план консолидации — projects/reklama.md
=== 2026-09-02 Продажи по странам vs кампании; расхождение цели 255698794 (157 vs 98 WON) — reklama.md, open-questions 47–48
=== 2026-09-02 Цель «Заказ оплачен+» = стадия «Ждём договор», конверсия 85 % → порог CPA 85 000; вопрос 47 закрыт — reklama.md, decisions.md

## 01.09.2026 — Visual: блок «Воронка по менеджерам»

Просьба Тимофея. Внизу страницы Visual — таблица: заявки с 15.07 и сколько
дошло до каждой стадии («Ждем паспорт» → «Ждем договор» → «Ждем предоплату»
→ «На торгах» → продажа), крупно — число, мелким — конверсия из предыдущей
стадии; последняя колонка — сквозная. Стадия считается по максимальной
достигнутой (stage_history + status_id='CONVERTED'), перескок засчитывается
всем пройденным шагам. Подсветка — отклонение от конверсии отдела на том же
переходе более чем на 20%, только при 8+ заявках на входе. Строка «Отдел» —
итог. Функция `_vz_funnel` + FUNNEL_SQL в web/dash.py, бэкап dash.py.bak-funnel.
Первый срез (5317 заявок): вход в воронку у всех одинаков (3%), расходятся
ниже — Жернов 81% торги→продажа и сквозная 1,43%; Пономарь 47% и 0,68%;
у Попова провал на «договор → предоплата» (62%) при лучшем входе (41 паспорт).

## 01.09.2026 — воронка Visual: переключатель окна

Над таблицей воронки — кнопки «Месяц» (30 дней, открыт по умолчанию),
«3 месяца», «Эта неделя», «Прошлая неделя». Все четыре окна считаются при
сборке, переключение на странице без перезагрузки (как «По дням» в KPI).
FUNNEL_SQL получил верхнюю границу %(until)s; `_vz_funnel_one` — одно окно,
`_vz_funnel` — обёртка с кнопками. Починены None-проценты на коротких окнах
(когда до стадии не дошёл никто — «—» вместо ошибки). Сборка страницы ~3 сек.

## 01.09.2026 — воронка Visual: окна «Прошлый месяц» и «6 месяцев»

Кнопок стало шесть: Месяц · Прошлый месяц · 3 месяца · 6 месяцев ·
Эта неделя · Прошлая неделя. Сборка страницы выросла до ~6 сек (шесть окон).
Падеж месяца поправлен на MONTHS_NOM («за август», а не «за августа»).
