# Секреты — никогда в репозиторий (правило 10 реестра)
.kbtoken
*.token
.env
.env.*
*.key
*.pem
id_rsa*
id_ed25519*

# Сырые выгрузки, логи, временное (правило 9) — им место в /root/out
*.bak
*.bak-*
*.tmp
*.log
*~
.DS_Store
