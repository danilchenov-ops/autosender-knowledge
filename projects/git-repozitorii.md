# Git-репозитории и правила работы двух агентов

Перевод реестра и кода Виртуального РОПа под git с зеркалом на GitHub, чтобы Claude и Codex
держали один контекст проекта.

## СОСТОЯНИЕ НА 13.09.2026

_Переписывается целиком на каждом чекпоинте. Старше 14 дней — не верить, обновить._

- **Что есть сейчас:**
  - Аккаунт GitHub — `danilchenov-ops`. Оба репозитория приватные, запушены, ветка `main`.
  - `autosender-knowledge` ← `/opt/knowledge`: 345 коммитов, синхронно (0 ahead / 0 behind).
    `.kbtoken` вычищен из всей истории, скан всех коммитов на секреты — чисто.
    Бэкап до перезаписи: `/root/out/knowledge-before-filter-20260913.bundle`.
  - `autosender-ropbot` ← `/opt/ropbot`: 3 коммита, 178 файлов, `.git` 1.7 МБ.
    Рабочее дерево без данных ужалось с 819 МБ до 2.2 МБ.
  - `AGENTS.md` + симлинк `CLAUDE.md` в обоих каталогах. В ropbot зафиксировано главное:
    код `app/` запечён в образ (`build: ./app`), правка не действует без `docker compose build`.
  - Доступ: **два разных deploy key** — GitHub не даёт повесить один ключ на два репозитория.
    `~/.ssh/github` → knowledge (хост `github.com`), `~/.ssh/github-ropbot` → ropbot
    (алиас `github-ropbot` в `~/.ssh/config`). Оба с правом записи.
  - Проверено после переноса: 6 контейнеров живы, 29 крон-таймеров на месте, база отвечает.

- **Действующие решения:**
  - Сайт autosender.ru остаётся на GitLab и в этот контур не входит.
  - Тестовый контур для ropbot не поднимаем — правим наживую.
  - Веток нет ни в одном репозитории, только `main`, прямой коммит без PR.
  - Пишущая копия одна — на сервере. Codex правит через MCP `ropbot`, клон на маке для чтения.
  - 72 ручных `.bak` (13 МБ, 43 копии `dash.py`) в `backups/manual-bak-2026-09-13/`, не удалены.

- **Что осталось:**
  - Настроить Codex: клон `autosender-knowledge` на маке для чтения + MCP `ropbot`
    в его конфиге, чтобы писал на сервер.
  - Семь MCP-серверов на маке Тимофея до сих пор без git — отдельная задача.

- **Ближайшие контрольные точки:**
  - 20.09.2026 → `git status` чист в обоих репозиториях, push проходит у обоих агентов.
  - 01.10.2026 → замер метрики (ниже).

## Цель

Claude и Codex в каждой сессии стартуют с одного и того же контекста, а изменения кода
перестают теряться и страховаться копиями файлов.

Метрика успеха: **к 01.10.2026 в `/opt/ropbot` ноль новых файлов `.bak` и `git status`
чист на конец каждой сессии** (проверка: `find /opt/ropbot -name '*.bak*' -not -path '*/backups/*' | wc -l` = 0).

## Где код и данные

- Реестр: `/opt/knowledge` на 5.35.99.93 → репозиторий `autosender-knowledge`.
- Код РОПа: `/opt/ropbot` на 5.35.99.93 → репозиторий `autosender-ropbot`.
- Архив ручных бэкапов: `/opt/ropbot/backups/manual-bak-2026-09-13/` (72 файла, 13 МБ).
- Ключ доступа: `~/.ssh/github` на ропбот-сервере. Указатель — `integrations.md`.
- Сайт (не наш контур): GitLab `Sisyphus-forever/autosender-2025`, KB `/opt/claude-kb`
  на 83.136.235.210.

## Принятые решения

| Дата | Решение | Кто | Основание |
|---|---|---|---|
| 13.09.2026 | Переносим на GitHub всё по Autosender кроме сайта | внесено Тимофеем | нужно, чтобы агенты всегда помнили контекст проекта |
| 13.09.2026 | Тестовый контур для ropbot не делаем, правим наживую | внесено Тимофеем | это не сайт, цена ошибки приемлема |
| 13.09.2026 | Два репозитория, не монорепо и не по направлениям | рекомендовано Claude | код рекламы, Битрикса и РОПа физически в одном `app/`, делит `common.py` и одну базу |
| 13.09.2026 | Веток нет, только main | рекомендовано Claude | рабочий каталог один и он же прод: переключение ветки меняет файлы под вторым агентом и под контейнерами |
| 13.09.2026 | Codex правит код через MCP `ropbot` на сервере, а не из клона | рекомендовано Claude | иначе возвращается деплой через `git pull`, от которого отказались |
| 13.09.2026 | `.bak` переносим в архив, а не удаляем | рекомендовано Claude | до git это была единственная история изменений |

## Открытые вопросы

Ведутся в /opt/knowledge/open-questions.md, здесь — только номера.

## Хронология

_Только дописывается. Старше 30 дней при >800 строк — в projects/archive/._

**13.09.2026.** Разведка по серверам. Найдено: реестр уже под git (339 коммитов на тот
момент), `/opt/ropbot` без git вообще при 819 МБ и 160 файлах кода, 72 ручных `.bak`
(43 копии `dash.py` по 384 КБ, плюс два `.env.bak-*` с боевыми токенами в каталоге кода),
`.kbtoken` в индексе и во всей истории реестра. Семь MCP-серверов живут только на маке
Тимофея, без git — вынесено в отдельную задачу на потом.

Установлено, что сайт ведётся не на GitHub, а на GitLab (`Sisyphus-forever/autosender-2025`,
1004 коммита, 112 за последние 30 дней) и там уже есть рабочий `AGENTS.md` с флоу
ветка → MR → merge и отдельными личностями `claude-autosender` / `codex-autosender`.
Этот файл взят за образец.

Сделано: `.gitignore` в обоих каталогах; `.kbtoken` (13 символов, нигде не используется,
скрипты берут токен из `/opt/ropbot/.env`) убран из индекса и вычищен из 341 коммита;
скан всей истории на секреты — чисто; `AGENTS.md` и `CLAUDE.md` в обоих репозиториях;
72 `.bak` в архив; первый коммит кода РОПа; ssh-ключ для GitHub.

Попутно зафиксировано: в 18:51, во время перезаписи истории, в реестр закоммитил другой
чат («КТ5 14.09»). Ничего не пострадало, но это ровно тот сценарий, из-за которого
в `AGENTS.md` записано правило про `git status` перед правкой.
