# Панели менеджеров и РОПа

Веб-страницы с картиной по менеджеру, каждому своя ссылка. Начато 22.08.2026.
Пилот — одна страница «Качество разговоров» на Пономаре, дальше расширяем
и по людям, и по содержанию.

## Зачем

Всё, что посчитано — правила идеального менеджера, скоринг заявок, оценка
разговоров — лежит в базе и раз в неделю уходит таблицей в Телеграм. Таблица
в мессенджере не годится для ежедневной работы: её нельзя открыть, когда нужно,
и в неё не влезает список конкретных разговоров со ссылками в CRM.

Решение Тимофея 22.08: свой сервер, постоянные ссылки, у каждого менеджера своя.
Отвергнуто: публикация артефактом на claude.ai (страница — снимок, обновление
только перепубликацией, доступ через расшаривание аккаунта).

## Как устроено

    Postgres ──► web/dash.py ──► /opt/ropbot/web/d/<токен>/index.html ──► nginx

- **Токены** — таблица `dash_tokens` (`token`, `kind`, `portal_user_id`, `page`,
  `active`, `note`, `created_at`, `built_at`). Ссылка вида
  `/d/b17904628c068aa12d7aee5f4e9b1679/`, 32 hex-символа, `openssl rand -hex 16`.
- **Генератор** — `/opt/ropbot/web/dash.py`. **В образ не запечён**: подаётся
  в контейнер через stdin (`docker exec -i ropbot-collector-1 python - --token X
  < web/dash.py`). Поэтому правка страницы не требует `docker compose build`
  и не роняет расшифровку. Обратная сторона — файл живёт вне `app/`, при переносе
  проекта его легко забыть.
- **Сборка** — `/opt/ropbot/web/build.sh`, крон `*/15` под `flock -n`
  (машина загружена, прогоны не должны наслаиваться). Запись атомарная:
  во временный файл, потом `mv`.
- **Отдача** — nginx, сайт `/etc/nginx/sites-available/dash`, корень
  `/opt/ropbot/web`. Каталог `/d/` без листинга, `/` отдаёт 404, `robots.txt`
  запрещает всё, заголовки `X-Robots-Tag: noindex`, `Referrer-Policy: no-referrer`,
  `Cache-Control: no-store`. В ufw открыты 80 и 443.
- **Данные наружу не ходят**: страница статическая, база в интернет не смотрит.

## Страница «Качество разговоров» (готова 22.08.2026)

Считается по карточке эталонного скрипта (`app/card.py`), окно 14 дней:

| Блок | Что показывает |
|---|---|
| Балл | средний `card_score` 0–100, среднее по отделу, изменение за 7 дней к предыдущим 7 |
| Плитки | сколько разговоров разобрано, доля сильных (≥50) и слабых (<25) |
| Пять критериев | доля разговоров, где критерий зачтён; штрих — среднее по отделу; вес критерия |
| По дням | средний балл за день, пунктир — среднее по отделу, таблица под спойлером |
| Разговоры | последние 40: дата, длительность, балл, что зачлось и чего нет, следующий шаг, резюме, ссылка в Битрикс |
| Как это считается | оговорки, в том числе «не для премий» |

Правила счёта: непригодные номера отброшены (правило 1), отдел — только
`managers.department = '[5]'`, время местное (Влд).

Телефон клиента маскируется (`+7 (904) ***-**-78`), полная карточка — по ссылке
в Битрикс, где и так нужна авторизация.

Цифры сверены отдельным запросом 22.08: 212 разговоров, балл 33,6, сильных 25%,
слабых 37%, отдел 33,9 — сходится со страницей.

## Пилот

| Кто | Токен | Ссылка |
|---|---|---|
| Дмитрий Пономарь (15077) | `b17904628c068aa12d7aee5f4e9b1679` | `http://danilchenov.fvds.ru/d/<токен>/` |

Выбран потому, что у него больше всех разобранных разговоров (230 на 22.08)
и самая большая нагрузка — страница сразу наполнена.

## HTTPS: упёрлись в чужой лимит

`certbot --nginx` на `danilchenov.fvds.ru` вернул
`too many certificates (50) already issued for "fvds.ru" in the last 168h`.
Имя — поддомен общего домена FirstVDS, недельный лимит Let's Encrypt считается
на `fvds.ru` и выбирается всеми клиентами хостера. Это не разовая неудача:
то же самое будет при каждом продлении.

Что сделано: `/opt/ropbot/web/certbot_retry.sh`, крон `17 * * * *` под `flock` —
ловит свободное окно и **сам снимает себя с крона** после успеха.

Запасной вариант Buypass Go SSL отпал: `api.buypass.com` не резолвится через
DNS-серверы хостера (через 8.8.8.8 резолвится). Лезть в `/etc/systemd/resolved.conf`
на боевой машине ради обходного пути не стали.

**Правильное решение — поддомен своего домена**, например `rop.autosender.ru`
A-записью на 5.35.99.93. Тогда лимит свой, продление автоматическое, и ссылка
выглядит прилично для менеджеров. Ждём решения Тимофея.

Пока сертификата нет, ссылка отдаётся по HTTP. **После выпуска сертификата
токен пилота сменить** — он какое-то время ходил открытым текстом.

## Осталось

1. **Поддомен `rop.autosender.ru`** (или подтверждение, что живём на fvds.ru).
2. **Сводная страница РОПа** — все менеджеры строками, показатели столбцами,
   подсветка отклонений от нормы.
3. **Остальные пять менеджеров** — по токену каждому, содержание то же.
4. **Другие блоки на страницу:** шесть правил идеального менеджера
   (`v_manager_ideal`), очередь заявок дня по классам ⚡/★/♛, динамика по неделям
   (`v_manager_weekly`), результат по вызревшим лидам (`v_manager_result`).
5. **Как раздавать ссылки.** Сейчас — переслать вручную. Аккуратнее: команда
   в @ironbossAS_bot, бот уже знает сотрудников и выдаёт ссылку тому, кто просит.
6. **Договориться, как этим пользуются.** Страница задумана для планёрок
   и отбора записей на прослушивание. Если её начнут читать как KPI — она
   врёт: каждый третий проигранный разговор набирает больше 50 баллов.

## Осторожность

- Окно 14 дней, у Владимира за это время может не набраться выборки —
  страница переживёт пустоту, но выводы по ней делать нельзя.
- Карточка есть только у разговоров от 90 секунд за последние три недели.
  Короткие звонки и недозвоны в балл не входят вообще.
- Сравнение с отделом — среднее по шести менеджерам, а не норма. Норма для
  критерия «следующий шаг» — 68% (уровень выигранных разговоров), а не 22%.


## РОП-ссылка (22.08, ночь)

Токен `kind='rop'` в `dash_tokens`, без `portal_user_id`. Вкладки:
**Контроль** (index.html — лента сбоев по живым заявкам, журнал `rop_control`,
синк `web/control.py` первым шагом `build.sh`) и **Менеджеры** (menedzhery.html —
сводная строка на каждого). Подробности — changelog 22.08 ночь и само ТЗ
`inbox/tz-kontrol-ropa.md` (исполнено). Константы порогов — в `web/control.py`.
Пункт 1 из «Осталось» (сводная РОПа) закрыт этой же работой.


## Дашборд менеджера: очередь «Начни с этих» (23.08, ВЫКЛЮЧЕНА в тот же день)

Над таблицей «Сейчас» — до 10 заявок в порядке: свежие <24 ч без разговора
(новые сверху, без попытки дольше 2 ч — красным) → ♛/составная ≥60 с тишиной
от 24 ч (с 36 ч красным; фора в сутки до ленты РОПа) → ⚡A/★B без звонка →
недозвоны A/B (1–4 из 5). Код: `queue_day`/`queue_block` в dash.py, данные —
QUEUE_SQL страницы «Заявки». Приоритет свежих — решение Тимофея: клиент
только что оставил заявку и ещё у телефона. Внизу — «в очереди N, показаны
первые 10». Попутно: маска телефонов в названии лида, скрыт «city not found»,
починен `.lead-w b.hot` (красная подсветка). **Выключена решением Тимофея в тот же день** («выглядит не очень, потом придумаю»): вызов queue_block убран из page_mgr_dash, код оставлен, включается одной строкой. Попутные починки остались.

## Выданные ссылки (23.08.2026)

Все ссылки вида `https://danilchenov.fvds.ru/d/<токен>/`, отдаются по https
(сертификат выпущен 22.08 вечером).

| Кто | Роль | Токен | Ссылка отправлена |
|---|---|---|---|
| Станислав Жернов (8829) | manager | `21aa9ffd0bd7c72b0bda374f5ecba9f2` | ✅ 23.08 в телеграм |
| Николай Попов (11807) | manager | `e339b6b4ee182c86fee1648dad88f076` | ✅ 23.08 |
| Дмитрий Пономарь (15077) | manager | `477049e5ce8f085e87a28b0daf6ad1c8` | ✅ 23.08 |
| Александр Томаш (11357) | manager | `2240008f4cf41c6aa9225e3e188c8df6` | ✅ 24.08 (зарегистрировался в боте ночью) |
| Михаил Симаненко (8831) | manager | `c223895dbd9a58c8ad1561c51c940d6c` | — не выдавалась |
| Светлана | rop | `7f4d4e1805673d4605e42a052e13cd41` | ✅ 23.08 |
| Тимофей | admin (хаб) | `530cb70bb7bb40127069bcaec0e32507` | — |

Рассылка — `app/send_dash_links.py`, повторно в тот же чат не уйдёт
(`tg_sent`, `kind='dash_link'`, проверка по `ok`).

**Навигации «как у РОПа» у менеджера нет** (проверено 23.08): в шапке только его
собственные вкладки «Дашборд» и «Скрипт», список менеджеров и хаб «Все панели»
ему недоступны. Ссылка «← Все панели» есть в разметке, но показывается только
браузеру, где в `localStorage` лежит адрес хаба, — то есть Тимофею.

Все четверо менеджеров и Светлана ссылки получили (Томаш — 24.08, сразу после
регистрации в боте). Без ссылки остался Михаил Симаненко (8831): токен заведён,
в боте не зарегистрирован.

## Дела и касания (24.08.2026)

**«Контроль отдела» → таблица «Запланированные дела в этом месяце»** (под «Сейчас»):
поставлено дел, клиентов, разговоров, касаний на клиента. Окно —
`max(1-е число месяца, 24.08.2026)`, обнуляется само 1-го числа.

- «Поставлено дел» — записи `activities` с `provider_id = 'CRM_TODO'`,
  созданные в окне, ответственный — менеджер. Это то, что менеджер **завёл**,
  а не выполнил.
- «Касание» = состоявшийся исходящий звонок (`duration > 0`). Определение моё,
  не Тимофея — если касанием считать и письма/сообщения, менять `PLAN_CALL_SQL`.
- На 24.08 у всех 1,1–1,3 касания на клиента: поговорили один раз и не вернулись.

**Дашборд менеджера → «Дела на сегодня»:** цифра раскрывает точный список лидов
(`<details>`: время дела, название, телефон, ссылка на карточку). Раньше вела
на `ACTIVITY_COUNTER[]=4` — это открывало все просроченные дела, а не сегодняшние.
Фильтра «дело со сроком сегодня» в URL списка лидов Битрикса нет, поэтому список
строится у нас: он из тех же строк, что дали цифру.

**Не решено:** просрочка по делам огромная (Пономарь 258, Симаненко 229) —
кандидат в ленту «Контроль» отдельным пунктом.

## Вкладка РОПа «Слепок работы» (25.08.2026)

По 10 последних заявок каждого менеджера, взятых в работу (не моложе 48 ч,
был исходящий звонок или смена статуса; спам/дубли/непригодные номера
отброшены, глубина 120 дней). Одна строка = один клиент, слева направо:
заявка → звонки → статусы → дела → финал.

- Шапка строки: значок ♛/⚡/★, имя+марка (клик — карточка Битрикса), маска
  телефона, город/бюджет/срок; справа итог — статус, сколько живёт заявка,
  сколько разговоров.
- Плашки: разговор «↗ 7:51 · договорённость» (фраза — next_step из
  call_scores, tooltip — summary), «↗ недозвон ×N» (серии свёрнуты, порог
  разговора 30 сек), «↙ клиент звонил» (входящий без разговора), пунктир
  «→ Статус», «📅 дело на …» (только незакрытые, просроченные красным),
  финал «✓ успех / ✕ статус».
- Паузы между событиями подписаны; красным — от 48 ч, у первого отклика —
  от часа с начала рабочего дня (ночные заявки 🌙 считаются с 9:00).
- Константы SLEPOK_* сверху блока в web/dash.py. Собирается в bundle all
  (раз в 15 минут), в «живые» страницы не входит.

Попутно починена история статусов лидов: Битрикс для лидов отдаёт STATUS_ID,
crm_sync писал пусто. Исправлен crm_sync.py (вступит при пересборке образа),
бэкфилл 120 дней сделан app/stagehist_fix.py, крон 13,43 * * * * дозаполняет
свежее до пересборки. Историю глубже ~конца апреля 2026 не поднимали.


## Сегодняшний прирост в таблице «Сейчас» (25.08.2026)

Просьба Тимофея: рядом с абсолютной цифрой статуса — зелёная стрелка ↑N,
сколько заявок вошло в этот статус СЕГОДНЯ («5 на торгах, +1 сегодня»).

- Источник — `stage_history` (переходы лидов по статусам, время Влд),
  свежее дозаполняется кроном 13,43. `DELTA_TODAY_SQL` + `delta_today()`
  в `web/dash.py`, кэш на сборку.
- Считается по всем колонкам таблицы единообразно: для «Не обработан» стрелка
  показывает приток новых лидов за день, для остальных — продвижение по
  воронке. Строка «Отдел» суммирует прирост.
- Работает и на дашборде менеджера (своя строка, свой прирост), и на странице
  РОПа. Класс `.up-today` (зелёный, `var(--good)`).
- Стрелка — это движение за день, а не только текущий остаток: лид, вошедший
  сегодня в статус и уже ушедший дальше, в стрелке учтён.
- Бэкап перед правкой: `web/dash.py.bak-delta`.

## Вкладка РОПа «Замеры» и судьба слепка (26.08.2026)

Визуальная лента «Слепок работы» убрана с экрана решением Тимофея
(код page_slepok в dash.py жив, вернуть — две строки, см. комментарий
у ROP_TABS; бэкап dash.py.bak-slepok-off). Механизм сбора manager_snapshot
и вкладка «Эталон» остаются — «Эталон» питается снимком.

Взамен — вкладка **«Замеры»** (zamery.html): памятка Тимофея о фоновых
процессах — что запущено, с какого числа, когда вернуться. Содержимое —
рукописный список ZAMERY в web/dash.py; **обновлять при каждом запуске
или остановке процесса**, страница обещает актуальность.
