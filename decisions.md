# Принятые решения

Что решили, когда и почему. Чтобы не переигрывать одно и то же по кругу.

---

## Реклама

**Основной KPI — стоимость цели «Заказ оплачен», не заявка и не клик.**
Безубыточный CPA — 100 000 ₽ валовой прибыли с оплаченного заказа.
Заявка ≈ 6 700 ₽. Цикл сделки 30–90 дней, бывает до 3 месяцев.

**Поиск на подписку не запускаем вовсе.** 18.08.2026. В поиске нет спроса
на подписку, плюс каннибализация с «ТЕСТ Поиск Ядро».

**Прокладка живёт на поддомене со счётчиком основного сайта**, а не на
отдельном счётчике. Один ClientID, ретаргетинг без кросс-доменных потерь.

**Горячие и Вовлечённые — раздельными кампаниями, не группами.** 18.08.2026.
В одной кампании автостратегия отдала бы почти весь бюджет большому сегменту.

**Чужие блок-листы площадок не переносим.** 19.08.2026, по итогам инцидента.
Площадки режем только по факту открутки, из отчёта. Ограничения возвращаем
по одному, с интервалом, чтобы видеть, какое душит.

**UTM на подписных кампаниях не ставим.** 19.08.2026, решение Тимофея.
Страница без форм, стоимость подписчика читается из отчётов Директа по цели.

## Инфраструктура

**Реестр — markdown в git, не база данных.** 20.08.2026. Факты разнородные,
схема была бы обузой; git даёт историю изменений даром.

## Виртуальный РОП / скоринг заявок

_Источник: `projects/virtualnyy-rop.md`, перенос чата «Заявки scoring система» 20.08.2026._

**Скоринг считает только поведение на сайте, сигналы CRM в баллы не идут.** 19.08.2026.
Первая версия использовала кампанию, ответы квиза и текст карточки — отвергнута, когда
стало видно недостоверность статусов: за сутки менеджеры списали в отказ 26 «не готов
купить» и 12 «неплатёжеспособных», из них 21 заявка была в верхней корзине скоринга.
Факт успешной сделки для обучения оставлен — за ним стоят деньги.

**Звонки и расшифровки в скоринге не участвуют.** 19.08.2026, решение Тимофея.

**Результат — маркировка в CRM, а не рассылка в Telegram.** 20.08.2026. Дайджест
отработал трижды и убран из крона: менеджер видит приоритет в той же карточке,
где работает, без второго окна.

**Значки в названии лида — только BMP-символы: A «⚡», B «★».** 20.08.2026, вынужденно.
Битрикс24 обрезает название до пустой строки на четырёхбайтовых эмодзи: просили 🔥,
потеряли названия у 17 заявок, восстановили. Проверены и работают ⚡ ★ ◆ ✔ ❗.
Перед массовой правкой карточек — бэкап полей (для названий есть `lead_title_backup`).

**Сопоставление заявок без ClientID по времени: мягкий вариант отвергнут, строгий принят
с меткой «≈».** 20.08.2026, по валидации на 400 лидах с известным ответом. Мягкие ключи
дали точность 12,5% — девять профилей из десяти были бы чужими. Строгие (окно 30 минут,
обязательное совпадение города и UTM, единственный кандидат) — 65,7%, покрытие ~22%
безымянных. Причина трудности: 15–17 тыс. визитов в сутки.

**Оценка замораживается при выставлении и не пересчитывается** — иначе замер точности
превращается в подгонку задним числом. Старт замера 20.08.2026, сверка 2 сентября.

**Обучающее окно — 440→45 дней:** свежие заявки в обучение не берём, исход не созрел
(90-й процентиль цикла сделки 38 дней). Переобучение ежедневно в 01:10 МСК.

---

## Звонки и расшифровка (из projects/zvonki-transkribaciya.md, 20.08.2026)

**Источник звонков — Битрикс24, а не API Билайна напрямую.** Телефония Билайна уже
интегрирована в портал, `voximplant.statistic.get` отдаёт и запись, и привязку к лиду,
и ответственного. API Билайна остаётся запасным путём. Нагрузка ~30 запросов в сутки
при лимите 172 800, событийные вебхуки не понадобились — опрос раз в 5 минут.

**Аудио не хранится вообще (`KEEP_AUDIO_DAYS=0`).** Записи и так лежат в Битриксе,
расшифровки занимают ~0,25 ГБ в год против ~12 ГБ в месяц аудио. Отклонён буфер
в 3 дня «на случай перепрогона»: свежие записи перекачиваются из Битрикса.
Следствие: расшифровки хранятся бессрочно, любой новый разбор можно прогнать
по архиву задним числом.

**Модель распознавания — faster-whisper `large-v3-turbo`.** Сравнение на одном и том же
живом звонке: `small` 3,3x реального времени, но теряет деловые факты («аукционный лист,
4 балла» превращается в «листочка открытой»); `medium` 1,3x; `turbo` 1,2x и лучшее
качество из трёх. При практически равной скорости выбран turbo. Отклонены `medium`
и `small`.

**Модель разбора разговоров — `gemini-3.5-flash-lite`.** Тест на 12 разговорах:
5 договорённостей из 5, ноль ложных, чистая шкала уверенности (0.0 против 0.9).
Отклонены: `gpt-5-nano` (пропустил 4 из 5), `gpt-5-mini` (ложное срабатывание на
вежливом «Будем на связи»), `gemini-2.5-flash-lite` (пропуск и ошибка в дате).
Разница в цене ~800 ₽/мес признана несущественной против цены пропущенной договорённости.

**Провайдер моделей — ProxyAPI.** Рубли, российская карта, без документов, все модели
одним ключом с возможностью переключения одной строкой конфига. Отклонён GigaChat:
freemium прямо запрещён для коммерческого использования, а платный доступ для юрлица
требовал договора.

**Telegram — только через SSH-туннель до заграничного релея.** С РФ-сервера
`api.telegram.org` недоступен, проверено: соединение не устанавливается. Служба
`tg-tunnel.service` держит `ssh -N -D 0.0.0.0:1080`, весь остальной трафик идёт напрямую.
Отклонены: Битрикс-чат (проще и бесплатно, но менеджеры работают не в нём),
MAX (не изучен), прокси только для Telegram (менее предсказуем).

**Считать по клиенту (номеру телефона), а не по карточке лида.** Принято после ошибки:
первый отчёт по недозвонам показал 47 «потерянных» клиентов, из которых реальных
оказалось 15 — остальные 32 были дублями, где клиента вёл другой менеджер по другой
карточке. Причина: за 90 дней 1 268 случаев, когда с одного номера за сутки заводится
2 и более заявок на 2–4 менеджеров.

**Лиды и звонки с непригодным номером телефона исключаются из всей статистики.**
По инициативе Тимофея. Таких лидов ~7 500, сделок среди них ноль, но 1 300 из них были
помечены «Недозвон» — набрать их физически невозможно. Правило зашито в представления
через `phone_kind`, а не соблюдается вручную.

**Рассылка напоминаний не включается до согласования правил.** Договорённости копятся
в `followups`. Причина: первые ложные срабатывания убивают доверие к системе, после чего
её обходят.

**Разбор качества разговоров отложен до закрытия организационных дыр.** Узкие места
оказались процессные (не звонят, звонят поздно, закрывают после одной попытки),
а не речевые. Языковая модель для их обнаружения не требуется.

**Приоритет в очереди расшифровки вместо ожидания полного бэкфилла.** Сначала разговоры
по выигранным лидам, затем контрольная группа проигранных с 3+ звонками, потом остальное.
Причина: карту успешного пути нельзя строить без контрольной группы, но и ждать три
недели ради неё не нужно.

---

## Реклама — аудит Директа и Метрики (из projects/reklama.md, 20.08.2026)

**Все суммы в рекламных расчётах — с НДС.** Директ отдаёт расход с НДС 22 %,
Метрика — без. Расхождение в 22 % ломает любой вывод о CPA. Коэффициент проверен
на реальной кампании («ТК Япония НОВАЯ»: 367 330,51 / 301 090,49 = ровно 1,22),
а не взят из документации.

**Кластеры поисковых запросов считать фильтрами по всему кластеру, а не по топ-N.**
За 30 дней 84 200 уникальных фраз, топ-880 покрывают лишь 23 % расхода. Отвергнут
разбор по выгрузке топа: он давал ложную картину «кругом мусор» — отчёт Директа
не сортируется по расходу и режется по `limit` в алфавитном порядке.

**Минус-слова этому аккаунту не нужны.** Весь нецелевой кластер (бесплатно, ремонт,
запчасти, аренда, кредит, продать) — 1 967 ₽/мес, 0,07 % бюджета. Чужие бренды
и агрегаторы — ещё 12 240 ₽, 0,4 %, при нормальной цене заявки. Стандартная
рекомендация из чек-листа здесь не применима, время на списки не тратим.

**Запросы про калькулятор и растаможку не резать, а масштабировать.** Кластер
«калькулятор / растаможка / утильсбор / пошлины» даёт оплаченный заказ за 13 831 ₽ —
вдвое дешевле цели. Страница калькулятора конвертит в оплату в 18 раз выше средней
по сайту (0,463 % против 0,025 %). Отвергнута интуитивная рекомендация «минусовать
информационные запросы»: человек, который считает растаможку, уже выбрал машину.

**Корректировка +30 % на десктоп вместо снижения мобильных.** Не сокращает объём.
Отвергнут вариант «мобильные −25 %»: при 66 % бюджета на мобильных это обвал
трафика ради недоказанной экономии. Основание: за 90 дней десктоп-поиск даёт оплату
за 19 663 ₽, мобильный поиск — за 42 863 ₽; вывод устойчив в двух атрибуциях.

**«Япония-5» и «Япония-6» — срезать бюджет вдвое, не выключать.** Обратимость важнее
скорости: кампаниям 26 дней при цикле сделки 30–90. Отвергнуто выключение (правило
о лаге, прецедент «Кореи-2»: 2 оплаты за первый месяц, потом 10 за две недели);
отвергнуто «оставить как есть» — цена заявки у них 3 508 и 3 785 ₽ против
1 434–1 625 ₽ у лучших, а это от лага сделки не зависит.

**Временные корректировки не ставить.** Часовой пояс счётчика — Владивосток,
аудитория от Калининграда до Камчатки. Разброс индекса конверсии 1,61 / 0,71
объясняется географией не хуже, чем часом суток. Уверенность 50 % — мало для решения.
Нужен срез «час × регион».

**Порог решения — маржа 100 000 ₽, а не «хуже среднего».** Сегменты между средним
CPA и порогом убыточности корректируем, но не отключаем.

_Источник: `projects/reklama.md`, перенос чата «Аудит Яндекс.Директа и Метрики» 20.08.2026._

## Реклама: решения чата «Подключение API Директа и запуск тестовых кампаний» (17–20.08.2026)

**Аукционные запросы не минусовать.** Решение 17.08 заминусовать
`аукцион/аукционы/аукционе/аукционный` отменено 18.08. Первый вывод был сделан
по топ-18 фразам и одной атрибуции; пересчёт по всему кластеру (11 165 фраз)
дал 33 506 кликов, ~413 000 ₽ с НДС, 190 заявок, 7 оплат по последнему значимому
переходу и 9 по первому клику — CPA 46 000–59 000 ₽, прибыльно при марже 100 000 ₽.
Кластер верхневоронковый: открывает сделку, а закрывают бренд и прямые заходы.
Отвергнута собственная первая рекомендация. Порог отката: CPA полного кластера
выше 100 000 ₽ с НДС за квартал.

**«Япония-5» и «Япония-6» не останавливать — контроль 31.08.** Первоначально
рекомендовалась остановка (CPA 275 149 и 95 387 ₽). Возражение Тимофея про малый
возраст кампаний подтвердилось данными: CPL у обеих падает (3 199 → 2 800 и
3 116 → 2 661 ₽), потеря клик→визит сократилась с 31–33 % до 14–17 %, а прецедент
«Кореи-2» показывает 2 оплаты за первый месяц и 10 за следующие 13 дней.
Критерий 31.08: ≥ 4 оплат накопленно и CPL последней недели ≤ 2 200 ₽.

**Обучать стратегию теста на заявке, а не на оплате.** При 50 000 ₽/нед оплат
будет 1–3 в неделю при запаздывании в недели; автостратегии нужно от 10 конверсий
в неделю. Заявок ожидается ~28–30. Цель «Заказ оплачен» остаётся KPI оценки,
но не целью обучения.

**Ценности целей проставлять реальные, а не заниженные.** Для стратегии важны
пропорции, но заниженные значения ломают колонки «Доход»/ДРР в Директе и делают
бессмысленной будущую целевую ДРР. Отвергнут вариант «поставить пропорционально,
но в 10 раз меньше» — функционально идентичен, а отчётность портит.

**Venoo оставить в целях с ценностью 1 400 ₽.** Предлагалось убрать как самую
частую и дешёвую цель, способную перекосить обучение. Возражение Тимофея
(«чат передаёт телефон в CRM») проверено: 2 904 визита с чатом → 2 081 созданный
заказ → 41 оплата, то есть 1,4 % и 12 % всех оплат сайта. Убирать нельзя,
но и приравнивать к заявке нельзя — отсюда 1 400 ₽.

**Тестовую кампанию собирать обычной поисковой, а не товарной.** Товарные
кампании не читаются через `campaigns.get` и не дают ни корректировок ставок,
ни управления фразами. Обычная кампания видна в API и управляема — это
дополнительный аргумент к самой гипотезе теста.

**Новые кампании создавать через API черновиками.** Запись включается точечно
и выключается сразу после; кампании создаются в статусе DRAFT, деньги тратить
не могут, Тимофей проверяет и запускает вручную. Отвергнут вариант «оставить
запись включённой постоянно» — предохранитель не должен зависеть от аккуратности.

**Бюджет брендовой кампании — 3 500 ₽/нед, от фактического спроса.** Объём
брендового трафика определяется спросом, а не бюджетом: 37 005 ₽ за 90 дней.
Больший бюджет просто не потратится. Пересмотр — если кампания две недели подряд
упирается в лимит.

**Запретить 13 площадок РСЯ (17.08).** Основание: пул 668 391 ₽ без НДС за 90 дней,
7 оплат, CPA ~116 000 ₽ — выше порога маржи; плюс независимые признаки
(у `dsp-opera-exchange.yandex.ru` 83 % оплаченных кликов не доезжает до сайта,
отказы 43 %). Запрещается приложение `com.vkontakte.android`, но НЕ сайт `vk.com`
(CPA 5 519 ₽). Решение обратимо. **Конфликтует с рекомендацией аудита 20.08
(там пять других площадок) — см. открытый вопрос.**

**Девять обязательных правил анализа внесены в инструкции claude-проекта.**
Появились после ошибки с аукционами: полнота выборки, две атрибуции, лаг сделки
30–90 дней, порог решения — маржа, считать всю воронку, базис НДС, обратимость
с датой контроля, знание бизнеса выше выборки, фиксация ошибок.

_Источник: `projects/reklama.md`, перенос чата «Подключение API Директа и запуск тестовых кампаний» 20.08.2026._

## Битрикс24: распределение лидов и аналитика

_Источник: `projects/bitrix-analitika.md`, перенос чата «CRM Bitrix распределение менеджеров» 20.08.2026._

**Распределение лидов — внешний обработчик на VPS, а не бизнес-процесс Битрикса.**
16.08.2026. Круговая очередь в БП хранится костыльно, параллельные экземпляры выдают
одного менеджера дважды, онлайн-статус БП не видит. Отвергнут и штатный робот
«Изменить ответственного»: не умеет переключать режим по дню недели и не имеет
фолбэка, когда рабочий день не начал никто.

**Указатель круга общий для будней и выходных**, хранится по мастер-списку, а не по
отфильтрованному. 16.08.2026. Очередь не сбивается при переключении режимов.

**`online_mode: any`, а не `timeman`.** 16.08.2026. Модуль «Рабочее время» на практике
не используется — рабочий день не открыт ни у кого. Режим `timeman` давал бы фолбэк
каждые выходные. При появлении дисциплины отметок перейти на `timeman` — он честнее.

**Цифры считает код, LLM только комментирует.** 20.08.2026. Модель получает готовые
числа на вход и не вычисляет их сама: одна правдоподобная выдуманная конверсия в отчёте
руководителю убивает доверие ко всей системе.

**Выход системы — задача в Битриксе с дедлайном, а не отчёт.** 20.08.2026. Отчёты
перестают открывать через две недели; очередь работы — нет.

**Потолок на «горячие» лиды — не больше 5 в день.** 20.08.2026. Система, выдающая
тридцать срочных лидов ежедневно, будет выключена на третий день. Пропустить половину
дешевле, чем зашуметь.

**KPI «заполненность CRM» не вводим.** 20.08.2026. Даст мусорные данные вместо пустых,
а на мусорных потом строят решения. Заполненность — побочный эффект нормальной работы.

**Отдельный Bitrix MCP на машине Тимофея не нужен.** 20.08.2026. Предлагался, пока
считалось, что доступа к CRM нет. Доступ есть через `ropbot ssh_run` и `B24_WEBHOOK`.

**Домен на DuckDNS, а не `nip.io`/`sslip.io`.** 16.08.2026. Своего домена нет, Битрикс
принимает только HTTPS с валидным сертификатом, а у `nip.io`/`sslip.io` регулярно исчерпан
лимит выпуска Let's Encrypt. _Источник: `projects/bitrix-analitika.md`._

**Caddy отвергнут в пользу существующего nginx.** 16.08.2026. Порт 80 на 195.2.74.207 занят
nginx с чужими сайтами; второй веб-сервер сломал бы работающее. В `install.sh` добавлен режим
`--no-web`. _Источник: `projects/bitrix-analitika.md`._

**Добор потерянных лидов обязателен, а не опционален.** 16.08.2026. Чужой
`/root/renew-ip-cert.sh` раз в ~60 дней останавливает nginx на время обновления
IP-сертификата — в это окно вебхук не дойдёт. Подтвердилось шире: 20.08 портал отдавал 503
на `crm.lead.get`, и добор подобрал лиды через 4 минуты. _Источник: `projects/bitrix-analitika.md`._

**Обновление кода сервиса — только через `systemctl restart`.** 16.08.2026.
`systemctl enable --now` не перезапускает работающую службу, и после обновления в памяти
остаётся старая версия. _Источник: `projects/bitrix-analitika.md`._

**Спам помечаем сразу и в круг распределения не пускаем.** 20.08.2026. Иначе мусор съедает ход
в круге и менеджер вместо реального лида получает пустышку. Ответственного не меняем.
_Источник: `projects/bitrix-analitika.md`._

**Автоматически метим только машинный мусор, смысловой спам — нет.** 20.08.2026. Цена ошибки
несимметрична: живой клиент, помеченный спамом, исчезает молча. Три уровня — машинный мусор
автоматом, подозрение тегом, «не наш клиент» решает менеджер.
_Источник: `projects/bitrix-analitika.md`._

**Телефон проверяем на восстановимость, а не на формат.** 20.08.2026. Достраивается однозначно
(не написали код страны) — не спам; надо угадывать потерянную цифру — спам.
_Источник: `projects/bitrix-analitika.md`._

**Казахстанские коды `+7 6xx` / `+7 7xx` валидны, `noname` требует ещё и битого телефона.**
20.08.2026. Обе правки родились из проверки на дошедших до покупки: там нашлись и казахстанские
номера, и шесть купивших клиентов с именем `noname` и живым телефоном.
_Источник: `projects/bitrix-analitika.md`._

**Оговорка «текст обращения отменяет телефонные правила» выключена.** 20.08.2026. Механизм
написан и покрыт тестами, `min_request_text: 0`. Цена — 4 лида из 2676 за три недели уходят
в спам, два из них были живыми запросами про конкретные машины. Возврат — одна строка конфига.
_Источник: `projects/bitrix-analitika.md`._

**Любое новое правило сначала прогоняется на истории, а не включается сразу.** 20.08.2026.
`router.py --spam-backtest` показывает два числа: сколько нашлось бы среди помеченных людьми
(польза) и сколько пометилось бы среди остальных (риск). Второе — главное. Есть режим
`spam.mode: shadow` — считает и пишет в журнал, ничего не меняя.
_Источник: `projects/bitrix-analitika.md`._

**«Звонить после»: IP-город главнее кода номера.** 20.08.2026. Источники расходятся в 29%
случаев: код номера — регион выдачи симки когда-то, IP — где человек был в момент заявки.
Второе ближе к вопросу «когда ему удобно ответить». Плата: покрытие IP 32% против 93%
у телефона, поэтому телефон остаётся запасным. _Источник: `projects/bitrix-analitika.md`._

**В поле «Звонить после» пишем только время — «17:00».** 20.08.2026. Отвергнуты варианты
с регионом в скобках и с указанием времени клиента: чистую строку проще сортировать
и фильтровать в списке лидов. _Источник: `projects/bitrix-analitika.md`._

**Предел «Звонить после» — 18:00.** 20.08.2026. Менеджеры официально работают до шести,
всё, что вычислилось позже, прижимается к пределу. В него упирается только Калининград.
_Источник: `projects/bitrix-analitika.md`._

**Старые лиды полем «Звонить после» не переписываем.** 20.08.2026. Данные для расчёта есть,
но массовая запись не нужна — поле работает для новых лидов.
_Источник: `projects/bitrix-analitika.md`._

**Регион по номеру берём из реестра номерной ёмкости, а не из libphonenumber.** 20.08.2026.
`phonenumbers` для российских кодов 9xx отдаёт «Россия» и все шестнадцать часовых поясов —
непригодно. Реестр даёт 8012 диапазонов и определяет 93% номеров.
_Источник: `projects/bitrix-analitika.md`._

**Дубли не схлопываем: повторное обращение уходит тому же менеджеру, круг не двигается.**
20.08.2026. Предложение «второй лид за день не добавлять» проверили на месячном срезе: под него
попал бы 681 лид из 6000, из них 14 дошли до денег — и в 13 случаях из 14 деньги пришли со
ВТОРОГО лида, а первый менеджеры закрывали как дубль. Правило убивало бы рабочую карточку
и сохраняло мусорную. Вместо этого связываем карточки комментариями и не тратим ход в круге:
560 повторных за месяц (9.3% потока) больше не разбирают очередь.
_Источник: `projects/bitrix-analitika.md`._

**Указатель круга не двигается на повторных обращениях.** 20.08.2026. Прямое требование
Тимофея: три одинаковых заявки одному менеджеру не должны стоить ему очереди.
_Источник: `projects/bitrix-analitika.md`._

**Реестр Автосендера и память оконного завода — два разных хранилища, не смешиваем.**
20.08.2026. `/opt/knowledge` на 5.35.99.93 — реестр Автосендера (мото/авто, Битрикс
`synergosmoto`, реклама, Виртуальный РОП, lead-router). База `org` в PostgreSQL на
92.63.100.125 (MCP `org`) — оркестрация и оконный завод (Битрикс `ziverd`), другой бизнес.
Проекты, решения и открытые вопросы одного не переносятся в другое. По задачам Автосендера
читать и писать сюда, по заводу — в `org`.

**Онлайн-отбор распространён на будни 10:00–18:00 по Владивостоку.** 20.08.2026. Раньше только
выходные. Теперь три режима: `workhours-online`, `weekend-online` и `offhours-all`.
_Источник: `projects/bitrix-analitika.md`._

**Вне рабочего окна раздаём всем, а не онлайн.** 20.08.2026. Иначе единственный случайно
зашедший в 22:00 менеджер собрал бы весь ночной поток. Ночью лид всё равно не обработают
мгновенно — справедливее раздать по кругу.
_Источник: `projects/bitrix-analitika.md`._

## Правила счёта: дубли и статус «Недозвон» (21.08.2026)

**Статус «Недозвон» на карточке не считается доказательством, что клиенту не звонили.**
21.08.2026, замечание Тимофея, проверено на данных июня–июля 2026.

Заявка дублируется, дубль закрывают «Недозвоном» сразу, а звонки по клиенту лежат на
предыдущей карточке — иногда сделанные тем же менеджером. Статус ставится по карточке,
работа идёт по клиенту.

Проверка на 7 828 заявках (мобильные, без PARTNER): 26,1% всех заявок — дубли, медиана
разрыва 0,87 суток, 52% заводятся в первые сутки. Дубль достаётся тому же менеджеру
только в 12,7% случаев — круг раздаёт его другим. Из 2 647 «Недозвонов» 626 (23,6%) —
дубли, и у 94,7% из них по номеру есть исходящие в окне ±3 дня, у 86,3% — состоявшийся
разговор в окне ±7 дней. Даже на первых карточках: 88,9% и 74,9%.

Следствие для любого KPI: окно звонков открывать от первой карточки номера, а не от
текущей; «недозвоны» и «неотработанные» агрегировать по `phone_e164`; реальная потеря —
только `client_untouched` в `v_lead_with_client`.

Обратная сторона проверена: на выигранных сделках искажения нет — ответственный менеджер
держит 95,8% времени разговоров, сделок, сделанных чужими руками, нет. Дубли портят
оценку плохой работы, а не хорошей.

Полный текст правила — правило 7 в `claude/analytics-rules.md` проекта «Виртуальный РОП».

**Недозвон на непригодном номере не считается недозвоном.** 21.08.2026, замечание
Тимофея. Если у заявки битый или спамный телефон и по нему не было ни одного исходящего —
это мусор на входе, а не пропущенная работа менеджера. Такие карточки идут отдельной
строкой «нечего набирать» и не попадают ни в счётчик недозвонов, ни в знаменатель
по скорости реакции.

Проверка: за всё время 5 795 заявок с битым номером и 1 727 вообще без номера, «Недозвон»
стоит у 841 и 460 — 1 301 недозвон, которого физически не было. Июнь–июль 2026: 64 таких
«недозвона», у 44 (69%) нет ни одного исходящего. При этом у 585 из 861 заявки с битым
номером есть e-mail — связаться можно, позвонить нельзя.

Классификатор `phone_kind` ошибается в обе стороны: по 20 из 64 «битых» номеров реально
звонили (по одному — 126 раз), и с января 2025 одна продажа пришла с номера, помеченного
битым. Поэтому правильная развязка — отдельный статус «нет пригодного контакта», а не
«Недозвон»: снимает вину с менеджера и оставляет заявку видимой, если есть e-mail
или мессенджер. Полный текст — правило 1.1 в `claude/analytics-rules.md`.

**Признак «страницы доверия» внесён сразу в боевую модель, без параллельной обкатки.**
21.08.2026, решение Тимофея (предлагался вариант с отдельной моделью v2c). Цена решения:
замер точности v2b перезапущен, все оценки выставлены заново, маркировка в CRM переписана.
Сам признак — заход на `/about`, `/reviews`, `/how-buy`, `/contacts`, `/delivery`,
`/favourites`: конверсия 3,49% против 1,16%, эффект держится внутри групп по глубине
просмотра (значит не следствие «много листал»). Разделы каталога отвергнуты — все около базы.

## Реклама: поправка 21.08.2026

**`GoalId 13` в стратегии — не поломка, а режим «оптимизация по ценности».**
Отменена рекомендация переключать цель обучения кампании 713613428
(пункт 11 плана аудита 20.08). В новом интерфейсе цель обучения при
«Максимуме конверсий» не задаётся отдельно — берётся из списка «Целевые
действия и их ценность»; счётчик обучения показал 30 конверсий за неделю,
что сходится с суммой пяти приоритетных целей, а не с заявками (9–21)
и не с вовлечёнными сессиями (сотни). Отвергнут вывод, сделанный по
буквальному чтению API двумя разборами независимо.

**Настройки кампании во время обучения стратегии не менять.** Директ явно
пишет «Идёт обучение стратегии, не меняйте настройки». Правка обнуляет
накопленное. Применимо к 713613428 минимум до 25.08.

**Группу «Расчёт и пошлины» в тесте не отключать при нуле заявок.**
Ноль по цели 63078325 не означает ноль конверсий: страница калькулятора
за 18–20.08 дала 5 созданных заказов и 2 чата при отказах 15 %. Правило
проекта № 5 (считать всю воронку) отработало второй раз.

**`ru.yandex.weatherplugin` не запрещать** несмотря на 430 кликов и ноль
заявок за 3 дня: на 90-дневном окне 2 оплаты при 49 769 ₽, CPA 24 884 ₽ —
прибылен. Правило № 1 (полнота выборки).

_Источник: `projects/reklama.md`, чекпоинт 21.08.2026._

**Не переключаем подписные кампании на «Максимум конверсий».** 21.08.2026.
Конверсий формально хватает (33 и 89 в неделю), но: кампаниям 3 дня и
автостратегия ещё учится; ценность цели `sub` стоит 0,30 ₽ и сломает
конверсионную стратегию; главное — оптимизация на подписку системно смещает
показы в сегменты, которые лучше подписываются и хуже покупают (iOS, молодёжь,
столицы). Пересмотр — когда появится связь подписчик → заявка → оплата
через UTM в постах канала.

## Составная оценка заявки, 21.08.2026

**Оценка складывается в лог-шансах, а не в процентах с фиксированными весами.**
Постановка Тимофея была «до звонка вес 50, после звонка добавляем или снимаем».
Сделано иначе — по его же указанию «делай так, как будет точнее». Сложение
лог-шансов решает сразу две задачи: даёт правильную арифметику для редкого
события (база 1,14%) и естественно обрабатывает пропуски — блок без данных
даёт ровно ноль. Это и есть «нормировать по доступному» (выбор Тимофея из
трёх вариантов). Фактический вклад блоков в разброс вышел не 50/50, а
поведение до звонка 16%, разговоры 73%, отклик клиента 11% — потому что честная
проверка v2b даёт AUC 0,648, а наблюдаемая вовлечённость клиента 0,86.

**Числа попыток менеджера в оценке нет.** Это сильнейший фактор в данных
(5+ попыток дают 4,41% против 0,26%), но он управляется тем самым человеком,
которому оценку показывают: брошенный лид получил бы низкий балл, из-за низкого
балла его бросили бы вернее. Оценка измеряла бы усердие менеджера вместо
живости клиента. В блок «отклик» вошли только действия клиента.

**Корона появляется только после разговора** (выбор Тимофея из трёх вариантов).
До звонка работают ⚡ и ★ — «кому звонить первым». Корона — «разговор подтвердил
интерес, не отпускай». Два разных смысла не смешиваются, поэтому корона
заменяет значок класса, а не добавляется к нему.

**Отдельного сигнала «горячий лид стынет» не делаем** (решение Тимофея 21.08).
Менеджер видит балл и корону, остальное — его работа.

**Порог короны 60 баллов.** Из перебранных порогов даёт около восьми корон в день
на семь менеджеров при конверсии 11,24% против базы 1,14% и ловит две трети всех
продаж. Порог 80 точнее (19,11%), но теряет четверть продаж; порог 50 даёт
двенадцать корон в день и размывает сигнал.

**Значок ♛ (U+265B), а не 👑.** Проверено на живом лиде 21.08: 👑 (четыре байта)
обнуляет название лида в Битриксе, ♛ ♕ ♔ (три байта, BMP) проходят.

**Вес карточки разговора ужат на четверть** (0,324 → 0,243 лог-шанса на 10 баллов).
Критерии карточки отбирались на тех же 224 разговорах, на которых мерился вес;
контрольная выборка их подтвердила (AUC 0,737), но она мала — 50 разговоров.
Страховка на случай, что часть разрыва всё-таки подгонка.

**Сторож сигналит, реестр правит человек. Автоподгонки реестра под факт нет.**
26.08.2026. Обсуждалась идея ночью обходить сервер и автоматически пополнять реестр.
Отвергнута: по принципу из INDEX.md реестр — что задумано, `audit/` — что есть, и ценность
именно в расхождении. Робот, подтягивающий реестр под факт, стирает сигнал: тихо упавший
сервис будет записан как норма и никто не заметит. Плюс обход по ssh видит только
инфраструктуру — изменения в Директе, Метрике и Битриксе ему не видны вообще.
Схема: `watch.sh` шлёт алерт по порогам, `audit.sh` копит факты, разбор расхождений
и запись в реестр — ручные.

## 27.08.2026 — в СМС клиентам представляемся «Автосендер»

Решение Тимофея: в исходящих СМС менеджеры представляются от «Автосендер»,
НЕ «Синергос Мото». Запомнить для всех клиентских текстов рассылок.

Там же: вместо ссылки на канал MAX в приветствии — приглашение написать
менеджеру в MAX лично (вариант со ссылкой на профиль или «по этому номеру» —
выбирается при запуске).

**Дежурства по графику перекрывают круг и проверку онлайна.** 22.08.2026. В даты из
`duty.yaml`, окно 12:00–18:00 по Владивостоку, все новые лиды идут дежурному. График лежит
отдельным файлом и перечитывается на лету — продлевать его можно без перезапуска службы.
_Источник: `projects/bitrix-analitika.md`._

**Дежурные лиды не двигают указатель круга.** 22.08.2026. Дежурство — отдельная нагрузка
по графику; если бы смена съедала ходы, дежурный потом несколько дней пропускал бы свою
очередь. Очередь после смены продолжается с того же места.
_Источник: `projects/bitrix-analitika.md`._

**Повторное обращение: содержимое переносим в первую карточку, потом удаляем повтор.**
22.08.2026. Развитие решения от 20.08 — Тимофей попросил не плодить карточки. Порядок операций
принципиален: не удался перенос — лид остаётся в CRM. Копия удалённого лида пишется в свою
базу, потому что корзина CRM живёт 30 дней и при восстановлении меняет ID.
_Источник: `projects/bitrix-analitika.md`._

**Любую запись в CRM делаем идемпотентной.** 22.08.2026. Лид может прийти в обработчик дважды —
от робота и от добора. У спама защита была, у дублей нет, и на тесте это дало два одинаковых
комментария в карточке. Правило: перед любым изменением проверять, не сделано ли уже.
_Источник: `projects/bitrix-analitika.md`._

**Перед любой работой, которая тратит токены LLM, — сначала называем цену.** 01.09.2026.
Просьба Тимофея. Любой прогон через ProxyAPI (пересчёт разборов, новые поля в промпте,
эксперименты на выборке, бэкфилл) сначала оценивается в рублях и показывается ему
до запуска: сколько разборов × средние токены × тариф, отдельно разовая стоимость
и прирост к ежемесячным расходам. Тарифы ProxyAPI с НДС на 01.09.2026:
gemini-3.7-flash — 455 ₽/1М вход, 2 275 ₽/1М выход; gemini-3.5-flash-lite — 91 / 758.
Батч-обработка даёт −50%. Фактический средний разбор на 3.7-flash: 3 280 вход /
608 выход ≈ 2,9 ₽; на lite: 2 247 / 303 ≈ 0,4 ₽. Всегда предлагать более дешёвый
вариант (только вперёд без бэкфилла, батч, lite) с явной ценой каждого.
_Источник: этот чат, 01.09.2026._

## [02.09.26] Порог безубыточного CPA цели «Заказ оплачен+» = 85 000 ₽ (было 100 000)
Основание: цель срабатывает на стадии лида «Ждём договор», конверсия стадии в покупку ≈ 85 % (stage_history, май–июль 2026).
Ценность цели в Метрике → 85 000. Ранжир кампаний не меняется.

**Продажа = переход лида в стадию «ТС куплен» (stage_id='13').** 01.09.2026.
Решение Тимофея после сверки: сделки `deals` со stage_semantic='S' по людям
расходятся с его учётом (Жернов 8 против 13, Симаненко 2 против 7), хотя сумма
по отделу совпадает; лид→CONVERTED завышает в разы (35–42 в месяц против 6–14),
потому что это конвертация, а не покупка. Стадия 13 совпала с его цифрами
у Жернова (13) и Попова (9) точно, у Пономаря и Томаша ±1. Расхождение
у Симаненко (12 против 7) списано на ручные клики в CRM — «может чутка
распадаться», за основу всё равно берём стадию 13.
Считаем по первому попаданию в стадию (`min(created_time)`), ответственный —
менеджер лида. Так теперь считают Visual (продажи недели), воронка (стадия
«Продажа») и колонка «Продано в этот период».
_Источник: этот чат, 01.09.2026._

**Продажу подтверждает РОП кнопкой в боте.** 02.09.2026. Решение Тимофея после
разбора: у Симаненко 5 из 12 августовских «ТС куплен» — свежие заявки, прыгнувшие
в конец воронки за часы, без торгов и без сделки. Теперь каждый новый переход лида
в стадию 13 уходит Светлане вопросом «Подтвердите покупку авто» с кнопками
«Да»/«Нет» (`app/sale_watch.py`, крон каждые 10 минут; ответы ловит tg_register.py
через callback_query). Счёт продаж на панелях идёт из вьюхи `v_sales`: «да» —
засчитано, «нет» — нет, пока ответа нет — работает автоправило «лид был на торгах
И есть связанная сделка». Это же автоправило считает всю историю до 02.09 —
задним числом вопросами не заваливаем. Проверка на августе: 13 / 9 / 5 / 9 / 7
(Жернов / Попов / Пономарь / Томаш / Симаненко) — совпало с ручным учётом Тимофея.
_Источник: этот чат, 02.09.2026._

## 2026-09-07 — М8 Егор Егоров: первый рабочий день 07.09.2026
Шестой менеджер отдела (ID 15079, manager8@autosender.ru). Точка отсчёта для всех
сравнений: до 07.09 у него нет заявок и разговоров, любые «средние по отделу»
за окна, захватывающие август, считаются с ним как с новичком. Событие —
`pulse.events` id 7.

## [08.09.26] Тест «Поиск Ядро» 713613428 — остановить (пауза)
0 договоров на 226 тыс ₽ при зрелости лидов 75 %; ожидание 2–3. Откат: ≥2 договора до 30.09 → этап 2 (режим по ценности).
## [08.09.26] Поправка правила 3: лаг заявка→договор = 2 дня (медиана), 92 % за 21 день. Судить по договорам через 3 недели.

## [14.09.26] КТ5: Я-6 → 50 000, Я-3 → 110 000 без НДС (рекомендовано); Корея-2 держать 75 000; фид Японии (кв. коллаж) — оставить
Откаты: Я-6 ≥2 договора до 28.09 при CPL <3 000 → 70 000; Я-3 CPL недели >2 500 → 90 000; Корея-2 повтор CPL >3 000 → 60 000.

## [13.09.26] Реестр и код РОПа — под git с зеркалом на GitHub, сайт остаётся на GitLab
Цель — чтобы Claude и Codex стартовали с одного контекста (внесено Тимофеем).
Два приватных репозитория: `autosender-knowledge` и `autosender-ropbot`. Монорепо отклонено:
код рекламы, Битрикса и РОПа физически в одном `app/`, делит `common.py` и одну базу, а машины разные.
Тестовый контур для ropbot не поднимаем — правим наживую (внесено Тимофеем).
Веток нет, только `main` и прямой коммит без PR: рабочая копия одна и она же прод,
переключение ветки меняло бы файлы под вторым агентом и под шестью контейнерами.
Codex правит код через MCP `ropbot` на сервере, а не из локального клона — иначе
возвращается деплой через `git pull`, от которого отказались. Клон на маке — только для чтения.
Откат: если два агента начнут регулярно затирать друг друга в `dash.py` — вернуть клоны и `git pull` на сервер.

## [15.09.26] Развесовка целевых действий в ТК вместо правки ценности цели в Метрике
Цены в поле «Средняя цена за достижение цели» (без НДС): Заказ оплачен+ 20 500, Заказ создан+ 500,
Подобрать автомобиль+ 400, Звонок 500, Venoo 200. Исходная точка — целевой CPA договора 25 000 ₽ с НДС.
Внедрять на одной ТК (Я-2), контроль через 2 недели, откат — «Без ограничений».

## [15.09.26] Развесовка целей Я-2 — вариант C под ограничение Яндекса «цена × 10 ≤ недельный бюджет»
В поля (без НДС): Заказ оплачен+ 7 000 (максимум при бюджете 70 000), Заказ создан+ 700,
Подобрать автомобиль+ 570, Звонок 570, Venoo 280. Запас по допустимой цене −1 %, вес договора 1,24 % → 13,5 %.
Цена 20 500 недостижима: требует бюджета 205 000 — аргумент к консолидации японских ТК.

## [17.09.26] Ревизия настроек: три шага сейчас, консолидация до конца октября (РЕКОМЕНДОВАНО, ждёт Тимофея)
Сейчас: Корея-2 75 000 → 50 000 б/НДС (маржинальный CPL 9 946 > 6 700); Я-6 стоп (1 договор на 255 311 ₽
за 4 нед); освобождённые 75 000 → Я-3 135 000, Я-НОВАЯ 125 000, 25 000 придержать до 19.09 (Китай).
Не трогать: Я-2 до 06.10 (замер; при сдвиге A+B < 5 п.п. — стоп и 70 000 → Я-3/Я-НОВАЯ), Корея до разбора
конверсии (0,58 % против 1,74 % у Японии), Китай до 19.09.
Конечная картина: Япония 4 → 2 ТК (Я-3 ~185 000, Я-НОВАЯ ~145 000), Корея 2 → 1, Китай 1.
Откаты: каждый перелив — по маржинальному CPL получателя, порог 6 700, один шаг в неделю.

## [17.09.26] Порог маржинального CPL — не константа 6 700, а 85 000 × фактическая конверсия заявка→договор
Пересчитывать на каждой КТ по зрелым 3 неделям. При конверсии 3 % порог 2 550, и три ТК из семи (Я-2, Китай,
Я-6) уже в минусе; при 7,9 % — 6 700 и в плюсе все. Пока конверсия ниже 5–6 %, бюджет выше ~520 000 б/НДС
не наращивать — только перелив и консолидация.
